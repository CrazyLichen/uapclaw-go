# 上游 github.com/amikos-tech/chroma-go-local 重新发布了 v0.3.4，
# 导致不同 Go 模块代理缓存的包 hash 不一致。
# go.sum 中记录的是 proxy.golang.org 的 hash，
# 本地开发使用 goproxy.cn 时 hash 可能不匹配，
# 需在本地环境变量中设置 GONOSUMDB=github.com/amikos-tech/chroma-go-local
# 并清除本地模块缓存中该包的缓存后重新下载。
GONOSUMDB=github.com/amikos-tech/chroma-go-local

# 上游 github.com/amikos-tech/chroma-go-local 重新发布了 v0.3.4，
# 导致 goproxy.cn（本地默认）与 proxy.golang.org（CI 默认）缓存的包 hash 不一致。
# 统一 GOPROXY 确保本地和 CI 下载到相同内容的模块。
# GONOSUMCHECK 跳过该依赖的 sumcheck 验证。
GOPROXY=https://goproxy.cn,direct
GONOSUMCHECK=github.com/amikos-tech/chroma-go-local
GONOSUMDB=github.com/amikos-tech/chroma-go-local

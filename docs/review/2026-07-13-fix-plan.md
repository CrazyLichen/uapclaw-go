# 7.13 Review 问题修复计划

> 生成日期：2026-07-13
> 基于：`docs/review/2026-07-13-logic-review.md` 验证后仍存在的问题
> 原则：逐项对照 Python 源码确认，严格对齐实现步骤

---

## 修复清单总览

| 编号 | 模块 | 级别 | 问题描述 | 状态 |
|------|------|------|---------|------|
| S1 | sys_operation | 🔴 P0 | Shell 安全检查正则缺少 `(?i)` | ✅ |
| S2 | sys_operation | 🔴 P0 | ShellType.Cmd 非 Windows panic | ✅ |
| S27 | WebChannel | 🔴 P0 | config_apply 缺少 api_key/token 加密 | ✅ |
| S25 | WebChannel | 🔴 P0 | handleCancel 无条件取消流式任务 | ✅ |
| S21 | Gateway | 🔴 P0 | cancel 不通知 AgentServer | ✅ |
| S3 | sys_operation | 🔴 P1 | Shell 进程注册时机错误 | ✅ |
| S20 | Gateway | 🟡 P1 | 缺少 CHAT_ANSWER 串行保护 | ✅ |
| S22 | Gateway | 🟡 P1 | handleChatUserAnswer 忽略 error | ✅ |
| S23 | Gateway | 🟡 P1 | disconnect 缺少 session_id 去重 | ✅ |
| S16 | DeepAdapter | 🟡 P1 | _is_acp_tool_profile 实现不一致 | ✅ |
| S4 | sys_operation | 🟡 P2 | sandbox fallback 逻辑不一致 | ✅ |
| S5 | sys_operation | 🟡 P2 | read_file 缺少互斥参数校验 | ✅ |
| S6 | sys_operation | 🟡 P2 | tail 模式全文读取 | ✅ |
| S7 | sys_operation | 🟡 P2 | write_file 缺少文件锁 | ✅ |
| S8 | sys_operation | 🟡 P2 | WriteStdin/ListProcesses 未实现 | ✅ |
| S9 | DeepAdapter | 🟡 P2 | 缺少 SetSkillManager 方法 | ✅ |
| S11 | DeepAdapter | 🟡 P2 | ProcessInterrupt 响应缺少字段 | ✅ |
| G2 | sys_operation | 🟡 P2 | search_files 缺少 exclude_patterns | ✅ |
| G3 | sys_operation | 🟡 P2 | list_files/list_directories 缺少 max_depth | ✅ |
| G4 | sys_operation | 🟡 P2 | CodeOperation 硬编码 python3 | ✅ |
| G5 | sys_operation | 🟡 P2 | write_file 非 UTF-8 编码简化 | ✅ |
| G6 | sys_operation | 🟡 P2 | isolation_key_template 连续下划线 | ✅ |

**暂保留占位（依赖其他模块回填）**：S10, S12, S13, S14, S15, S17

**已验证无需修复**：S19, S26, S18, G1, G11, G14, G15, G22, G24, G25

---

## 一、P0 级别修复

### 1.1 S1：Shell 安全检查正则缺少 `(?i)`

**文件**：`internal/agentcore/sys_operation/local/shell_operation.go`

**修改内容**：
1. 所有内置 `dangerousPatterns` 正则前加 `(?i)` 前缀
   - 第 631 行：`\brm\s+-rf\b` → `(?i)\brm\s+-rf\b`
   - 第 632 行：`\bdel\s+/[a-z]*[fsq][a-z]*\b` → `(?i)\bdel\s+/[a-z]*[fsq][a-z]*\b`
   - 第 633-644 行：所有内置模式均加 `(?i)`
2. `initCustomDangerousPatterns` 中自定义模式包裹 `(?i)`
   - 第 698 行：`re, err := regexp.Compile(rawPattern)` → `re, err := regexp.Compile("(?i)" + rawPattern)`

**对照 Python**：`re.compile(r"\brm\s+-rf\b", re.IGNORECASE)` + 自定义 `re.search(raw_pattern, command, re.IGNORECASE)`

---

### 1.2 S2：ShellType.Cmd 非 Windows 下 panic

**文件**：`internal/agentcore/sys_operation/local/shell_operation.go`

**修改内容**：
1. 修改 `resolveExecutionPlan` 返回签名，增加 `error` 返回值：
   ```go
   func (s *LocalShellOperation) resolveExecutionPlan(command string, shellType sysop.ShellType) (args []string, isShellCmd bool, shellName string, err error)
   ```
2. `ShellTypeCmd` 非 Windows 分支改为返回 error：
   ```go
   case sysop.ShellTypeCmd:
       if !isWindows {
           return nil, false, "", exception.New(...)
       }
   ```
3. 修改 3 个调用点（ExecuteCmd 第 185 行、ExecuteCmdStream 第 345 行、ExecuteCmdBackground 第 483 行），检查 error 后提前返回

**对照 Python**：`raise build_error(StatusCode.SYS_OPERATION_SHELL_EXECUTION_ERROR, ...)`

---

### 1.3 S27：config_apply 缺少 api_key/token 加密

**文件**：`internal/swarm/gateway/channel_manager/web/config_apply.go`

**修改内容**：
1. `ApplyConfigPayload` 增加 `crypto CryptoProvider` 参数：
   ```go
   func ApplyConfigPayload(params map[string]any, crypto CryptoProvider) (envUpdates map[string]string, yamlUpdated []string, err error)
   ```
2. 在函数开头对齐 Python `_encrypt_config_params`，加密含 `api_key`/`token` 的字段：
   ```go
   // 对齐 Python _encrypt_config_params: 遍历 params，key 名包含 api_key 或 token 时加密
   for key, val := range params {
       kl := strings.ToLower(key)
       if (strings.Contains(kl, "api_key") || strings.Contains(kl, "token")) && crypto != nil {
           if strVal, ok := val.(string); ok && strVal != "" {
               params[key] = crypto.Encrypt(strVal)
           }
       }
   }
   ```
3. 更新调用方，传入 `CryptoProvider`

**对照 Python**：`_apply_config_payload` 第一行 `params = _encrypt_config_params(params)`，判断 `"api_key" in key.lower() or "token" in key.lower()`

---

### 1.4 S25：handleCancel 无条件取消流式任务

**文件**：`internal/swarm/server/handle_envelope.go`

**修改内容**：
1. 在 `handleCancel` 中提取 intent：
   ```go
   intent := ""
   if request.Params != nil {
       var params map[string]any
       if json.Unmarshal(request.Params, &params) == nil {
           if i, ok := params["intent"].(string); ok {
               intent = i
           }
       }
   }
   ```
2. 仅在 `intent == "cancel" || intent == "supplement"` 时调用 `cancelStreamTask`：
   ```go
   if intent == "cancel" || intent == "supplement" || intent == "" {
       s.cancelStreamTask(sessionID)
   }
   ```
   注：`intent == ""` 保持向后兼容，无 intent 时默认取消

**对照 Python**：`if intent in ("cancel", "supplement"): stream_task.cancel()`

---

### 1.5 S21：cancel 不通知 AgentServer

**文件**：`internal/swarm/gateway/message_handler/cancel.go`

**修改内容**：
1. 移除 `len(requestIDs) > 0` 条件，改为只检查 `sessionID` 非空：
   ```go
   if mh.agentClient != nil && mh.agentClient.IsConnected() {
       // 无论是否有活跃流式任务，都通知 AgentServer
       // 对齐 Python：即使网关侧已无活跃流式拉取任务，也必须通知 AgentServer
       ...
   }
   ```

**对照 Python**：`_cancel_agent_work_for_session` 中，只要 `old_sid` 非空就发送 cancel，注释 "即使网关侧已无活跃流式拉取任务，也必须通知 AgentServer"

---

## 二、P1 级别修复

### 2.1 S3：Shell 进程注册时机错误

**文件**：`internal/agentcore/sys_operation/local/shell_operation.go`

**修改内容**：
1. `ExecuteCmd` 中：在 `handler.Invoke(ctx)` 前注册进程，使用 `defer` 注销
   ```go
   // 对齐 Python: proc = await _create_subprocess → track_sid = _track_shell_process(proc) → try: invoke → finally: _untrack
   if handler.cmd.Process != nil {
       sid := trackShellProcess(ctx, handler.cmd.Process)
       defer untrackShellProcess(sid, handler.cmd.Process)
   }
   invokeData, err := handler.Invoke(ctx)
   ```
2. `ExecuteCmdBackground` 同理调整
3. `ExecuteCmdStream` 中 stream 模式确认注册时机正确

**对照 Python**：`track_sid = _track_shell_process(proc)` → `try: invoke_data = await process_handler.invoke()` → `finally: _untrack_shell_process(track_sid, proc)`

---

### 2.2 S20：缺少 CHAT_ANSWER 串行保护

**文件**：`internal/swarm/gateway/message_handler/dispatch.go`

**修改内容**：
1. `nonStreamRPCMayRunParallel` 中添加 `chat.user_answer` 到串行保护列表：
   ```go
   return method != string(schema.ReqMethodChatSend) &&
       method != string(schema.ReqMethodChatCancel) &&
       method != "chat.resume" &&
       method != string(schema.ReqMethodChatAnswer)
   ```

**对照 Python**：`return m not in (CHAT_SEND, CHAT_RESUME, CHAT_CANCEL, CHAT_ANSWER)`

---

### 2.3 S22：handleChatUserAnswer 忽略 error

**文件**：`internal/swarm/gateway/message_handler/forward_loop.go`

**修改内容**：
1. 第 191 行改为检查 error：
   ```go
   resp, err := mh.processNonStreamRequest(ctx, msg, env)
   if err != nil {
       logger.Warn(logComponent).
           Str("event_type", "chat_answer_process_failed").
           Str("msg_id", msg.ID).
           Err(err).
           Msg("processNonStreamRequest 失败，跳过 evolution 审批")
       return
   }
   ```

**对照 Python**：`_process_non_stream_request` 内部 try/except，失败返回 None；Go 中也提前返回

---

### 2.4 S23：disconnect 缺少 session_id 去重

**文件**：`internal/swarm/gateway/message_handler/disconnect.go`

**修改内容**：
1. 添加 `seen` set 去重：
   ```go
   seen := make(map[string]struct{})
   for _, key := range sessionKeys {
       channelID := key[0]
       sessionID := key[1]
       if sessionID == "" {
           continue
       }
       sid := strings.TrimSpace(sessionID)
       if sid == "" {
           continue
       }
       if _, ok := seen[sid]; ok {
           continue  // 已处理，跳过
       }
       seen[sid] = struct{}{}
       // ... 原有取消逻辑
   }
   ```

**对照 Python**：`seen: set[str] = set()` + `if not sid or sid in seen: continue` + `seen.add(sid)`

---

### 2.5 S16：_is_acp_tool_profile 实现不一致

**文件**：`internal/swarm/server/adapter/deep_adapter_config.go`

**修改内容**：
1. 修改 `isAcpToolProfile` 签名和实现，从 `instanceOverrides` 读取：
   ```go
   func (d *DeepAdapter) isAcpToolProfile(instanceOverrides map[string]any) bool {
       if instanceOverrides == nil {
           return false
       }
       // 优先检查 tool_profile
       if tp, ok := instanceOverrides["tool_profile"]; ok {
           s := strings.TrimSpace(strings.ToLower(fmt.Sprint(tp)))
           if s != "" {
               return s == "acp"
           }
       }
       // fallback 检查 channel_id
       if cid, ok := instanceOverrides["channel_id"]; ok {
           s := strings.TrimSpace(strings.ToLower(fmt.Sprint(cid)))
           return s == "acp"
       }
       return false
   }
   ```
2. 更新所有调用方，传入 `instanceOverrides` 而非 `configBase`

**对照 Python**：`tool_profile = str(config.get("tool_profile") or "").strip().lower()` → `if tool_profile: return tool_profile == "acp"` → fallback `channel_id == "acp"`

---

## 三、P2 级别修复

### 3.1 S4：sandbox fallback 逻辑不一致

**文件**：`internal/agentcore/sys_operation/local/fs_operation.go`

**修改内容**：
1. `resolvePath` 中 sandbox fallback 改为 `[getWorkspace(), getProjectRoot()]`：
   ```go
   if len(sandboxRoots) == 0 {
       sandboxRoots = []string{}
       if ws := getWorkspace(); ws != "" {
           sandboxRoots = append(sandboxRoots, ws)
       }
       if pr := getProjectRoot(); pr != "" {
           sandboxRoots = append(sandboxRoots, pr)
       }
   }
   ```

**对照 Python**：`roots = [p for p in (get_workspace(), get_project_root()) if p]`

---

### 3.2 S5：read_file 缺少互斥参数校验

**文件**：`internal/agentcore/sys_operation/local/fs_operation.go`

**修改内容**：
1. 在 `ReadFile` 入口添加互斥参数校验：
   ```go
   // tail 和 head 互斥
   if o.Tail > 0 && o.Head > 0 {
       return nil, exception.New(StatusSysOperationFsParamError, "head and tail are mutually exclusive")
   }
   // tail 和 line_range 互斥
   if o.Tail > 0 && o.LineRange != "" {
       return nil, exception.New(StatusSysOperationFsParamError, "tail and line_range are mutually exclusive")
   }
   // bytes 模式不允许 head/tail/line_range
   if o.Mode == "bytes" && (o.Head > 0 || o.Tail > 0 || o.LineRange != "") {
       return nil, exception.New(StatusSysOperationFsParamError, "bytes mode does not support head/tail/line_range")
   }
   ```

**对照 Python**：`validate_mutually_exclusive` + `validate_binary_mode`

---

### 3.3 S6：tail 模式实现反向 seek

**文件**：`internal/agentcore/sys_operation/local/fs_operation.go`

**修改内容**：
1. 实现 `readTail` 函数，从文件末尾反向 seek 读取，对齐 Python `_read_tail`：
   ```go
   func readTail(filePath string, tail int) ([]string, error) {
       // 1. 打开文件，seek 到末尾
       // 2. 反向逐块读取（TAIL_CHUNK_SIZE = 8192）
       // 3. 解析行，收集 tail 行
       // 4. 返回最后 tail 行
   }
   ```
2. `ReadFile` 中 tail 分支改为调用 `readTail`

**对照 Python**：`_read_tail(file_path, tail, encoding)` — `f.seek(0, os.SEEK_END)` + 反向读取 + 行收集

---

### 3.4 S7：write_file 缺少跨进程文件锁

**文件**：新增 `internal/agentcore/sys_operation/local/file_lock.go` + `file_lock_unix.go` + `file_lock_windows.go`

**修改内容**：
1. 实现 per-file 跨进程文件锁，对齐 Python `_file_lock`：
   - `fileLock` 结构体：锁文件路径、fd、超时（10s）
   - Unix 实现：`syscall.Flock(fd, LOCK_EX|LOCK_NB)` + 轮询超时（50ms 间隔，10s deadline）
   - Windows 实现：`syscall.LockFileEx` + 轮询超时
   - 使用 `//go:build` 标签分离平台实现
2. `WriteFile` 中在写入前获取文件锁，defer 释放：
   ```go
   lock, err := acquireFileLock(resolvedPath, 10*time.Second)
   if err != nil {
       return nil, exception.New(...)
   }
   defer releaseFileLock(lock)
   ```

**对照 Python**：`_file_lock(data_path)` — `fcntl.flock(fd, LOCK_EX|LOCK_NB)` + 轮询 + 10s 超时 + Windows `msvcrt.locking`

---

### 3.5 S8：WriteStdin 和 ListProcesses 实现

**文件**：`internal/agentcore/sys_operation/local/shell_operation.go` + `shell_process_registry.go`

**修改内容**：
1. `ShellProcessRegistry` 增加接口：
   - `ListProcesses() []ProcessInfo` — 遍历注册表返回进程列表
   - 注册时同时保存 `stdin pipe` 引用
2. `WriteStdin` 实现：通过 sessionID 查找进程的 stdin pipe，写入数据
3. `ListProcesses` 实现：调用 `registry.ListProcesses()` 返回进程信息

**对照 Python**：`write_stdin` — 查找 stdin pipe 写入；`list_processes` — 遍历 ShellProcessRegistry

---

### 3.6 S9：缺少 SetSkillManager 方法

**文件**：`internal/swarm/server/adapter/deep_adapter.go`

**修改内容**：
1. 添加 `SetSkillManager` 方法，类型使用 runtime/skill 包的 `*skill.SkillManager`：
   ```go
   func (d *DeepAdapter) SetSkillManager(skillMgr *skill.SkillManager) {
       d.skillManager = skillMgr
   }
   ```
2. 修改 `skillManager` 字段类型从 `interface{}` 改为 `*skill.SkillManager`

**对照 Python**：`def set_skill_manager(self, skill_manager: SkillManager) -> None: self._skill_manager = skill_manager`

**注意**：facade（`uapclaw.go` 第445-447行）已准备好调用 `SetSkillManager`，通过接口断言 `interface{ SetSkillManager(*skill.SkillManager) }`，只需 DeepAdapter 实现此方法即可接通。

---

### 3.7 S11：ProcessInterrupt 响应缺少字段

**文件**：`internal/swarm/server/adapter/deep_adapter.go`

**修改内容**：
1. 在 `ProcessInterrupt` 返回的 payload 中补充 3 个字段：
   ```go
   payload := map[string]any{
       "event_type": "chat.interrupt_result",
       "intent":     intent,
       "success":    true,
       "message":    interruptMsg,
       "new_input":  newInput,  // 已提取，直接写入
   }
   // ⤵️ 10.6.3-10: todos 和 cancelled_tools 依赖 StreamEventRail 实现
   if d.streamEventRail != nil {
       // ⤵️ 10.6.3-10: payload["todos"] = ...
       // ⤵️ 10.6.3-10: payload["cancelled_tools"] = ...
   }
   ```

**对照 Python**：`payload = {"event_type": "chat.interrupt_result", "intent": intent, "success": success}` + 有条件补充 `new_input`/`todos`/`cancelled_tools`

---

### 3.8 G2：search_files 缺少 exclude_patterns

**文件**：`internal/agentcore/sys_operation/local/fs_operation.go`

**修改内容**：
1. `SearchFiles` 中匹配完成后，过滤 exclude_patterns：
   ```go
   if len(o.ExcludePatterns) > 0 {
       filtered := matchedPaths[:0]
       for _, p := range matchedPaths {
           excluded := false
           for _, ep := range o.ExcludePatterns {
               if matched, _ := filepath.Match(ep, filepath.Base(p)); matched {
                   excluded = true
                   break
               }
           }
           if !excluded {
               filtered = append(filtered, p)
           }
       }
       matchedPaths = filtered
   }
   ```

**对照 Python**：`exclude_set = set(); for pat in exclude_patterns: exclude_set.update(set(base.rglob(pat))); matched_paths = [p for p in matched_paths if p not in exclude_set]`

---

### 3.9 G3：list_files/list_directories 缺少 max_depth

**文件**：`internal/agentcore/sys_operation/local/fs_operation.go`

**修改内容**：
1. `walkDir` 增加当前深度参数，超过 MaxDepth 时停止递归：
   ```go
   func (f *LocalFsOperation) walkDir(o FsOptions, base string, depth int, result *[]string) {
       if o.MaxDepth > 0 && depth > o.MaxDepth {
           return
       }
       // ... 原有逻辑，递归调用时 depth+1
   }
   ```

**对照 Python**：`max_depth` 递归深度限制

---

### 3.10 G4：CodeOperation 硬编码 python3

**文件**：`internal/agentcore/sys_operation/local/code_operation.go`

**修改内容**：
1. 优先读取 `PYTHON_EXECUTABLE` 环境变量，fallback 到 `python3`：
   ```go
   func resolvePythonExecutable() string {
       if exe := os.Getenv("PYTHON_EXECUTABLE"); exe != "" {
           return exe
       }
       return "python3"
   }
   ```
2. 替换硬编码 `"python3"` 为 `resolvePythonExecutable()`

**对照 Python**：`sys.executable` 动态获取当前 Python 解释器路径

---

### 3.11 G5：write_file 非 UTF-8 编码处理

**文件**：`internal/agentcore/sys_operation/local/fs_operation.go`

**修改内容**：
1. 引入 `golang.org/x/text/encoding` 包
2. `WriteFile` 中非 UTF-8 编码时使用编码转换：
   ```go
   if o.Encoding != "" && !strings.EqualFold(o.Encoding, "utf-8") && !strings.EqualFold(o.Encoding, "utf8") {
       enc := encoding.ByName(o.Encoding)
       if enc != nil {
           dataBytes, err = enc.NewEncoder().Bytes([]byte(txt))
           // ...
       }
   }
   ```

**对照 Python**：`data_bytes = txt.encode(encoding)`

---

### 3.12 G6：isolation_key_template 连续下划线

**文件**：`internal/agentcore/sys_operation/sys_operation_card.go`

**修改内容**：
1. `generateIsolationKeyTemplate` 中过滤空部分，手动拼接：
   ```go
   // 对齐 Python: prefix = f"{isolation_prefix}_" if isolation_prefix else ""
   var parts []string
   parts = append(parts, containerScope)
   if launcherType != "" { parts = append(parts, launcherType) }
   if sandboxType != "" { parts = append(parts, sandboxType) }
   if isolationPrefix != "" { parts = append(parts, isolationPrefix) }
   parts = append(parts, identity)
   return strings.Join(parts, "_")
   ```

**对照 Python**：`prefix = f"{isolation_prefix}_" if isolation_prefix else ""` → `f"{container_scope.value}_{launcher_type}_{sandbox_type}_{prefix}{identity}"`

---

## 四、暂保留占位的问题

以下问题依赖其他模块回填，保留 `⤵️` 注释，不在本次修复范围：

| 编号 | 问题描述 | 依赖项 |
|------|---------|--------|
| S10 | ProcessInterrupt 缺少 StreamEventRail 调用 | StreamEventRail 实现 (10.6.3-10) |
| S12 | Team/AutoHarness/Slash 分流 | Team/AutoHarness/SkillEvolutionRail |
| S13 | CreateInstance 缺少 sys_operation/subagents | sysop_builder / subagents (10.3.7-11) |
| S14 | CreateInstance 缺少 _get_tool_cards | agentcore tool_cards 回填 |
| S15 | buildAgentIdentityPrompt 返回空 | 10.6.1-2 身份提示词构建 |
| S17 | A2X 客户端空实现 | A2X 协议实现 (11.10) |

---

## 五、修改文件清单

| 文件 | 修改项 |
|------|--------|
| `internal/agentcore/sys_operation/local/shell_operation.go` | S1, S2, S3, S8 |
| `internal/agentcore/sys_operation/shell_process_registry.go` | S8 |
| `internal/agentcore/sys_operation/local/fs_operation.go` | S4, S5, S6, S7, G2, G3, G5 |
| `internal/agentcore/sys_operation/local/file_lock.go` (新) | S7 |
| `internal/agentcore/sys_operation/local/file_lock_unix.go` (新) | S7 |
| `internal/agentcore/sys_operation/local/file_lock_windows.go` (新) | S7 |
| `internal/agentcore/sys_operation/local/code_operation.go` | G4 |
| `internal/agentcore/sys_operation/sys_operation_card.go` | G6 |
| `internal/swarm/gateway/channel_manager/web/config_apply.go` | S27 |
| `internal/swarm/gateway/channel_manager/web/web_handlers.go` | S27 调用方 |
| `internal/swarm/server/handle_envelope.go` | S25 |
| `internal/swarm/gateway/message_handler/cancel.go` | S21 |
| `internal/swarm/gateway/message_handler/dispatch.go` | S20 |
| `internal/swarm/gateway/message_handler/forward_loop.go` | S22 |
| `internal/swarm/gateway/message_handler/disconnect.go` | S23 |
| `internal/swarm/server/adapter/deep_adapter.go` | S9, S11 |
| `internal/swarm/server/adapter/deep_adapter_config.go` | S16 |

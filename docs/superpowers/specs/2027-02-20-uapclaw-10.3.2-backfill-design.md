# 10.3.2 UapClaw 门面回填对齐设计

## 1. 背景与目标

10.3.2 章节实现了 `UapClaw` 门面（对齐 Python `JiuWenClaw`），但留有 12 个 `⤵️` 回填标记。经逐项审查，确认哪些回填点可以立即回填、哪些需要保留等待依赖章节。

**目标**：对齐 Python `jiwenswarm/server/runtime/agent_adapter/interface.py` 中 `JiuWenClaw` 的完整逻辑，补全缺失的云端记忆钩子、Team 模式判断、Dreaming 生命周期、InteractiveInput 构造等。

## 2. 回填决策汇总

### 2.1 立即回填（不依赖外部未实现模块）

| # | 回填点 | 位置 | Python 对齐行号 |
|---|--------|------|-----------------|
| 1 | 云端记忆：MemoryBlocks 拼接注入 system prompt | uapclaw.go:172 | interface.py:843-845 |
| 2 | 云端记忆：ProcessMessage 后钩子 | uapclaw.go:208 | interface.py:866-877 |
| 3 | 云端记忆：ProcessMessageStream 前钩子 | uapclaw.go:264 | interface.py:951-963 |
| 4 | 云端记忆：ProcessMessageStream 后钩子 | uapclaw.go:381 | interface.py:1134-1146 |
| 5 | Team 模式判断（isTeamMode / isAutoHarnessResume） | uapclaw.go:244 | interface.py:909-918 |
| 6 | Team 模式使用原始 query + BuildInputs 三条路径完整对齐 | uapclaw.go:245 + build_inputs.go:83-84 | interface.py:940-949 + 391-415 |
| 7 | Auto-Harness resume 绕过 Session 队列 | uapclaw.go:388 | interface.py:1007-1012 |
| 10 | CreateInstance 启动 dreaming | uapclaw.go:501 | interface.py:319-322 |
| 11 | ReloadAgentConfig 前停止 dreaming | uapclaw.go:515 | interface.py:336-337 |
| 12 | ReloadAgentConfig 后重启 dreaming | uapclaw.go:519 | interface.py:340-343 |
| 13 | G33: SetSkillnetInstallCompleteHook | uapclaw.go:594 | interface.py:288-289 |
| 15 | BuildInputs InteractiveInput 类型守卫 | build_inputs.go:83 | interface.py:391 |

### 2.2 部分回填（实现骨架，内部逻辑留 ⤵️）

| # | 回填点 | 位置 | 可实现部分 | ⤵️ 等待 |
|---|--------|------|-----------|---------|
| 8 | Team 模式分流 _processTeamInterrupt | uapclaw.go:401 | isTeamMode 判断 + resume 完整实现 | TeamManager.pause/cancel_session_runtime（10.6.19-23） |
| 9 | cancelTeamWorkForSession | uapclaw.go:423 | 函数骨架 + 异常处理 | TeamManager.terminate_session_runtime（10.6.19-23） |

### 2.3 删除标记（与 Python 不对齐）

| # | 回填点 | 位置 | 原因 |
|---|--------|------|------|
| 14 | G34: ensureAdapter 中启动 dreaming | uapclaw.go:595 | Python 不在 _ensure_adapter 中启动 dreaming，dreaming 只在 create_instance 后启动 |

### 2.4 保留 ⤵️（依赖外部未实现模块）

| # | 回填点 | 依赖章节 |
|---|--------|----------|
| 7（Team 部分） | Team 后续请求绕过 Session 队列 | 10.6.19-23（TeamManager） |
| 8（pause/cancel） | _processTeamInterrupt 中 TeamManager 调用 | 10.6.19-23（TeamManager） |
| 9（terminate） | _cancelTeamWorkForSession 中 TeamManager 调用 | 10.6.19-23（TeamManager） |

## 3. 详细设计

### 3.1 云端记忆钩子（#1-#4）

**核心逻辑**：所有 4 个钩子都受 `memory_mode == "cloud"` 条件守卫。`BuildInputs` 已返回 `memoryMode`（第 2 个返回值），但 `ProcessMessage` 和 `ProcessMessageStream` 当前忽略了它。

#### 3.1.1 ProcessMessage（#1 + #2）

**Before-chat 钩子**（对齐 Python 834-845）：

```go
// 当前代码缺少 memory_mode == "cloud" 条件，且未消费 MemoryBlocks
inputs, memoryMode, _ := uc.BuildInputs(request)

// 修改为：
if memoryMode == "cloud" {
    memCtx := &extensions.MemoryHookContext{
        SessionID:    sessionID,
        RequestID:    request.RequestID,
        ChannelID:    &request.ChannelID,
        AgentName:    "",
        WorkspaceDir: workspace.AgentWorkspaceDir(),
        Extra:        parseRequestParams(request),
    }
    extReg.Trigger(ctx, extensions.AgentServerMemoryBeforeChat, memCtx.ToMap())
    // 从 memCtx.MemoryBlocks 拼接记忆注入 inputs
    memoryBlock := strings.Join(memCtx.MemoryBlocks, "\n\n")
    if memoryBlock != "" {
        inputs["memory_block"] = memoryBlock
    }
}
```

**After-chat 钩子**（对齐 Python 866-877）：

```go
// 在 resp.OK 分支内，assistant 历史记录之后
if memoryMode == "cloud" && resp.OK {
    content := uc.extractResponseContent(resp)
    afterCtx := &extensions.MemoryHookContext{
        SessionID:        sessionID,
        RequestID:        request.RequestID,
        ChannelID:        &request.ChannelID,
        AgentName:        "",
        WorkspaceDir:     workspace.AgentWorkspaceDir(),
        AssistantMessage: content,
        Extra:            parseRequestParams(request),
    }
    extReg.Trigger(ctx, extensions.AgentServerMemoryAfterChat, afterCtx.ToMap())
}
```

#### 3.1.2 ProcessMessageStream（#3 + #4）

**Before-chat 钩子**：与 #1 逻辑完全一致，在 `inputs, memoryMode, _ := uc.BuildInputs(request)` 之后、创建中转 channel 之前触发。

**After-chat 钩子**：在 `streamComplete:` 标签处、`NewTerminalChunk` 之前触发：

```go
streamComplete:
    // 云端记忆对话后钩子
    if memoryMode == "cloud" {
        assistantMessage := finalAnswerContent
        if assistantMessage == "" {
            assistantMessage = strings.Join(finalAnswerChunks, "")
        }
        afterCtx := &extensions.MemoryHookContext{
            SessionID:        sessionID,
            RequestID:        request.RequestID,
            ChannelID:        &request.ChannelID,
            AgentName:        "",
            WorkspaceDir:     workspace.AgentWorkspaceDir(),
            AssistantMessage: assistantMessage,
            Extra:            parseRequestParams(request),
        }
        extReg.Trigger(ctx, extensions.AgentServerMemoryAfterChat, afterCtx.ToMap())
    }
    resultCh <- schema.NewTerminalChunk(request.RequestID, request.ChannelID)
```

### 3.2 Team 模式判断（#5）

**新增函数**（对齐 Python interface.py:909-918）：

```go
// isTeamMode 判断请求是否为 Team 模式。
// 对齐 Python: is_team_mode = team_flag or (mode in {"team", "team.plan", "code.team"})
func isTeamMode(request *schema.AgentRequest) bool {
    params := parseRequestParams(request)
    if teamFlag, ok := params["team"].(bool); ok && teamFlag {
        return true
    }
    if mode, ok := params["mode"].(string); ok {
        modeLower := strings.TrimSpace(strings.ToLower(mode))
        return modeLower == "team" || modeLower == "team.plan" || modeLower == "code.team"
    }
    return false
}

// isAutoHarnessResume 判断请求是否为 Auto-Harness resume。
// 对齐 Python: is_auto_harness_resume = mode == "auto_harness" and isinstance(activate_response, dict)
func isAutoHarnessResume(request *schema.AgentRequest) bool {
    params := parseRequestParams(request)
    if mode, ok := params["mode"].(string); ok {
        if strings.TrimSpace(strings.ToLower(mode)) == "auto_harness" {
            if _, ok2 := params["activate_response"].(map[string]any); ok2 {
                return true
            }
        }
    }
    return false
}
```

### 3.3 BuildInputs 三条路径完整对齐（#6 + #15 + #16）

**对齐 Python interface.py:391-415**：

```go
func (uc *UapClaw) BuildInputs(request *schema.AgentRequest) (map[string]any, string, string) {
    // ... 前置字段提取不变 ...

    var finalQuery any
    query, _ := params["query"].(string)

    // PATH A: InteractiveInput 类型守卫
    // 注意：从 JSON 反序列化后 params["query"] 只能是 string，此分支在 WebSocket 路径中不可达。
    // 但保留防御性守卫，与 Python isinstance(query, InteractiveInput) 对齐。
    // 若未来有内部代码直接构造 inputs["query"] 为 *InteractiveInput，此守卫会生效。
    // 当前始终走 PATH B/C。
    // PATH B: answers 分支 → 构建 InteractiveInput
    answers, _ := params["answers"].([]any)
    if len(answers) > 0 {
        requestID, _ := params["request_id"].(string)
        source, _ := params["source"].(string)
        interactiveInput := buildInteractiveInputFromAnswers(requestID, answers, source)
        if interactiveInput != nil {
            finalQuery = interactiveInput
        } else {
            finalQuery = BuildUserPrompt(query, files, channel, language, trustedDirs, metadata)
        }
    } else {
        // PATH C: 普通对话 → BuildUserPrompt 包装
        finalQuery = BuildUserPrompt(query, files, channel, language, trustedDirs, metadata)
    }

    // ... 后续 inputs 组装不变 ...
}
```

**新增函数** `buildInteractiveInputFromAnswers`（对齐 Python interface.py:454-517）：

```go
// buildInteractiveInputFromAnswers 从用户答案构建 InteractiveInput。
// 对齐 Python: JiuWenClaw._build_interactive_input_from_answers()
func buildInteractiveInputFromAnswers(requestID string, answers []any, source string) *interaction.InteractiveInput {
    ii, err := interaction.NewInteractiveInput()
    if err != nil {
        return nil
    }

    // AskUserRail 路径：source == "ask_user_interrupt"
    if source == "ask_user_interrupt" {
        answersDict := make(map[string]any)
        for _, a := range answers {
            if answer, ok := a.(map[string]any); ok {
                questionText, _ := answer["question"].(string)
                selectedOptions, _ := answer["selected_options"].([]any)
                var answerValue string
                if len(selectedOptions) > 0 {
                    answerValue, _ = selectedOptions[0].(string)
                }
                if questionText != "" && answerValue != "" {
                    answersDict[questionText] = answerValue
                }
            }
        }
        ii.Update(requestID, map[string]any{"answers": answersDict})
        return ii
    }

    // 未知 source（非 permission_interrupt）返回 nil
    if source != "" && source != "permission_interrupt" {
        return nil
    }

    // PermissionRail 路径：approve/reject/always_allow
    var answer map[string]any
    if len(answers) > 0 {
        answer, _ = answers[0].(map[string]any)
    }
    if answer == nil {
        answer = make(map[string]any)
    }

    var selectedOptions []any
    if so, ok := answer["selected_options"].([]any); ok {
        selectedOptions = so
    }
    var value string
    if len(selectedOptions) > 0 {
        value, _ = selectedOptions[0].(string)
    }
    customInput, _ := answer["custom_input"].(string)

    var confirmPayload map[string]any
    switch value {
    case "approve", "本次允许", "Approve":
        confirmPayload = map[string]any{"approved": true, "auto_confirm": false, "feedback": ""}
    case "always_allow", "总是允许", "Always Allow":
        confirmPayload = map[string]any{"approved": true, "auto_confirm": true, "persist_allow": true, "feedback": ""}
    case "reject", "拒绝", "Reject":
        feedback := customInput
        if feedback == "" {
            feedback = "用户拒绝"
        }
        confirmPayload = map[string]any{"approved": false, "auto_confirm": false, "feedback": feedback}
    default:
        confirmPayload = map[string]any{"approved": false, "auto_confirm": false, "feedback": "未知选项: " + value}
    }

    ii.Update(requestID, confirmPayload)
    return ii
}
```

**Team 模式 rawQuery 替换**（对齐 Python interface.py:940-949）：

在 `ProcessMessageStream` 中，`BuildInputs` 之后、创建中转 channel 之前：

```go
inputs, memoryMode, rawQuery := uc.BuildInputs(request)

// Team 模式：使用原始 query，不经过 BuildUserPrompt 包装
if isTeamMode(request) {
    if _, ok := inputs["query"].(*interaction.InteractiveInput); !ok {
        inputs["query"] = rawQuery
        logger.Info(logComponent).
            Str("event_type", "team_raw_query").
            Str("raw_query", truncate(rawQuery, 100)).
            Msg("Team 模式使用原始 query")
    }
}
```

### 3.4 Auto-Harness resume 绕过 Session 队列（#7 部分回填）

**对齐 Python interface.py:1007-1012**：

在 `ProcessMessageStream` 中，将当前的 `EnsureSessionProcessor` 替换为条件分流：

```go
// Auto-Harness resume 绕过 Session 队列
if isAutoHarnessResume(request) {
    logger.Info(logComponent).
        Str("request_id", request.RequestID).
        Str("session_id", sessionID).
        Msg("Auto-Harness resume 请求，绕过 Session 队列")
    go func() {
        // 直接执行流式任务
        ch, streamErr := a.ProcessMessageStreamImpl(ctx, request, inputs)
        // ... 生产者逻辑 ...
    }()
} else {
    // ⤵️ 10.3.2: Team 后续请求绕过 Session 队列（等待 TeamManager 10.6.19-23）
    _ = uc.sessionManager.EnsureSessionProcessor(ctx, sessionID)
}
```

注意：这部分需要重构 `ProcessMessageStream` 的生产者 goroutine 逻辑，使其可在两种模式下复用。

### 3.5 Team 中断分流（#8 + #9）

**_processTeamInterrupt 函数**（对齐 Python interface.py:702-763）：

```go
// processTeamInterrupt 处理 Team 模式中断请求。
// 对齐 Python: JiuWenClaw._process_team_interrupt()
func (uc *UapClaw) processTeamInterrupt(
    ctx context.Context,
    request *schema.AgentRequest,
    intent string,
    sessionID string,
) (*schema.AgentResponse, error) {
    switch intent {
    case "resume":
        // 对齐 Python: resume 直接返回提示信息
        return schema.NewAgentResponse(request.RequestID, request.ChannelID,
            schema.WithResponseOK(true),
            schema.WithResponsePayload(map[string]any{
                "event_type": "chat.interrupt_result",
                "intent":     intent,
                "success":    true,
                "message":    "团队暂停后，直接发送下一条消息即可继续。",
            }),
        ), nil

    case "pause":
        // ⤵️ 10.6.19-23: team_manager.pause_session_runtime(sessionID, reason)
        paused := false
        // paused = teamManager.PauseSessionRuntime(ctx, sessionID, reason)
        _ = uc.sessionManager.CancelSessionTask(ctx, sessionID, "interrupt(pause)", nil)
        message := "团队已暂停"
        if !paused {
            message = "当前没有可暂停的团队任务"
        }
        return schema.NewAgentResponse(request.RequestID, request.ChannelID,
            schema.WithResponseOK(true),
            schema.WithResponsePayload(map[string]any{
                "event_type": "chat.interrupt_result",
                "intent":     intent,
                "success":    paused,
                "message":    message,
            }),
        ), nil

    case "cancel":
        // ⤵️ 10.6.19-23: team_manager.cancel_session_runtime(sessionID, reason)
        cancelled := false
        // cancelled = teamManager.CancelSessionRuntime(ctx, sessionID, reason)
        _ = uc.sessionManager.CancelSessionTask(ctx, sessionID, "interrupt(cancel)", nil)
        message := "团队当前执行已结束"
        if !cancelled {
            message = "当前没有可取消的团队任务"
        }
        return schema.NewAgentResponse(request.RequestID, request.ChannelID,
            schema.WithResponseOK(true),
            schema.WithResponsePayload(map[string]any{
                "event_type": "chat.interrupt_result",
                "intent":     intent,
                "success":    cancelled,
                "message":    message,
            }),
        ), nil

    default:
        return schema.NewAgentResponse(request.RequestID, request.ChannelID,
            schema.WithResponseOK(true),
            schema.WithResponsePayload(map[string]any{
                "event_type": "chat.interrupt_result",
                "intent":     intent,
                "success":    false,
                "message":    "团队模式暂不支持中断意图: " + intent,
            }),
        ), nil
    }
}
```

**_cancelTeamWorkForSession 函数**（对齐 Python interface.py:765-782）：

```go
// cancelTeamWorkForSession 终止当前 session 的 Team runtime（若存在）。
// 对齐 Python: JiuWenClaw._cancel_team_work_for_session()
func (uc *UapClaw) cancelTeamWorkForSession(sessionID string, channelID string, logPrefix string) bool {
    // ⤵️ 10.6.19-23: get_team_manager + terminate_session_runtime
    // teamManager := getTeamManager(channelID)
    // return teamManager.TerminateSessionRuntime(ctx, sessionID, logPrefix)
    logger.Info(logComponent).
        Str("session_id", sessionID).
        Str("channel_id", channelID).
        Msg("cancelTeamWorkForSession 等待 TeamManager 回填（10.6.19-23）")
    return false
}
```

**ProcessInterrupt 修改**：

```go
func (uc *UapClaw) ProcessInterrupt(ctx context.Context, request *schema.AgentRequest) (*schema.AgentResponse, error) {
    intent := uc.extractIntent(request)
    sessionID := session.NormalizeSessionID(uc.extractSessionID(request))

    // Team 模式分流
    if isTeamMode(request) {
        return uc.processTeamInterrupt(ctx, request, intent, sessionID)
    }

    // ... 原有逻辑不变 ...

    // cancel（默认）
    resp, err := a.ProcessInterrupt(ctx, request)
    uc.cancelTeamWorkForSession(sessionID, request.ChannelID, "interrupt(intent="+intent+"): ")
    waitTimeout := 5 * time.Second
    _ = uc.sessionManager.CancelSessionTask(ctx, sessionID, "interrupt(cancel)", &waitTimeout)
    return resp, err
}
```

### 3.6 Dreaming 生命周期（#10-#12）

**CreateInstance**（对齐 Python interface.py:319-322）：

```go
func (uc *UapClaw) CreateInstance(ctx context.Context, opts ...CreateInstanceOption) error {
    cfg := defaultCreateInstanceConfig()
    for _, o := range opts {
        o(&cfg)
    }
    a, err := uc.ensureAdapter(cfg.mode)
    if err != nil {
        return err
    }
    if err := a.CreateInstance(ctx, cfg.config, cfg.mode, cfg.subMode); err != nil {
        return err
    }
    logger.Info(logComponent)...
    // 启动 dreaming 后台任务
    go func() {
        _ = a.TryStartDreaming(context.Background(), func() bool {
            return uc.sessionManager.HasActiveTasks()
        })
    }()
    return nil
}
```

**ReloadAgentConfig**（对齐 Python interface.py:335-343）：

```go
func (uc *UapClaw) ReloadAgentConfig(configBase map[string]any, envOverrides map[string]any) error {
    uc.adapterMu.Lock()
    a := uc.adapter
    uc.adapterMu.Unlock()
    if a == nil {
        return nil
    }
    // 停止 dreaming
    _ = a.TryStopDreaming(context.Background())
    if err := a.ReloadAgentConfig(context.Background(), configBase, envOverrides); err != nil {
        return err
    }
    // 重启 dreaming
    go func() {
        _ = a.TryStartDreaming(context.Background(), func() bool {
            return uc.sessionManager.HasActiveTasks()
        })
    }()
    return nil
}
```

### 3.7 CreateInstance Functional Options 重构（#13）

**对齐 Python `create_instance(config=None, *, mode="agent", sub_mode=None)` 的默认参数机制**：

```go
// createInstanceConfig CreateInstance 的内部配置。
type createInstanceConfig struct {
    config  map[string]any
    mode    string
    subMode string
}

// CreateInstanceOption CreateInstance 的可选参数。
type CreateInstanceOption func(*createInstanceConfig)

// defaultCreateInstanceConfig 返回默认配置。
func defaultCreateInstanceConfig() createInstanceConfig {
    return createInstanceConfig{mode: "agent"}
}

// WithCreateInstanceConfig 设置配置。
func WithCreateInstanceConfig(config map[string]any) CreateInstanceOption {
    return func(c *createInstanceConfig) { c.config = config }
}

// WithCreateInstanceMode 设置模式。
func WithCreateInstanceMode(mode string) CreateInstanceOption {
    return func(c *createInstanceConfig) { c.mode = mode }
}

// WithCreateInstanceSubMode 设置子模式。
func WithCreateInstanceSubMode(subMode string) CreateInstanceOption {
    return func(c *createInstanceConfig) { c.subMode = subMode }
}
```

**ensureAdapter 中注册 hook**：

```go
func (uc *UapClaw) ensureAdapter(mode string) (adapter.AgentAdapter, error) {
    ...
    // 注册 SkillNet 安装完成后的回调
    uc.skillManager.SetSkillnetInstallCompleteHook(func(ctx context.Context) error {
        return uc.CreateInstance(ctx)
    })
    uc.adapter = a
    ...
}
```

### 3.8 删除 G34 标记（#14）

删除 `uapclaw.go:595` 的 `// ⤵️ G34: 启动 dreaming 后台任务（adapter.TryStartDreaming）` 注释行。Python 的 `_ensure_adapter` 不启动 dreaming，dreaming 只在 `create_instance` 成功后启动。

## 4. 文件组织

保持现有目录结构不变（`runtime/` + `adapter/` 分包），通过注释标注对齐 Python：

| Go 文件 | Go 包 | Python 对齐 | 说明 |
|---------|------|-------------|------|
| `runtime/uapclaw.go` | `runtime` | `agent_adapter/interface.py` | 门面 + Team 中断 + cancelTeamWork |
| `runtime/build_inputs.go` | `runtime` | `agent_adapter/interface.py` | BuildInputs + InteractiveInput 构造 |
| `adapter/deep_adapter_team.go` | `adapter` | `agent_adapter/team_helpers.py` | Team 流式处理 |

## 5. 需要确认的依赖

| 依赖项 | 当前状态 | 需要确认 |
|--------|---------|---------|
| `sessionManager.HasActiveTasks()` | 需确认方法是否存在 | 如不存在需新增 |
| `extensions.MemoryHookContext.Extra` 字段 | 当前为 `any` | 需确认 `parseRequestParams` 返回值是否兼容 |
| `interaction.NewInteractiveInput()` | 已存在 | ✅ |
| `adapter.AgentAdapter.TryStartDreaming` | 已存在 | ✅ |
| `adapter.AgentAdapter.TryStopDreaming` | 已存在 | ✅ |
| `SkillManager.SetSkillnetInstallCompleteHook` | 已存在 | ✅ |

## 6. 不修改的部分

以下 ⤵️ 标记保留，等待依赖章节实现后回填：

- **Team 后续请求绕过 Session 队列**：需要 `TeamManager`（10.6.19-23）判断 `is_team_first_request`
- **_processTeamInterrupt 中 pause/cancel**：需要 `TeamManager.pause/cancel_session_runtime`（10.6.19-23）
- **_cancelTeamWorkForSession 中 terminate**：需要 `TeamManager.terminate_session_runtime`（10.6.19-23）
- **TryStartDreaming 内部实际调用**：需要 `swarm memory dreaming.startDreaming`（10.6.13-18）
- **TryStopDreaming 内部实际调用**：需要 `swarm memory dreaming.stopDreaming`（10.6.13-18）

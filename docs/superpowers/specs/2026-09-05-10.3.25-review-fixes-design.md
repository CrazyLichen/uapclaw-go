# 10.3.25 审查修复设计：bash.go 重复调用 + stub 删除 + event_type 对齐

> 修复 10.3.25 实现审查发现的 3 个问题

## 一、问题清单

| # | 问题 | 严重度 | 文件 |
|---|------|--------|------|
| 1 | bash.go 重复调用 DetectAndRecordDeletions | 🔴 | `shell/bash.go` |
| 2 | stream_utils.go 中 4 个 stub 函数 | 🟡 | `server/utils/stream_utils.go` |
| 3 | ParseStreamChunk event_type/payload 与 Python 不一致 | 🔴 | `server/utils/stream_utils.go` + `server/adapter/deep_adapter.go` |

## 二、问题 1：bash.go 重复调用 DetectAndRecordDeletions

### 2.1 现状

`bash.go` 中调用了两次 `filesystem.DetectAndRecordDeletions(historyPath)`：
- L262-264：`if historyPath != "" && !meaning.IsError`（与 Python 一致）
- L287-289：`if historyPath != ""`（Python 中不存在）

成功路径上函数被调用两次，浪费 I/O 且与 Python 不对齐。

### 2.2 修复

删除 L285-289 共 5 行：

```go
// 删除以下代码
// 执行后：检测并记录文件删除
// 对齐 Python: _detect_and_record_deletions(history_path)
if historyPath != "" {
    filesystem.DetectAndRecordDeletions(historyPath)
}
```

仅保留 L262-264 的条件调用，完全对齐 Python `_tool.py` L231。

### 2.3 影响范围

仅 `bash.go` 一个文件。`DetectAndRecordDeletions` 函数本身是幂等的（mutex 保护 + 原子写入），删除重复调用不会改变行为语义。

## 三、问题 2：删除 4 个 stub 函数

### 3.1 现状

`stream_utils.go` 中有 4 个标注"当前为 stub，后续将补充完整实现"的函数：

- `ParseDictChunk`
- `ParseTypedChunk`
- `ParseEventTypedChunk`
- `ParseResponseChunk`

这些函数当前无任何调用方（`ParseStreamChunk` 的 switch 已内联处理所有类型），属于死代码。

### 3.2 修复

删除 4 个 stub 函数及其测试用例（如有）。

### 3.3 影响范围

`stream_utils.go` + `stream_utils_test.go`。由于无调用方，删除不影响任何编译或行为。

## 四、问题 3：ParseStreamChunk 一比一对齐 Python

### 4.1 函数签名变更

增加 `hasStreamedContent ...bool` 可变参数，对齐 Python 的 `_has_streamed_content`：

```go
// Before
func ParseStreamChunk(output *stream.OutputSchema, usage *UsageAccumulator,
    emittedAskUserIDs map[string]bool, converter InteractionConverterFunc) map[string]any

// After
func ParseStreamChunk(output *stream.OutputSchema, usage *UsageAccumulator,
    emittedAskUserIDs map[string]bool, converter InteractionConverterFunc,
    hasStreamedContent ...bool) map[string]any
```

用 `...bool` 保持向后兼容，默认 false。调用方 `deep_adapter.go` 需同步传参。

### 4.2 各 chunkType 对齐明细

对齐 Python `_parse_typed_chunk`（stream_utils.py L107-353）。

#### 4.2.1 controller_output

**现状 Go：** 直接 switch payload["type"]，task_failed 取 `payload["error"]`。

**Python 逻辑（L150-170）：**
1. 先调 `_find_interaction_payloads(payload)` 找 `__interaction__` 载荷
2. 找到 → `_parse_interaction_payload(interactions)` 返回
3. 未找到 → 检查 `payload.type`：
   - `task_completion` → nil
   - `task_failed` → 从 `payload.data` 列表找第一个有 `.text` 属性的元素作为错误信息

**修复：**
```go
case "controller_output":
    // 1. 先搜索 __interaction__ 载荷
    interactions := FindInteractionPayloads(payload)
    if len(interactions) > 0 {
        return ParseInteractionPayload(interactions[0], converter)
    }
    // 2. 检查内部 type
    innerType, _ := payload["type"].(string)
    switch innerType {
    case "task_completion":
        return nil
    case "task_failed":
        // 从 data 列表找 .text 字段
        errorMsg := "任务执行失败"
        if data, ok := payload["data"].([]any); ok {
            for _, item := range data {
                if m, ok := item.(map[string]any); ok {
                    if text, ok := m["text"].(string); ok && text != "" {
                        errorMsg = text
                        break
                    }
                }
            }
        }
        return map[string]any{
            "event_type": "chat.error",
            "error":      errorMsg,
        }
    default:
        return map[string]any{
            "event_type": "chat.delta",
            "content":    payload,
        }
    }
```

#### 4.2.2 answer

**现状 Go：** 固定返回 `chat.final`。

**Python 逻辑（L202-231）：**
1. `result_type == "error"` → `chat.error`
2. payload dict 中取 `output`（也是 dict），再取 `output.output` 作为 content
3. `is_chunked = output.get("chunked", False)`
4. `_has_streamed_content && !is_chunked` → `chat.final`
5. `is_chunked` → `chat.delta`
6. 默认 → `chat.final`

**修复：**
```go
case "answer":
    // 对齐 Python L202-231
    if p, ok := payload.(map[string]any); ok {
        if p["result_type"] == "error" {
            return map[string]any{
                "event_type": "chat.error",
                "error":      ExtractStringFromPayload(p, "output"),
            }
        }
        var content string
        var isChunked bool
        if output, ok := p["output"].(map[string]any); ok {
            content, _ = output["output"].(string)
            isChunked, _ = output["chunked"].(bool)
        } else {
            content = fmt.Sprintf("%v", p["output"])
        }
        if content == "" || strings.TrimSpace(content) == "" {
            return nil
        }
        hsc := len(hasStreamedContent) > 0 && hasStreamedContent[0]
        if hsc && !isChunked {
            return map[string]any{"event_type": "chat.final", "content": content}
        }
        if isChunked {
            return map[string]any{"event_type": "chat.delta", "content": content}
        }
        return map[string]any{"event_type": "chat.final", "content": content}
    }
    return map[string]any{"event_type": "chat.final", "content": fmt.Sprintf("%v", payload)}
```

#### 4.2.3 thinking

**现状 Go：** `chat.thinking` + `payload["content"]`。

**Python 逻辑（L292-297）：** `chat.processing_status` + `is_processing: true` + `current_task: "thinking"`。

**修复：**
```go
case "thinking":
    return map[string]any{
        "event_type":    "chat.processing_status",
        "is_processing": true,
        "current_task":  "thinking",
    }
```

#### 4.2.4 context.usage

**现状 Go：** `chat.context_usage`，透传整个 payload。

**Python 逻辑（L307-314）：** `context.usage`，提取 `rate`/`context_max`/`tokens_used`。

**修复：**
```go
case "context.usage":
    return map[string]any{
        "event_type":  "context.usage",
        "rate":        ExtractFloatFromPayload(payload, "rate"),
        "context_max": ExtractIntFromPayload(payload, "context_max"),
        "tokens_used": ExtractIntFromPayload(payload, "tokens_used"),
    }
```

#### 4.2.5 todo.updated

**现状 Go：** `todo.updated`，透传整个 payload。

**Python 逻辑（L299-305）：** 从 payload 提取 `todos` 列表。

**修复：**
```go
case "todo.updated":
    todos, _ := payload["todos"].([]any)
    if todos == nil {
        todos = []any{}
    }
    return map[string]any{
        "event_type": "todo.updated",
        "todos":      todos,
    }
```

#### 4.2.6 tool_result

**现状 Go：** `chat.tool_result`，透传整个 payload。

**Python 逻辑（L252-282）：** 从 payload 提取 `tool_result` 子对象，再提取 `result`/`tool_name`/`tool_call_id`/`raw_output`/`success`/`status`/`is_error`/`summary`。

**修复：**
```go
case "tool_result":
    resultInfo := payload
    if sub, ok := payload["tool_result"].(map[string]any); ok {
        resultInfo = sub
    }
    result := map[string]any{
        "result": ExtractStringFromPayload(resultInfo, "result"),
    }
    // tool_name: 优先 tool_name，回退 name
    if tn, ok := resultInfo["tool_name"].(string); ok && tn != "" {
        result["tool_name"] = tn
    } else if n, ok := resultInfo["name"].(string); ok {
        result["tool_name"] = n
    }
    // tool_call_id: 优先 tool_call_id，回退 toolCallId
    if tcid, ok := resultInfo["tool_call_id"].(string); ok && tcid != "" {
        result["tool_call_id"] = tcid
    } else if tcid2, ok := resultInfo["toolCallId"].(string); ok {
        result["tool_call_id"] = tcid2
    }
    // raw_output: 优先 raw_output，回退 rawOutput
    if ro, ok := resultInfo["raw_output"]; ok {
        result["raw_output"] = ro
    } else if ro2, ok := resultInfo["rawOutput"]; ok {
        result["raw_output"] = ro2
    }
    // success/status/is_error/summary
    for _, key := range []string{"success", "status", "is_error", "summary"} {
        if v, exists := resultInfo[key]; exists {
            result[key] = v
        }
    }
    return map[string]any{
        "event_type": "chat.tool_result",
    }.merge(result)  // 展开 result 字段
```

注意：最终返回需要将 result 字段展开到顶层（Python 用 `**result_payload`），Go 中用循环赋值实现：

```go
ret := map[string]any{"event_type": "chat.tool_result"}
for k, v := range result {
    ret[k] = v
}
return ret
```

#### 4.2.7 tool_update

**现状 Go：** `chat.tool_update`，透传整个 payload。

**Python 逻辑（L241-250）：** 从 payload 提取 `tool_update` 子对象，展开字段。

**修复：**
```go
case "tool_update":
    var updatePayload map[string]any
    if sub, ok := payload["tool_update"].(map[string]any); ok {
        updatePayload = sub
    } else {
        updatePayload = map[string]any{"content": fmt.Sprintf("%v", payload["tool_update"])}
    }
    result := map[string]any{"event_type": "chat.tool_update"}
    for k, v := range updatePayload {
        result[k] = v
    }
    return result
```

#### 4.2.8 content_chunk / llm_output

**现状 Go：** 直接返回 `chat.delta` + `payload["content"]`。

**Python 逻辑（L172-200）：** 先提取 content，空内容返回 nil。

**修复：**
```go
case "content_chunk":
    content := ExtractStringFromPayload(payload, "content")
    if content == "" || strings.TrimSpace(content) == "" {
        return nil
    }
    return map[string]any{"event_type": "chat.delta", "content": content}
```

`llm_output` 同理，但当前 Go 的 `ParseStreamChunk` 注释说 llm_output 在 goroutine 中直接处理不经过此函数。如果保留分支，同样按 Python 对齐。

#### 4.2.9 default 分支

**现状 Go：** 固定 `chat.delta` + 整个 payload 作为 content。

**Python 逻辑（L325-353）：** 先检查 payload dict 中是否有 `event_type`（team.* 命名空间直传，否则 `chat.{chunkType}`），无 event_type 则 `chat.{chunkType}` + 展开字段。

**修复：**
```go
default:
    // 对齐 Python L325-353
    if innerEventType, ok := payload["event_type"].(string); ok {
        if strings.HasPrefix(innerEventType, "team.") {
            // team 命名空间事件直传
            result := map[string]any{}
            for k, v := range payload {
                result[k] = SerializeValue(v)
            }
            return result
        }
    }
    // payload 是 dict → 展开
    if len(payload) > 0 {
        result := map[string]any{
            "event_type": "chat." + chunkType,
        }
        for k, v := range payload {
            if k != "event_type" {
                result[k] = SerializeValue(v)
            }
        }
        return result
    }
    // payload 非空 → 作为 content
    return map[string]any{
        "event_type": "chat." + chunkType,
        "content":    fmt.Sprintf("%v", payload),
    }
```

### 4.3 新增辅助函数

#### 4.3.1 SerializeValue

对齐 Python `_serialize_value`（L472-483）：

```go
// SerializeValue 将非 JSON 原生值序列化为前端安全的 payload。
// 对齐 Python: _serialize_value(value) (stream_utils.py L472-483)
func SerializeValue(value any) any {
    switch v := value.(type) {
    case time.Time:
        return v.UTC().Format(time.RFC3339Nano)
    default:
        return v
    }
}
```

Go 中不需要处理 `datetime.date`/`Enum`（Go 没有 Python 的 date/Enum 与 JSON 原生类型的混淆问题）。

#### 4.3.2 FindInteractionPayloads

对齐 Python `_find_interaction_payloads`（L378-432）：

```go
// FindOption FindInteractionPayloads 可选参数。
type FindOption struct {
    MaxDepth int
}

// FindInteractionPayloads 递归搜索 __interaction__ 载荷。
// 对齐 Python: _find_interaction_payloads(obj, _depth=0, _seen=None) (stream_utils.py L378-432)
func FindInteractionPayloads(obj any, opts ...FindOption) []any {
    maxDepth := 8
    if len(opts) > 0 && opts[0].MaxDepth > 0 {
        maxDepth = opts[0].MaxDepth
    }
    _ = maxDepth // 递归深度在内部函数中通过 depth 参数控制
    return findInteractionPayloadsRecursive(obj, 0, nil)
}

func findInteractionPayloadsRecursive(obj any, depth int, seen map[uintptr]bool) []any {
    if obj == nil || depth > 8 {
        return nil
    }
    // 循环检测：对 map/slice/ptr 取地址作 seen 标记
    rv := reflect.ValueOf(obj)
    if rv.Kind() == reflect.Map || rv.Kind() == reflect.Slice || rv.Kind() == reflect.Ptr {
        ptr := rv.Pointer()
        if seen == nil {
            seen = make(map[uintptr]bool)
        }
        if seen[ptr] {
            return nil
        }
        seen[ptr] = true
    }

    // 检查 type == "__interaction__"
    switch v := obj.(type) {
    case map[string]any:
        if v["type"] == "__interaction__" {
            return []any{v["payload"]}
        }
        if v["event_type"] == "chat.ask_user_question" {
            return []any{map[string]any{
                "id":    v["request_id"],
                "value": map[string]any{"questions": v["questions"]},
            }}
        }
        var found []any
        for _, val := range v {
            found = append(found, findInteractionPayloadsRecursive(val, depth+1, seen)...)
        }
        return found
    case []any:
        var found []any
        for _, val := range v {
            found = append(found, findInteractionPayloadsRecursive(val, depth+1, seen)...)
        }
        return found
    default:
        // Go 没有 model_dump，仅对 map[string]any 和 []any 递归
        return nil
    }
}
```

#### 4.3.3 FindInteractionPayload

对齐 Python `_find_interaction_payload`（L435-443）：

```go
// FindInteractionPayload 返回第一个匹配的 __interaction__ 载荷。
// 对齐 Python: _find_interaction_payload(obj) (stream_utils.py L435-443)
func FindInteractionPayload(obj any, opts ...FindOption) any {
    matches := FindInteractionPayloads(obj, opts...)
    if len(matches) == 0 {
        return nil
    }
    return matches[0]
}
```

### 4.4 调用方同步修改

`deep_adapter.go` 中 `ParseStreamChunk` 调用需传 `hasStreamedContent` 参数。当前 goroutine 中该值在流式场景下应为 `true`（已有内容流式输出后），需从流式状态中获取。

### 4.5 测试变更

`stream_utils_test.go` 需更新：

| 测试 | 变更 |
|------|------|
| `TestParseStreamChunk_thinking` | event_type 改为 `chat.processing_status`，验证 `is_processing`/`current_task` |
| `TestParseStreamChunk_contextUsage` | event_type 改为 `context.usage`，验证 `rate`/`context_max`/`tokens_used` |
| `TestParseStreamChunk_answer` | 新增 `result_type=error`/`is_chunked`/`hasStreamedContent=true` 场景 |
| `TestParseStreamChunk_toolResult` | 验证字段提取（result/tool_name/tool_call_id/raw_output 等） |
| `TestParseStreamChunk_toolUpdate` | 验证 tool_update 子对象提取 |
| `TestParseStreamChunk_todoUpdated` | 验证 todos 列表提取 |
| `TestParseStreamChunk_controllerOutput` | 验证 __interaction__ 搜索 + task_failed 从 data 取 text |
| `TestParseStreamChunk_defaultFallback` | event_type 改为 `chat.{chunkType}` |
| `TestSerializeValue_time` | 已有，保持 |
| `TestFindInteractionPayloads_嵌套` | 已有，保持 |
| `TestFindInteractionPayloads_循环检测` | 已有，保持 |
| 删除 stub 函数测试 | 如有 |

## 五、文件变更清单

| # | 文件 | 操作 | 说明 |
|---|------|------|------|
| 1 | `internal/agentcore/harness/tools/shell/bash.go` | 修改 | 删除 L285-289 重复调用 |
| 2 | `internal/swarm/server/utils/stream_utils.go` | 修改 | 删除 4 个 stub + 对齐所有 chunkType + 新增 3 个 helper + 签名变更 |
| 3 | `internal/swarm/server/utils/stream_utils_test.go` | 修改 | 更新/新增测试用例 + 删除 stub 测试 |
| 4 | `internal/swarm/server/adapter/deep_adapter.go` | 修改 | 传 hasStreamedContent 参数 |

## 六、风险评估

| 风险 | 缓解措施 |
|------|---------|
| event_type 改名可能影响前端 | 前端目前可能已适配 Go 格式；需确认前端是否依赖这些 event_type |
| FindInteractionPayloads 的 reflect 开销 | 仅在 controller_output 分支触发，非热路径 |
| `hasStreamedContent ...bool` 可变参数不够显式 | 与 IsTeamParams 的 params 风格一致；如有强偏好可改为显式参数 |

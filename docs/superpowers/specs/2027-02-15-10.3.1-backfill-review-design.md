# 10.3.1 回填标记审查与修正设计

## 背景

10.3.1（AgentWebSocketServer）章节已标记完成，但 `internal/swarm/server/` 下存在大量 ⤵️ 回填标记。
经逐个验证代码实际实现状态，发现三类问题需要处理。

## 第一类：清理过时 ⤵️ 注释（10处）

功能已实现但注释仍标记 ⤵️/⤴️，移除标记保留功能说明。

| # | 文件 | 行号 | 修改前 | 修改后 |
|---|------|------|--------|--------|
| 1 | `adapter/deep_adapter.go` | 92 | `// ⤵️ 10.3.5 CreateInstance: create_instance 传入的 config 字典` | `// instanceOverrides CreateInstance 传入的 config 字典` |
| 2 | `adapter/deep_adapter.go` | 117 | `// ⤵️ 10.6.3-10: ContextAssembleRail` | `// contextAssembleRail 上下文组装轨道` |
| 3 | `adapter/deep_adapter.go` | 123 | `// ⤵️ 10.6.3-10: ContextProcessorRail` | `// contextProcessorRail 上下文处理轨道` |
| 4 | `adapter/deep_adapter.go` | 144 | `// ⤴️ 9.15 回填：HeartbeatRail` | `// heartbeatRail 心跳轨道` |
| 5 | `adapter/deep_adapter.go` | 174 | `// ⤵️ 10.6.24 多模态工具` | `// visionToolsRegistered 视觉工具注册状态` |
| 6 | `adapter/deep_adapter.go` | 179 | `// ⤵️ 10.6.24 多模态工具` | `// audioToolsRegistered 音频工具注册状态` |
| 7 | `adapter/deep_adapter.go` | 184 | `// ⤵️ 10.6.24 多模态工具` | `// videoToolRegistered 视频工具注册状态` |
| 8 | `adapter/deep_adapter_tools.go` | 23 | `// ⤵️ 9.38-49 Harness 工具集: 具体工具名称待回填` | `// 工具名称常量` |
| 9 | `adapter/code_adapter.go` | 49 | `// ⤵️ 10.3.7-11 CodeAgentRail` | `// codeAgentRail Code 模式专有护栏` |
| 10 | `runtime/agent_config.go` | 406/415 | `⤴️ 10.3.7-11: 对齐 Python...` | 移除 `⤴️ 10.3.7-11:` 前缀，保留功能说明 |

## 第二类：修正 ⤵️ 指向（~22处）

`deep_adapter_tools.go` 中所有 `⤵️ 9.38-49` 修正为 `⤵️ 10.6.24`。
原因：这些工具（wiki/paid_search/free_search/image_gen/xiaoyi/SkillToolkit/acp_chat）属于 Swarm 内置工具集（10.6.24），
不在 agentcore 基础工具集（9.38-49）范围内。9.38-49 已完成的是 Shell/文件系统/CodeTool/MCP 等基础工具。

涉及行号：87, 91, 92, 93, 297, 304, 310, 316, 403, 405, 442, 457, 472, 541, 544, 556, 582, 590, 600。

## 第三类：PermissionContext 回填（3处）

### 新增代码

在 `internal/swarm/schema/permission.go` 中追加 context key 和工具函数：

```go
// 权限上下文 context key
type toolPermChannelIDKey struct{}
type toolPermContextKey struct{}

// WithToolPermissionChannelID 将 channelID 注入 context
func WithToolPermissionChannelID(ctx context.Context, channelID string) context.Context {
    return context.WithValue(ctx, toolPermChannelIDKey{}, channelID)
}

// ToolPermissionChannelIDFromCtx 从 context 中获取 channelID
func ToolPermissionChannelIDFromCtx(ctx context.Context) string {
    if v, ok := ctx.Value(toolPermChannelIDKey{}).(string); ok {
        return v
    }
    return ""
}

// WithPermissionContextValue 将 PermissionContext 注入 context
func WithPermissionContextValue(ctx context.Context, pc *PermissionContext) context.Context {
    return context.WithValue(ctx, toolPermContextKey{}, pc)
}

// PermissionContextFromCtx 从 context 中获取 PermissionContext
func PermissionContextFromCtx(ctx context.Context) *PermissionContext {
    if v, ok := ctx.Value(toolPermContextKey{}).(*PermissionContext); ok {
        return v
    }
    return nil
}
```

### 修改 deep_adapter.go

**ProcessMessageImpl（~line 699）**：
```go
// 修改前：
// ⤵️ 10.1.8: token_cid = TOOL_PERMISSION_CHANNEL_ID.set(...)
// ⤵️ 10.1.8: token_perm = setup_permission_context(request)

// 修改后：
ctx = schema.WithToolPermissionChannelID(ctx, request.ChannelID)
if request.PermissionContext != nil {
    ctx = schema.WithPermissionContextValue(ctx, request.PermissionContext)
}
```

**ProcessMessageImpl deferred cleanup（~line 739）**：
```go
// 修改前：
// ⤵️ 10.1.8: TOOL_PERMISSION_CHANNEL_ID.reset(token_cid)
// ⤵️ 10.1.8: cleanup_permission_context(token_perm)

// 修改后：移除注释（Go context 不可变值模式不需要 reset/cleanup）
```

**ProcessMessageStreamImpl（~line 842）**：同 ProcessMessageImpl 的修改。

### 同步清理

`handle_envelope.go:808` 的 `⤵️ 10.3.12` 注释已过时（`GetClientCapabilities("acp")` 已调用），移除该标记。

## 设计决策

- **不可变值模式**：Go 的 `context.Context` 天然作用域隔离，不需要 Python `contextvars` 的 `set()`/`reset()` 模式
- **新增代码位置**：context key 和工具函数放在 `schema/permission.go`，与 `PermissionContext` 类型定义在一起
- **不处理项**：其余 ~68 处 ⤵️ 标记指向的功能确实未实现（ACP/A2X/Team/Rails/Memory 等），保持不动

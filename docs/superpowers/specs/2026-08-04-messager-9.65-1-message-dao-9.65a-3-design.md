# 9.65-1 Messager 接口 + InProcess 实现 & 9.65a-3 MessageDao + MessageManager 设计

## 概述

本文档描述两个紧密关联的实现章节：

1. **9.65-1** — Messager 接口定义 + InProcessMessager 实现，作为 9.65a-3 的前置依赖
2. **9.65a-3** — MessageDao（7 方法）+ TeamMessageManager（7 方法薄门面），包含 read_status watermark 机制

两个章节合并设计，因为 9.65-1 的核心产出（Messager 接口）直接被 9.65a-3 的 TeamMessageManager 消费。

---

## 9.65-1 Messager 接口 + InProcessMessager

### 流程位置与作用

9.65-1 位于 TeamBackend 子系统的事件通信层，是 9.65（Team Messager）的第一个子节：

```
9.65a-1 (TeamDB 基础层) ✅
9.65a-2 (TaskDao + TaskManager) ✅
9.65-1 (Messager 接口 + InProcess) ← 本节（前置）
9.65a-3 (MessageDao + MessageManager) ← 依赖 9.65-1
9.65a-4 (TeamBackend 门面) ← 依赖 9.65a-3
9.65-2 (PyZmqMessager) ← 后续
```

作用：解耦工具层与消息传输实现，使 TaskManager 和 MessageManager 能通过接口发布团队事件，而不依赖具体传输后端。

### 文件组织（对齐 Python）

| Python 文件 | Go 文件 | 说明 |
|---|---|---|
| `messager/messager.py` | `messager/messager.go` | **新增**：Messager 接口 + MessagerHandler 类型 |
| `messager/inprocess.py` | `messager/inprocess.go` | **新增**：InProcessMessager + 全局 Bus |
| `messager/base.py` | `messager/base.go` | **回填**：CreateMessager 工厂函数 |
| `messager/__init__.py` | 无需（Go 包级导出） | — |

### Messager 接口定义

对齐 Python `Messager` ABC（`openjiuwen/agent_teams/messager/messager.py`），8 个方法：

```go
// MessagerHandler 消息处理回调函数类型。
// 对齐 Python: MessagerHandler = Callable[[EventMessage], Awaitable[None]]
type MessagerHandler func(ctx context.Context, msg schema.EventMessage) error

// Messager 团队事件消息通信接口。
// 对齐 Python: Messager (openjiuwen/agent_teams/messager/messager.py)
type Messager interface {
    // Start 启动消息传输层
    Start(ctx context.Context) error
    // Stop 停止消息传输层
    Stop(ctx context.Context) error
    // Publish 向主题发布事件消息
    Publish(ctx context.Context, topicID string, message schema.EventMessage) error
    // Subscribe 订阅主题，注册回调
    Subscribe(ctx context.Context, topicID string, handler MessagerHandler) error
    // Unsubscribe 取消订阅
    Unsubscribe(ctx context.Context, topicID string) error
    // Send 点对点发送消息给指定 agent
    Send(ctx context.Context, agentID string, message schema.EventMessage) error
    // RegisterDirectMessageHandler 注册点对点消息回调
    RegisterDirectMessageHandler(ctx context.Context, handler MessagerHandler) error
    // UnregisterDirectMessageHandler 取消注册点对点消息回调
    UnregisterDirectMessageHandler(ctx context.Context) error
}
```

### InProcessMessager 实现

对齐 Python `InProcessMessager`（`openjiuwen/agent_teams/messager/inprocess.py`）：

- 进程全局 `bus` 单例（`sync.Mutex` 保护，`sync.Once` 惰性初始化）
- `bus` 两个数据结构：
  - `topicSubs map[string]map[string]MessagerHandler` — pub-sub（topic → {agentID → handler}）
  - `p2p map[string]MessagerHandler` — point-to-point（agentID → handler）
- `Publish()` 自动 stamp `SenderID` 过滤自发布（对齐 Python `message.model_copy(update={"sender_id": self._agent_id})`）
- `CleanupInProcessBus()` 测试间重置全局 Bus
- `agentID` 从 `config.NodeID` 获取，空字符串为默认值

### CreateMessager 回填

```go
func CreateMessager(config MessagerTransportConfig) (Messager, error) {
    switch config.Backend {
    case "inprocess":
        return NewInProcessMessager(config), nil
    // ⤵️ 9.65-2: pyzmq 后端
    default:
        return nil, fmt.Errorf("unsupported messager backend: %s", config.Backend)
    }
}
```

### schema/events.go 补充

当前 `NewEventMessage` 需手动指定 eventType 和 payload。需要添加自动映射机制。

**方案：TypedEvent 接口 + 各事件实现方法**

```go
// TypedEvent 带类型标识的团队事件接口。
// 对齐 Python: BaseEventMessage + EventMessage.from_event() 的自动映射
type TypedEvent interface {
    // EventTypeName 返回 TeamEvent 常量
    EventTypeName() string
    // ToPayload 转换为 map[string]any 载荷
    ToPayload() map[string]any
}
```

每个具体事件结构体实现 `EventTypeName()` 和 `ToPayload()`：

```go
func (e TaskCreatedEvent) EventTypeName() string { return TeamEventTaskCreated }
func (e TaskCreatedEvent) ToPayload() map[string]any {
    return map[string]any{
        "team_name": e.TeamName, "task_id": e.TaskID, "status": e.Status,
    }
}
```

新增工厂函数：

```go
// EventMessageFromEvent 从具体事件创建 EventMessage。
// 对齐 Python: EventMessage.from_event(event)
func EventMessageFromEvent(e TypedEvent) EventMessage {
    return EventMessage{
        EventType: e.EventTypeName(),
        Payload:   e.ToPayload(),
    }
}
```

### 回填 messager any 占位

| 文件 | 当前 | 变更 |
|---|---|---|
| `tools/task_manager.go:74` | `messager any` | `messager Messager` |
| `tools/task_manager.go:93` | `NewTeamTaskManager(..., messager any, ...)` | `messager Messager` |
| `tools/task_manager.go` 所有 `⤵️ 9.65 回填` | 占位注释 | 替换为真实 `tm.messager.Publish(ctx, topicID, EventMessageFromEvent(event))` 调用 |
| `agent/infra.go:15` | `Messager any` | `Messager messager.Messager` |
| `agent/agent_configurator.go:245` | `SetupTeamBackend(..., messager any, ...)` | `messager messager.Messager` |
| `agent/agent_configurator.go:389` | `Messager() any` | `Messager() messager.Messager` |
| `agent/agent_configurator.go:392` | `SetMessager(v any)` | `SetMessager(v messager.Messager)` |

### TaskManager 事件发布回填

对齐 Python `TeamTaskManager` 中所有 `messager.publish` 调用点，统一模式：

```go
// 辅助方法
func (tm *TeamTaskManager) publishTaskEvent(ctx context.Context, event TypedEvent) {
    if tm.messager == nil {
        return
    }
    topicID := schema.TeamTopicTask.Build(agentteams.GetSessionID(ctx), tm.teamName)
    msg := schema.EventMessageFromEvent(event)
    if err := tm.messager.Publish(ctx, topicID, msg); err != nil {
        logger.Error(logger.ComponentAgentCore).Err(err).
            Str("event_type", event.EventTypeName()).
            Msg("发布任务事件失败")
    }
}
```

回填点（9 处）：

| 方法 | 事件类型 |
|---|---|
| `Add` | `TaskCreatedEvent` |
| `Claim` | `TaskClaimedEvent` |
| `Assign` | `TaskClaimedEvent` |
| `Complete` | `TaskCompletedEvent` |
| `Cancel` | `TaskCancelledEvent` |
| `CancelAllTasks` | `TaskCancelledEvent`（每个） |
| `UpdateTask` | `TaskUpdatedEvent` |
| `SubmitPlan` | `TaskPlanRequestEvent` |
| `ApprovePlan` | `TaskPlanResponseEvent` |

注：Python TaskManager 还有 `_publishUnblockedEvents`（`TaskUnblockedEvent`）和 `_maybePublishTaskListDrained`（`TaskListDrainedEvent`）两个辅助方法，需在 Complete/Cancel 等终止传播后检查是否触发。Go 端对齐时需在 `terminateTaskInSession` 后追加相应逻辑。

---

## 9.65a-3 MessageDao + MessageManager

### 流程位置与作用

9.65a-3 位于 TeamBackend 子系统的消息通信层，是 Agent 间通信的核心基础设施：

- **直发消息**：成员 A → 成员 B，`is_read` 字段管理已读状态
- **广播消息**：成员 A → 全团队，per-member `read_status` watermark 管理已读状态
- **任务完成前置条件**：`HasUnreadMessages()` 是 `check_task_list_drained()` 的一部分
- **9.65a-4 的前置依赖**：TeamBackend 门面需要通过 MessageManager 发送 shutdown/cancel 消息

### 文件组织（对齐 Python）

| Python 文件 | Go 文件 | 说明 |
|---|---|---|
| `tools/database/message_dao.py` | `database/message_dao.go` | **回填**：移除占位，说明实现位于 memory_impl.go |
| `tools/database/__init__.py` | `database/database.go` | **回填**：MessageDao 空接口→7 方法接口 |
| `tools/models.py` | `database/models.go` | **新增**：TeamMessageBase + MessageReadStatusBase |
| `tools/memory_database.py` | `database/memory_impl.go` | **回填**：新增字段 + 7 方法 + 回填已有方法 |
| `tools/message_manager.py` | `tools/message_manager.go` | **回填**：接口→struct + 7 方法实现 |

### 数据模型

#### TeamMessageBase

对齐 Python `TeamMessageBase`（`openjiuwen/agent_teams/tools/models.py`）：

```go
// TeamMessageBase 消息行模型。
// 对齐 Python: TeamMessageBase (openjiuwen/agent_teams/tools/models.py)
// 动态表 team_message_<session_suffix> 的行模型。
type TeamMessageBase struct {
    // MessageID 消息唯一标识（主键）
    MessageID string `json:"message_id"`
    // TeamName 团队名称
    TeamName string `json:"team_name"`
    // FromMemberName 发送者
    FromMemberName string `json:"from_member_name"`
    // ToMemberName 接收者（广播消息为空）
    ToMemberName string `json:"to_member_name,omitempty"`
    // Content 消息内容
    Content string `json:"content"`
    // Timestamp 毫秒时间戳
    Timestamp int64 `json:"timestamp"`
    // Broadcast 是否广播消息
    Broadcast bool `json:"broadcast"`
    // IsRead 直发消息的已读标记。广播消息为 nil（广播已读状态由 MessageReadStatusBase 管理）。
    // 对齐 Python: is_read = None if broadcast else is_read
    // nil = 广播消息（不参与直发已读判定）
    // *false = 直发未读, *true = 直发已读
    IsRead *bool `json:"is_read"`
}
```

**is_read `*bool` 语义**：
- `nil` = 广播消息（不参与直发已读判定）
- `boolPtr(false)` = 直发未读
- `boolPtr(true)` = 直发已读
- `CreateMessage` 时：广播消息 `is_read=nil`，直发消息 `is_read=boolPtr(false)` 或 `boolPtr(true)`（HITT 场景）
- `GetMessages` 查询未读时：`is_read != nil && *is_read == false`（nil 自动跳过）
- `MarkMessageRead` 时：直发设 `is_read=boolPtr(true)`，广播更新 watermark

#### MessageReadStatusBase

对齐 Python `MessageReadStatusBase`（`openjiuwen/agent_teams/tools/models.py`）：

```go
// MessageReadStatusBase 广播已读水位模型。
// 对齐 Python: MessageReadStatusBase (openjiuwen/agent_teams/tools/models.py)
// 动态表 message_read_status_<session_suffix> 的行模型。
// 每个 member 每个团队一条记录，存储已读的最新广播消息的 timestamp。
type MessageReadStatusBase struct {
    // MemberName 成员名（主键）
    MemberName string `json:"member_name"`
    // TeamName 团队名（主键）
    TeamName string `json:"team_name"`
    // ReadAt 已读水位时间戳（该成员已读的最新广播消息的 timestamp）
    ReadAt *int64 `json:"read_at"`
}
```

### MessageDao 接口（7 方法）

替换 `database.go` 中的空接口 `MessageDao interface{}`：

```go
// MessageDao 消息 DAO 接口。
// 对齐 Python: MessageDao (openjiuwen/agent_teams/tools/database/message_dao.py)
type MessageDao interface {
    // GetMessage 按 ID 查消息。返回 nil 表示不存在。
    GetMessage(ctx context.Context, messageID string) (*TeamMessageBase, error)
    // CreateMessage 创建消息。返回 true 表示成功，false 表示 messageID 冲突。
    // 广播消息时 IsRead 必须为 nil；直发消息时 IsRead 为 boolPtr(false/true)。
    CreateMessage(ctx context.Context, msg *TeamMessageBase) bool
    // GetMessages 获取直发消息（非广播）。
    // fromMemberName 为空字符串表示不过滤。unreadOnly=true 时仅返回未读。
    GetMessages(ctx context.Context, teamName, toMemberName string, unreadOnly bool, fromMemberName string) ([]*TeamMessageBase, error)
    // GetBroadcastMessages 获取广播消息（排除自己发送的）。
    // unreadOnly=true 时通过 read_status watermark 过滤。
    GetBroadcastMessages(ctx context.Context, teamName, memberName string, unreadOnly bool, fromMemberName string) ([]*TeamMessageBase, error)
    // GetTeamMessages 获取团队所有消息。broadcast 为空字符串表示不过滤。
    GetTeamMessages(ctx context.Context, teamName string, broadcast string) ([]*TeamMessageBase, error)
    // HasUnreadMessages 是否有未读消息。
    // 直发：检查 is_read=false；广播：检查 per-member watermark。
    HasUnreadMessages(ctx context.Context, teamName string, includeBroadcast bool) bool
    // MarkMessageRead 标记已读。直发设 is_read=true；广播更新 read_status watermark。
    MarkMessageRead(ctx context.Context, messageID, memberName string) bool
}
```

### InMemoryTeamDatabase 实现

新增字段：

```go
type InMemoryTeamDatabase struct {
    // ... 已有字段
    messages   map[string]*TeamMessageBase         // key=messageID
    readStatus map[string]*MessageReadStatusBase   // key=memberName+"\x00"+teamName
}
```

回填已有方法：

| 方法 | 变更 |
|---|---|
| `NewInMemoryTeamDatabase` | 初始化 `messages`/`readStatus` map |
| `CleanupAllRuntimeState` | 追加清空 `messages`/`readStatus` |
| `ForceDeleteTeamSession` | 追加删除该 team 下所有消息和 readStatus |
| `DropCurSessionTables` | 追加清空 `messages`/`readStatus` |
| `Close` | 追加 nil 化 |

7 个方法实现（对齐 Python `memory_database.py` 的 Message Operations 区）：

| 方法 | 关键逻辑 |
|---|---|
| `GetMessage` | 按 messageID 从 map 查找 |
| `CreateMessage` | messageID 冲突返回 false；广播时 `is_read=nil`，直发保留传入值；timestamp 用 `GetCurrentTime()` |
| `GetMessages` | 遍历 messages，按 teamName + toMemberName + !broadcast 过滤，可选 unreadOnly（`is_read != nil && *is_read == false`）/ fromMemberName |
| `GetBroadcastMessages` | 遍历 messages，按 teamName + broadcast + fromMemberName != memberName 过滤；unreadOnly 时用 readStatus watermark 比较（`msg.Timestamp > *rs.ReadAt`） |
| `GetTeamMessages` | 按 teamName 过滤，可选 broadcast |
| `HasUnreadMessages` | 直发：检查 `is_read != nil && *is_read == false`；广播：遍历成员列表，对每个成员检查 watermark 是否覆盖所有广播消息 |
| `MarkMessageRead` | 直发：设 `is_read=boolPtr(true)`；广播：更新 readStatus watermark（`read_at = max(read_at, msg.Timestamp)`） |

新增编译校验：

```go
var _ MessageDao = (*InMemoryTeamDatabase)(nil) // InMemoryTeamDatabase 必须满足 MessageDao 接口
```

### TeamMessageManager 实现

从接口改为具体 struct，7 个方法（对齐 Python `TeamMessageManager`）：

```go
// TeamMessageManager 团队消息管理器。
// 对齐 Python: TeamMessageManager (openjiuwen/agent_teams/tools/message_manager.py)
// 薄门面，委托 db.Message() 执行 DAO 操作，通过 messager 发布事件。
type TeamMessageManager struct {
    // db 团队数据库实例（内含 MessageDao）
    db database.TeamDatabase
    // teamName 团队标识
    teamName string
    // memberName 当前成员标识
    memberName string
    // messager 事件发布器
    messager messager.Messager
}
```

7 个方法：

| 方法 | 逻辑 |
|---|---|
| `SendMessage` | uuid 生成 messageID → `db.Message().CreateMessage(直发)` → `messager.Publish(MessageEvent)` |
| `BroadcastMessage` | uuid 生成 messageID → `db.Message().CreateMessage(广播)` → `messager.Publish(BroadcastEvent)` |
| `GetMessages` | 委托 `db.Message().GetMessages()` |
| `GetBroadcastMessages` | 委托 `db.Message().GetBroadcastMessages()` |
| `GetTeamMessages` | 委托 `db.Message().GetTeamMessages()` |
| `HasUnreadMessages` | 委托 `db.Message().HasUnreadMessages()` |
| `MarkMessageRead` | 委托 `db.Message().MarkMessageRead()` + 日志 |

### 回填标记清单

| 标记位置 | 回填内容 |
|---|---|
| `database.go:134` `MessageDao interface{}` | 替换为 7 方法完整接口 |
| `database.go:33` `⤵️ 9.65a-3` 注释 | 移除标记 |
| `message_dao.go` 全文件占位 | 移除占位，说明实现位于 memory_impl.go |
| `memory_impl.go:17` `⤵️ 9.65a-3` 注释 | 移除标记 |
| `memory_impl.go:156` `⤵️ 9.65a-3` 注释 | 移除标记 |
| `memory_impl.go` 结构体 | 新增 `messages`/`readStatus` 字段 |
| `memory_impl.go` 已有方法 | 追加清空/删除逻辑 |
| `models.go` | 新增 TeamMessageBase + MessageReadStatusBase |
| `message_manager.go` | 接口→struct，7 方法实现 |
| `task_manager.go` 所有 `⤵️ 9.65 回填` | 替换为真实 publish 调用 |
| `agent/infra.go` | `Messager any` → `Messager messager.Messager` |
| `agent/agent_configurator.go` | `messager any` → `messager messager.Messager` |
| `messager/base.go` | 回填 CreateMessager |
| `schema/events.go` | 添加 TypedEvent 接口 + EventMessageFromEvent + 各事件方法 |
| `database/doc.go` | 更新文件目录 |
| `messager/doc.go` | 更新文件目录 |
| `tools/doc.go` | 更新文件目录 |

---

## 实现顺序

```
步骤 1: 9.65-1 Messager 接口 + InProcessMessager
  1a. schema/events.go — 添加 TypedEvent 接口 + EventMessageFromEvent + 各事件 EventTypeName/ToPayload
  1b. messager/messager.go — Messager 接口定义 + MessagerHandler 类型
  1c. messager/inprocess.go — InProcessMessager 实现 + 全局 Bus
  1d. messager/base.go — 回填 CreateMessager
  1e. task_manager.go — messager any → Messager，回填所有 publish 调用
  1f. agent/infra.go + agent_configurator.go — messager any → Messager
  1g. messager/base_test.go + inprocess_test.go — 测试
  1h. schema/events_test.go — TypedEvent 测试

步骤 2: 9.65a-3 MessageDao + MessageManager
  2a. database/models.go — 新增 TeamMessageBase + MessageReadStatusBase
  2b. database/database.go — MessageDao 接口回填 7 方法
  2c. database/memory_impl.go — InMemoryTeamDatabase 实现 MessageDao + 新字段 + 回填已有方法
  2d. database/message_dao.go — 移除占位
  2e. tools/message_manager.go — 接口→struct，7 方法实现
  2f. database/memory_impl_test.go — MessageDao 测试
  2g. tools/message_manager_test.go — TeamMessageManager 测试

步骤 3: 更新文档
  3a. IMPLEMENTATION_PLAN.md — 9.65-1 ✅, 9.65a-3 ✅
  3b. doc.go 文件更新
```

---

## 对应 Python 代码

| Go 章节 | Python 文件 |
|---|---|
| 9.65-1 | `openjiuwen/agent_teams/messager/messager.py` · `messager/inprocess.py` · `messager/base.py` |
| 9.65a-3 | `openjiuwen/agent_teams/tools/database/message_dao.py` · `tools/message_manager.py` · `tools/models.py` · `tools/memory_database.py` |

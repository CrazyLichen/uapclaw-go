# 9.11-9.12 Rails Python 对齐审核修复 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 修复 Go Rails（ProgressiveToolRail/TaskCompletionRail/TaskPlanningRail/AgentModeRail）及 DeepAgent/CreateSubagent 与 Python 实现的差异，确保方法签名、步骤流程、提示词文本一比一一致。

**Architecture:** 逐项修复 12 项已确认差异，每项独立提交。核心改动集中在 rails/ 目录 4 个文件 + deep_agent.go + factory.go + interfaces/deep_agent.go + prompts/sections/ 2 个文件。

**Tech Stack:** Go 1.22+, 项目现有测试框架

---

## File Structure

| File | Responsibility | Action |
|------|---------------|--------|
| `harness/deep_agent.go` | CreateSubagent 工厂分派 | Modify L631-636 |
| `harness/interfaces/deep_agent.go` | DeepAgentInterface 接口声明 | Modify L20-60 |
| `harness/tools/agent_mode/task_tool_factory.go` | task_tool 工厂 | Modify L16 |
| `harness/tools/subagent/task_tool.go` | task_tool 子 Agent 调用 | Modify L90 |
| `harness/rails/agent_mode.go` | AgentModeRail 全部逻辑 | Modify 多处 |
| `harness/rails/progressive.go` | ProgressiveToolRail initVisibleTools | Modify L799-807 |
| `harness/rails/task_planning.go` | TaskPlanningRail Init/BeforeModelCall/Uninit | Modify 多处 |
| `harness/tools/tool_discovery/search_tools.go` | SearchToolsTool trace 事件 | Modify L88-94 |
| `harness/prompts/sections/progressive_tool_rail.go` | ProgressiveToolRail 提示词 | Modify L15, L45 |
| `harness/prompts/sections/agent_mode.go` | PlanMode section builder | 可能需检查 |

---

### Task 1: CreateSubagent 删除多余工厂分支 (Fix-1)

**Files:**
- Modify: `internal/agentcore/harness/deep_agent.go:631-636`

- [ ] **Step 1: 删除 plan_agent/verification_agent/explore_agent 三个 case 分支**

将 L631-636 的三个 case 删除，这些子 Agent 在 Python 中 factory_name 为空，走 default 分支。

修改前：
```go
	case "plan_agent":
		return nil, fmt.Errorf("plan_agent 工厂尚未实现，⤵️ 9.28 回填")
	case "verification_agent":
		return nil, fmt.Errorf("verification_agent 工厂尚未实现，⤵️ 9.29 回填")
	case "explore_agent":
		return nil, fmt.Errorf("explore_agent 工厂尚未实现，⤵️ 9.30 回填")
```

修改后：删除这三行 case。

- [ ] **Step 2: 更新 CreateSubagent 注释，移除 ⤵️ 标记**

将方法注释中的 `⤵️ 9.3 / 9.25-9.31 回填：工厂分派所有分支返回 stub 错误` 更新为仅保留 `⤵️ 9.3 / 9.25-9.27 / 9.31 回填：browser/code/research/mobile_gui 工厂待实现`。

- [ ] **Step 3: 运行测试**

Run: `cd /home/opensource/uap-claw-go && go test ./internal/agentcore/harness/... -count=1 -timeout 120s 2>&1 | tail -20`

- [ ] **Step 4: 提交**

```bash
git add internal/agentcore/harness/deep_agent.go
git commit -m "fix(harness): remove plan/verification/explore agent stub branches from CreateSubagent

Python 中这些子 Agent 的 factory_name 为空，走 default → create_deep_agent，
Go 多余的 stub 分支与 Python 设计矛盾。"
```

---

### Task 2: DeepAgentInterface 加 Card() 方法 (Fix-12a)

**Files:**
- Modify: `internal/agentcore/harness/interfaces/deep_agent.go:20-60`
- Modify: `internal/agentcore/harness/tools/agent_mode/task_tool_factory.go:16`
- Modify: `internal/agentcore/harness/tools/subagent/task_tool.go:90`

- [ ] **Step 1: 在 DeepAgentInterface 接口声明 Card() 方法**

在 `harness/interfaces/deep_agent.go` 的 `DeepAgentInterface` 接口中添加：

```go
	// Card 返回 Agent 身份卡片。
	// 对齐 Python: DeepAgent.card 属性
	Card() *agentschema.AgentCard
```

需要新增 import: `"github.com/uapclaw/uapclaw-go/internal/agentcore/single_agent/agents/schema"` 作为 `agentschema`。

注意：`DeepAgent` 已经通过嵌入 `*agents.ReActAgent` 和实现 `BaseAgent` 接口拥有 `Card()` 方法，所以只需在接口声明即可，无需新写实现。

- [ ] **Step 2: 替换 task_tool_factory.go 中的 ReactAgent().Card()**

文件：`harness/tools/agent_mode/task_tool_factory.go:16`

修改前：
```go
	if card := parentAgent.ReactAgent().Card(); card != nil {
```

修改后：
```go
	if card := parentAgent.Card(); card != nil {
```

- [ ] **Step 3: 替换 task_tool.go 中的 ReactAgent().Card()**

文件：`harness/tools/subagent/task_tool.go:90`

修改前：
```go
		if subagentCard := subagent.ReactAgent().Card(); subagentCard != nil {
```

修改后：
```go
		if subagentCard := subagent.Card(); subagentCard != nil {
```

- [ ] **Step 4: 运行测试**

Run: `cd /home/opensource/uap-claw-go && go test ./internal/agentcore/harness/... -count=1 -timeout 120s 2>&1 | tail -20`

- [ ] **Step 5: 提交**

```bash
git add internal/agentcore/harness/interfaces/deep_agent.go internal/agentcore/harness/tools/agent_mode/task_tool_factory.go internal/agentcore/harness/tools/subagent/task_tool.go
git commit -m "refactor(harness): add Card() to DeepAgentInterface, replace ReactAgent().Card() calls"
```

---

### Task 3: AgentModeRail buildEnterPlanModeStatus/buildPlanFileInfo 三状态分支 + 提示词复刻 (Fix-2)

**Files:**
- Modify: `internal/agentcore/harness/rails/agent_mode.go:816-850`

- [ ] **Step 1: 重写 buildEnterPlanModeStatus**

用 `planFilePath 是否非空` 判断 enter_plan_mode 是否已调用（对齐 Python），提示词一比一复刻。

修改后：
```go
func (r *AgentModeRail) buildEnterPlanModeStatus(planFilePath string, planExists bool) string {
	if r.languageIsCN() {
		if planFilePath != "" {
			return "enter_plan_mode 已调用完成。请继续工作流。"
		}
		return "你尚未调用 enter_plan_mode。请立即调用它作为你的第一个操作。"
	}
	// 英文
	if planFilePath != "" {
		return "enter_plan_mode has been called. Proceed with the workflow."
	}
	return "You have NOT called enter_plan_mode yet. Call it NOW as your first action."
}
```

- [ ] **Step 2: 重写 buildPlanFileInfo**

三状态分支，提示词一比一复刻 Python（含 edit_file/write_file 工具名引用）。

修改后：
```go
func (r *AgentModeRail) buildPlanFileInfo(planFilePath string, planExists bool) string {
	if planFilePath == "" {
		if r.languageIsCN() {
			return "尚无 plan 文件。请先调用 enter_plan_mode 创建。"
		}
		return "No plan file yet. Call enter_plan_mode first to create one."
	}
	if planExists {
		if r.languageIsCN() {
			return fmt.Sprintf("计划文件已存在于 %s。你可以使用 edit_file 工具读取并增量编辑它。", planFilePath)
		}
		return fmt.Sprintf("A plan file already exists at %s. You can read it and make incremental edits using the edit_file tool.", planFilePath)
	}
	if r.languageIsCN() {
		return fmt.Sprintf("计划文件尚不存在。你应该使用 write_file 工具在 %s 创建计划。", planFilePath)
	}
	return fmt.Sprintf("No plan file exists yet. You should create your plan at %s using the write_file tool.", planFilePath)
}
```

- [ ] **Step 3: 运行测试**

Run: `cd /home/opensource/uap-claw-go && go test -tags=test ./internal/agentcore/harness/rails/... -count=1 -timeout 120s 2>&1 | tail -30`

如测试中引用了旧提示词，需同步更新测试期望值。

- [ ] **Step 4: 提交**

```bash
git add internal/agentcore/harness/rails/agent_mode.go internal/agentcore/harness/rails/agent_mode_test.go
git commit -m "fix(rails): AgentModeRail buildEnterPlanModeStatus/buildPlanFileInfo 三状态分支 + 提示词一比一复刻 Python

- 改用 planFilePath 是否非空判断 enter_plan_mode 已调用
- 三状态分支：未调用/已调用未创建/已调用已存在
- 提示词文本严格复刻 Python（含 edit_file/write_file 工具名引用）"
```

---

### Task 4: _is_task_tool_registered 对齐查询 AbilityManager (Fix-3)

**Files:**
- Modify: `internal/agentcore/harness/rails/agent_mode.go:708-719`

- [ ] **Step 1: 重写 isTaskToolRegistered**

改为查询 AbilityManager 检查 task_tool 是否已注册，对齐 Python 的 ResourceMgr 查询。

修改后：
```go
func (r *AgentModeRail) isTaskToolRegistered() bool {
	// 对齐 Python L348-356: 在已注册工具中搜索 "task_tool" 名称
	if r.ownsTaskTool {
		return true
	}
	if r.agent == nil {
		return false
	}
	am := r.agent.AbilityManager()
	if am == nil {
		return false
	}
	for _, card := range am.List() {
		if card != nil && card.Name == "task_tool" {
			return true
		}
	}
	return false
}
```

- [ ] **Step 2: 运行测试**

Run: `cd /home/opensource/uap-claw-go && go test -tags=test ./internal/agentcore/harness/rails/... -count=1 -timeout 120s 2>&1 | tail -30`

- [ ] **Step 3: 提交**

```bash
git add internal/agentcore/harness/rails/agent_mode.go internal/agentcore/harness/rails/agent_mode_test.go
git commit -m "fix(rails): isTaskToolRegistered 对齐 Python 查询 AbilityManager"
```

---

### Task 5: AgentModeRail BeforeModelCall 操作顺序对齐 + buildAvailableAgents 修复 (Fix-7 + Fix-12b)

**Files:**
- Modify: `internal/agentcore/harness/rails/agent_mode.go:265-278` (BeforeModelCall 非 plan 模式)
- Modify: `internal/agentcore/harness/rails/agent_mode.go:772-801` (buildAvailableAgents)

- [ ] **Step 1: 调整 BeforeModelCall 非 plan 模式操作顺序**

将 sync_task_tool 移到 filter_hidden 之前，对齐 Python L162→L196→L190-194 顺序。

修改 L265-278 区域，将：
```go
		// 对齐 Python L164-168: 过滤 hiddenInNormal 工具
		r.filterHiddenTools(cbc, hiddenInNormal)

		// 对齐 Python L196: 同步 task_tool 可见性
		r.syncTaskToolForModelToolInputs(cbc)
```

改为：
```go
		// 对齐 Python L196: 同步 task_tool 可见性
		r.syncTaskToolForModelToolInputs(cbc)

		// 对齐 Python L164-168: 过滤 hiddenInNormal 工具
		r.filterHiddenTools(cbc, hiddenInNormal)
```

- [ ] **Step 2: 重写 buildAvailableAgents 非 SubAgentConfig 分支**

对齐 Python 的 `getattr(spec, "card", None)` fallback 逻辑。使用 `BaseAgent` 接口获取 Card。

修改后：
```go
func (r *AgentModeRail) buildAvailableAgents(subagents []hschema.SubagentSpec) string {
	var lines []string
	for _, spec := range subagents {
		if saConfig, ok := spec.(*hschema.SubAgentConfig); ok && saConfig != nil {
			name := "general-purpose"
			desc := "DeepAgent instance"
			if saConfig.AgentCard != nil {
				name = saConfig.AgentCard.Name
				desc = saConfig.AgentCard.Description
			}
			lines = append(lines, fmt.Sprintf("%q: %s", name, desc))
		} else {
			// 非 SubAgentConfig 类型（如 DeepAgent 实例）
			// 对齐 Python: getattr(spec, "card", None) → getattr(card, "name", None) or "general-purpose"
			name := "general-purpose"
			desc := "DeepAgent instance"
			if baseAgent, ok := spec.(agentinterfaces.BaseAgent); ok {
				if card := baseAgent.Card(); card != nil {
					if card.Name != "" {
						name = card.Name
					}
					if card.Description != "" {
						desc = card.Description
					}
				}
			}
			if name == "general-purpose" {
				// 尝试 SpecName 作为 fallback
				if specName := spec.SpecName(); specName != "" {
					name = specName
				}
			}
			lines = append(lines, fmt.Sprintf("%q: %s", name, desc))
		}
	}
	if len(lines) == 0 {
		return ""
	}
	result := ""
	for i, line := range lines {
		if i > 0 {
			result += "\n"
		}
		result += line
	}
	return result
}
```

- [ ] **Step 3: 运行测试**

Run: `cd /home/opensource/uap-claw-go && go test -tags=test ./internal/agentcore/harness/rails/... -count=1 -timeout 120s 2>&1 | tail -30`

- [ ] **Step 4: 提交**

```bash
git add internal/agentcore/harness/rails/agent_mode.go internal/agentcore/harness/rails/agent_mode_test.go
git commit -m "fix(rails): AgentModeRail BeforeModelCall 顺序对齐 + buildAvailableAgents 读取 Card fallback"
```

---

### Task 6: TaskPlanningRail BeforeModelCall 早退逻辑对齐 (Fix-4)

**Files:**
- Modify: `internal/agentcore/harness/rails/task_planning.go` BeforeModelCall 方法

- [ ] **Step 1: 在 BeforeModelCall 开头加 builder nil 早退**

在 BeforeModelCall 方法开头获取 builder，若 nil 则直接 return nil，跳过 prompt injection 和 model switching。

在现有 BeforeModelCall 方法开头（获取 modelSwitcher 之前）插入：

```go
	// 对齐 Python: if self.system_prompt_builder is None: return
	sb := cbc.Agent().SystemPromptBuilder()
	if sb == nil {
		return nil
	}
```

并将后续的 prompt injection 部分使用此 `sb`，同时将 model switching 逻辑移到 `sb == nil` return 之后。

- [ ] **Step 2: 运行测试**

Run: `cd /home/opensource/uap-claw-go && go test -tags=test ./internal/agentcore/harness/rails/... -count=1 -timeout 120s 2>&1 | tail -30`

- [ ] **Step 3: 提交**

```bash
git add internal/agentcore/harness/rails/task_planning.go internal/agentcore/harness/rails/task_planning_test.go
git commit -m "fix(rails): TaskPlanningRail BeforeModelCall 早退逻辑对齐 Python

当 SystemPromptBuilder 为 nil 时整体 return，跳过 prompt injection 和 model switching。"
```

---

### Task 7: TaskPlanningRail Init 工具去重 + resource_mgr 集成 (Fix-10 + Fix-11)

**Files:**
- Modify: `internal/agentcore/harness/rails/task_planning.go` Init/Uninit 方法

- [ ] **Step 1: Init 中加已有工具去重逻辑**

在创建 todo 工具之前，遍历 `am.List()` 检查是否已有 `todo_create`/`todo_list`/`todo_get`/`todo_modify`，已存在则跳过创建。

在 Init 方法中，`todo.CreateTodosTool(...)` 调用之前加入：

```go
	// 对齐 Python: 检查已有 todo 工具，避免重复注册
	existingTodoNames := make(map[string]struct{})
	for _, card := range am.List() {
		if card != nil && (card.Name == "todo_create" || card.Name == "todo_list" ||
			card.Name == "todo_get" || card.Name == "todo_modify") {
			existingTodoNames[card.Name] = struct{}{}
		}
	}
```

然后根据 `existingTodoNames` 过滤需要创建的工具，如果 4 个都已存在则跳过工具创建但仍设置 todoTool。

- [ ] **Step 2: Init 中加 resource_mgr 注册**

在 `am.Add(t.Card())` 之后，额外调 `resourceMgr.AddTool(t)`：

```go
	resourceMgr := runner.GetResourceMgr()
	// 在每个工具注册到 am 后：
	if resourceMgr != nil {
		resourceMgr.AddTool(t)
	}
```

- [ ] **Step 3: Uninit 中加 resource_mgr 注销**

在 `am.Remove(t.Card().Name)` 之后，额外调 `resourceMgr.RemoveTool()`：

```go
	if resourceMgr != nil {
		_, _ = resourceMgr.RemoveTool([]string{t.Card().ID})
	}
```

- [ ] **Step 4: 运行测试**

Run: `cd /home/opensource/uap-claw-go && go test -tags=test ./internal/agentcore/harness/rails/... -count=1 -timeout 120s 2>&1 | tail -30`

- [ ] **Step 5: 提交**

```bash
git add internal/agentcore/harness/rails/task_planning.go internal/agentcore/harness/rails/task_planning_test.go
git commit -m "fix(rails): TaskPlanningRail Init 工具去重 + resource_mgr 集成

- Init: 检查 AbilityManager 已有 todo 工具再注册
- Init: 补 resource_mgr.AddTool 注册
- Uninit: 补 resource_mgr.RemoveTool 注销"
```

---

### Task 8: ProgressiveToolRail initVisibleTools 有序初始化 (Fix-5)

**Files:**
- Modify: `internal/agentcore/harness/rails/progressive.go:799-807`

- [ ] **Step 1: 改为有序初始化，保证 always 在 default 前面**

修改前（L799-807）：
```go
	// 合并 always_visible + default_visible
	var initial []string
	for k := range r.alwaysVisible {
		initial = append(initial, k)
	}
	for k := range defaultVisible {
		initial = append(initial, k)
	}
	initial = dedupPreserveOrder(initial)
```

修改后：
```go
	// 合并 always_visible + default_visible（对齐 Python: [*always_visible, *default_visible]）
	// 先收集 always（保证排序稳定），再收集 default
	alwaysSlice := setToSortedSlice(r.alwaysVisible)
	defaultSlice := setToSortedSlice(defaultVisible)
	var initial []string
	initial = append(initial, alwaysSlice...)
	initial = append(initial, defaultSlice...)
	initial = dedupPreserveOrder(initial)
```

- [ ] **Step 2: 运行测试**

Run: `cd /home/opensource/uap-claw-go && go test ./internal/agentcore/harness/rails/... -count=1 -timeout 120s 2>&1 | tail -30`

- [ ] **Step 3: 提交**

```bash
git add internal/agentcore/harness/rails/progressive.go internal/agentcore/harness/rails/progressive_test.go
git commit -m "fix(rails): ProgressiveToolRail initVisibleTools 有序初始化

保证 always_visible 在 default_visible 前面，对齐 Python [*always, *default] 插入序。"
```

---

### Task 9: ProgressiveToolRail search_tools trace 事件对齐 Python (Fix-6)

**Files:**
- Modify: `internal/agentcore/harness/tools/tool_discovery/search_tools.go:88-94`
- Modify: `internal/agentcore/harness/tools/tool_discovery/search_tools_test.go:68-69`

- [ ] **Step 1: 修改 search_tools.go trace 事件结构**

修改前（L88-94）：
```go
			traceFn(sess, map[string]any{
					"event_type": "tool_search",
					"query":      input.Query,
					"limit":      limit,
					"count":      len(matches),
					"agent_id":   agentID,
				})
```

修改后（对齐 Python: {"action": "search_tools", "query": ..., "limit": ..., "detail_level": ..., "match_count": ...}）：
```go
			traceFn(sess, map[string]any{
					"action":       "search_tools",
					"query":        input.Query,
					"limit":        limit,
					"detail_level": input.DetailLevel,
					"match_count":  len(matches),
				})
```

- [ ] **Step 2: 更新 search_tools_test.go 中的 trace 断言**

将测试中的 `event["event_type"] != "tool_search"` 改为 `event["action"] != "search_tools"`，同时增加 `detail_level` 和 `match_count` 键的断言，移除 `agent_id` 和 `count` 键的断言。

- [ ] **Step 3: 运行测试**

Run: `cd /home/opensource/uap-claw-go && go test ./internal/agentcore/harness/tools/tool_discovery/... -count=1 -timeout 120s 2>&1 | tail -30`

- [ ] **Step 4: 提交**

```bash
git add internal/agentcore/harness/tools/tool_discovery/search_tools.go internal/agentcore/harness/tools/tool_discovery/search_tools_test.go
git commit -m "fix(tools): search_tools trace 事件结构对齐 Python

改为 {action: search_tools, detail_level, match_count} 结构，
移除 event_type/count/agent_id 字段。"
```

---

### Task 10: 中文引号统一为 ASCII 直引号 (Fix-8)

**Files:**
- Modify: `internal/agentcore/harness/prompts/sections/progressive_tool_rail.go:15`
- Modify: `internal/agentcore/harness/prompts/sections/progressive_tool_rail.go:45`

- [ ] **Step 1: 修改 L15 的 Unicode 弯引号为 ASCII 直引号**

修改前：
```go
"请注意：这里展示的是\u201c工具地图\u201d，不是\u201c全部可立即调用的工具清单\u201d。\n" +
```

修改后：
```go
"请注意：这里展示的是\"工具地图\"，不是\"全部可立即调用的工具清单\"。\n" +
```

- [ ] **Step 2: 修改 L45 的 Unicode 弯引号为 ASCII 直引号**

修改前：
```go
"8. 不要停留在\u201c下一步我将\u2026\u2026\u201d这类自然语言计划上；若已有足够信息选择工具，" +
```

修改后：
```go
"8. 不要停留在\"下一步我将……\"这类自然语言计划上；若已有足够信息选择工具，" +
```

注意：`\u2026\u2026`（两个省略号字符）保持不变，因为 Python 也是 `……`（两个 U+2026）。只改引号。

- [ ] **Step 3: 运行测试**

Run: `cd /home/opensource/uap-claw-go && go test ./internal/agentcore/harness/prompts/... -count=1 -timeout 120s 2>&1 | tail -30`

- [ ] **Step 4: 提交**

```bash
git add internal/agentcore/harness/prompts/sections/progressive_tool_rail.go
git commit -m "fix(prompts): ProgressiveToolRail 中文引号改为 ASCII 直引号

Python 用 \" (U+0022)，Go 误用 \\u201c/\\u201d 弯引号，现已对齐。"
```

---

### Task 11: isPlanFile 用 filepath.EvalSymlinks 解析 symlink (Fix-9)

**Files:**
- Modify: `internal/agentcore/harness/rails/agent_mode.go:557-564`

- [ ] **Step 1: 修改 isPlanFile 的 resolve 函数**

修改前（L557-564）：
```go
	tryResolve := func(p string) string {
		abs, err := filepath.Abs(p)
		if err != nil {
			return p
		}
		return abs
	}
```

修改后：
```go
	tryResolve := func(p string) string {
		abs, err := filepath.Abs(p)
		if err != nil {
			return p
		}
		resolved, err := filepath.EvalSymlinks(abs)
		if err != nil {
			return abs
		}
		return resolved
	}
```

- [ ] **Step 2: 运行测试**

Run: `cd /home/opensource/uap-claw-go && go test -tags=test ./internal/agentcore/harness/rails/... -count=1 -timeout 120s 2>&1 | tail -30`

- [ ] **Step 3: 提交**

```bash
git add internal/agentcore/harness/rails/agent_mode.go internal/agentcore/harness/rails/agent_mode_test.go
git commit -m "fix(rails): isPlanFile 用 filepath.EvalSymlinks 解析 symlink

对齐 Python Path.resolve() 行为，先 Abs 再 EvalSymlinks。"
```

---

### Task 12: 全量回归测试 + 最终提交

- [ ] **Step 1: 运行全量测试**

Run: `cd /home/opensource/uap-claw-go && go test -tags=test ./internal/agentcore/... -count=1 -timeout 300s 2>&1 | tail -30`

- [ ] **Step 2: 检查覆盖率**

Run: `cd /home/opensource/uap-claw-go && go test -tags=test -cover ./internal/agentcore/harness/rails/... -timeout 120s 2>&1 | tail -20`

- [ ] **Step 3: 推送所有提交**

```bash
git push
```

# 10.3.23 Hooks 对齐修复实施计划

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 修复 10.3.23 Hooks 系统与 Python 的对齐问题：ToolArgs 类型改造、RunAll panic 保护和未知类型跳过、handleHooksList 错误保护

**Architecture:** 自底向上实施——先改基础类型（ToolArgs→map、NewArgs→*map），再改消费者（双向同步、冗余解析消除），最后改 hooks 专属逻辑（RunAll/handleHooksList）

**Tech Stack:** Go 1.22+, testify/assert, 项目现有 logger/schema 框架

---

## Task 1: ToolCallInputs.ToolArgs 改为 map[string]any + parseToolArguments

**Files:**
- Modify: `internal/agentcore/single_agent/interfaces/callback.go:197` (ToolArgs 字段)
- Modify: `internal/agentcore/single_agent/interfaces/callback.go:601` (ForkForToolCall 赋值)
- Modify: `internal/agentcore/single_agent/interfaces/inputs_test.go` (断言适配)
- Test: `internal/agentcore/single_agent/interfaces/inputs_test.go`

- [ ] **Step 1: 修改 ToolArgs 字段类型**

在 `callback.go` L197 将 `ToolArgs string` 改为 `ToolArgs map[string]any`，更新注释为 `// ToolArgs 工具参数字典（对齐 Python ToolCallInputs.tool_args: Any，运行时 dict）`

- [ ] **Step 2: 新增 parseToolArguments 函数**

在 `callback.go` 非导出函数区块添加：

```go
// parseToolArguments 将 JSON 字符串解析为 map[string]any
// 对齐 Python: json.loads(tool_call.arguments) if isinstance(str) else tool_call.arguments
func parseToolArguments(args string) map[string]any {
	if args == "" {
		return map[string]any{}
	}
	var result map[string]any
	if err := json.Unmarshal([]byte(args), &result); err != nil {
		return map[string]any{}
	}
	return result
}
```

注意：需要在文件头部 import 中添加 `"encoding/json"`。

- [ ] **Step 3: 修改 ForkForToolCall 赋值**

在 `callback.go` `ForkForToolCall` 方法中，将 `ToolArgs: toolCall.Arguments` 改为 `ToolArgs: parseToolArguments(toolCall.Arguments)`

- [ ] **Step 4: 修改 inputs_test.go 断言**

将 `inputs_test.go` 中 ToolArgs 相关断言从 string 比较改为 map 比较：

```go
// L340 改前
assert.Equal(t, `{"q": "test"}`, inputs.ToolArgs)
// L340 改后
assert.Equal(t, map[string]any{"q": "test"}, inputs.ToolArgs)

// L350 改前
assert.Equal(t, "", inputs.ToolArgs)
// L350 改后
assert.Equal(t, map[string]any{}, inputs.ToolArgs)
```

- [ ] **Step 5: 编译并运行测试**

Run: `cd /home/opensource/uap-claw-go && go build ./internal/agentcore/single_agent/interfaces/... && go test ./internal/agentcore/single_agent/interfaces/... -v -count=1`

预期：interfaces 包编译通过、测试通过。其他包会因 ToolArgs 类型变更编译失败，这是预期的，后续 Task 逐步修复。

- [ ] **Step 6: Commit**

```bash
git add internal/agentcore/single_agent/interfaces/callback.go internal/agentcore/single_agent/interfaces/inputs_test.go
git commit -m "refactor: ToolCallInputs.ToolArgs 从 string 改为 map[string]any，对齐 Python"
```

---

## Task 2: SecurityAllow.NewArgs / ApproveResult.NewArgs 改为 *map[string]any

**Files:**
- Modify: `internal/agentcore/harness/rails/security/base_security_rail.go:48-50` (SecurityAllow.NewArgs)
- Modify: `internal/agentcore/harness/rails/security/base_security_rail.go:188` (Allow 方法签名)
- Modify: `internal/agentcore/harness/rails/security/base_security_rail.go:189` (Allow 构造)
- Modify: `internal/agentcore/harness/rails/security/base_security_rail.go:195-196` (Approve 方法)
- Modify: `internal/agentcore/harness/rails/security/base_security_rail.go:441-443` (applySecurityDecision 判断+赋值)
- Modify: `internal/agentcore/harness/rails/security/base_security_rail.go:423,657,687` (Allow("") 调用点)
- Modify: `internal/agentcore/harness/rails/interrupt/interrupt_base.go:28-29` (ApproveResult.NewArgs)
- Modify: `internal/agentcore/harness/rails/interrupt/interrupt_base.go:102-103` (Approve 方法)
- Modify: `internal/agentcore/harness/rails/interrupt/interrupt_base.go:210` (Approve("") 调用)
- Modify: `internal/agentcore/harness/rails/interrupt/interrupt_base.go:224-225` (applyDecision 判断+赋值)
- Modify: `internal/agentcore/harness/rails/security/tool_security_rail.go:345,403,455,527` (Approve("") 调用点)
- Modify: `internal/agentcore/harness/rails/security/tool_security_rail.go:891-892` (applyPermissionDecision 判断+赋值)
- Modify: `internal/agentcore/harness/rails/security/prompt_security_rail.go:83,95` (Allow("") 调用点)
- Modify: `internal/agentcore/harness/rails/interrupt/confirm_rail.go:96,117` (Approve("") 调用点)
- Modify: `internal/agentcore/harness/rails/security/base_security_rail_test.go` (测试适配)
- Modify: `internal/agentcore/harness/rails/interrupt/interrupt_base_test.go` (测试适配)
- Test: `internal/agentcore/harness/rails/security/...`, `internal/agentcore/harness/rails/interrupt/...`

- [ ] **Step 1: 修改 SecurityAllow.NewArgs 字段**

`base_security_rail.go` L48-50：

```go
// 改前
type SecurityAllow struct {
	NewArgs string
}
// 改后
type SecurityAllow struct {
	// NewArgs 可选，替换后的参数（nil=不替换，对齐 Python new_args: dict | None）
	NewArgs *map[string]any
}
```

- [ ] **Step 2: 修改 Allow/Approve 方法签名和构造**

```go
// 改前
func (r *BaseSecurityRail) Allow(newArgs string) *SecurityAllow {
	return &SecurityAllow{NewArgs: newArgs}
}
func (r *BaseSecurityRail) Approve(newArgs string) *SecurityAllow {
	return r.Allow(newArgs)
}

// 改后
func (r *BaseSecurityRail) Allow(newArgs *map[string]any) *SecurityAllow {
	return &SecurityAllow{NewArgs: newArgs}
}
func (r *BaseSecurityRail) Approve(newArgs *map[string]any) *SecurityAllow {
	return r.Allow(newArgs)
}
```

- [ ] **Step 3: 修改 applySecurityDecision 中 NewArgs 判断和 ToolArgs 赋值**

```go
// L441-443 改前
if d.NewArgs != "" {
	toolInputs.ToolArgs = d.NewArgs
}
// 改后
if d.NewArgs != nil {
	toolInputs.ToolArgs = *d.NewArgs
}
```

- [ ] **Step 4: 修改所有 Allow("") 调用点为 Allow(nil)**

`base_security_rail.go` 中 L423、L657、L687：`r.Allow("")` → `r.Allow(nil)`

`prompt_security_rail.go` 中 L83、L95：`r.Allow("")` → `r.Allow(nil)`

- [ ] **Step 5: 修改 ApproveResult.NewArgs 字段**

`interrupt_base.go` L28-29：

```go
// 改前
type ApproveResult struct {
	NewArgs string
}
// 改后
type ApproveResult struct {
	// NewArgs 可选，替换工具参数（nil=不替换，对齐 Python new_args: dict | None）
	NewArgs *map[string]any
}
```

- [ ] **Step 6: 修改 BaseInterruptRail.Approve 方法**

```go
// 改前
func (r *BaseInterruptRail) Approve(newArgs string) *ApproveResult {
	return &ApproveResult{NewArgs: newArgs}
}
// 改后
func (r *BaseInterruptRail) Approve(newArgs *map[string]any) *ApproveResult {
	return &ApproveResult{NewArgs: newArgs}
}
```

- [ ] **Step 7: 修改 applyDecision 中 NewArgs 判断和 ToolArgs 赋值**

`interrupt_base.go` L224-225：

```go
// 改前
if d.NewArgs != "" {
	toolInputs.ToolArgs = d.NewArgs
}
// 改后
if d.NewArgs != nil {
	toolInputs.ToolArgs = *d.NewArgs
}
```

- [ ] **Step 8: 修改所有 Approve("") 调用点为 Approve(nil)**

`interrupt_base.go` L210：`r.Approve("")` → `r.Approve(nil)`
`tool_security_rail.go` L345、L403、L455、L527：`r.Approve("")` → `r.Approve(nil)`
`confirm_rail.go` L96、L117：`r.Approve("")` → `r.Approve(nil)`

- [ ] **Step 9: 修改 tool_security_rail.go 中 applyPermissionDecision**

L891-892：

```go
// 改前
if d.NewArgs != "" {
	toolInputs.ToolArgs = d.NewArgs
}
// 改后
if d.NewArgs != nil {
	toolInputs.ToolArgs = *d.NewArgs
}
```

- [ ] **Step 10: 修改 base_security_rail_test.go**

```go
// L55 改前
allow := r.Allow("")
// L55 改后
allow := r.Allow(nil)

// L57 改前
assert.Equal(t, "", allow.NewArgs)
// L57 改后
assert.Nil(t, allow.NewArgs)

// L59 改前
allowWithArgs := r.Allow(`{"command": "ls"}`)
// L59 改后
args := map[string]any{"command": "ls"}
allowWithArgs := r.Allow(&args)

// L60 改前
assert.Equal(t, `{"command": "ls"}`, allowWithArgs.NewArgs)
// L60 改后
assert.NotNil(t, allowWithArgs.NewArgs)
assert.Equal(t, args, *allowWithArgs.NewArgs)

// L66 改前
approve := r.Approve("")
// L66 改后
approve := r.Approve(nil)

// L68 改前
assert.Equal(t, "", approve.NewArgs)
// L68 改后
assert.Nil(t, approve.NewArgs)
```

- [ ] **Step 11: 修改 interrupt_base_test.go**

```go
// L66 改前
decision := r.Approve(`{"arg":"val"}`)
// L66 改后
args := map[string]any{"arg": "val"}
decision := r.Approve(&args)

// L68 改前
assert.Equal(t, `{"arg":"val"}`, decision.NewArgs)
// L68 改后
assert.NotNil(t, decision.NewArgs)
assert.Equal(t, args, *decision.NewArgs)

// L266 改前
&ApproveResult{NewArgs: `{"cmd":"pwd"}`}
// L266 改后
pwdArgs := map[string]any{"cmd": "pwd"}
&ApproveResult{NewArgs: &pwdArgs}

// L267 改前
assert.Equal(t, `{"cmd":"pwd"}`, toolInputs.ToolArgs)
// L267 改后
assert.Equal(t, map[string]any{"cmd": "pwd"}, toolInputs.ToolArgs)

// L280 改前
&ApproveResult{NewArgs: ""}
// L280 改后
&ApproveResult{NewArgs: nil}

// L281 改前
assert.Equal(t, `{"cmd":"ls"}`, toolInputs.ToolArgs)
// L281 改后
assert.Equal(t, map[string]any{"cmd": "ls"}, toolInputs.ToolArgs)
```

- [ ] **Step 12: 编译并运行测试**

Run: `cd /home/opensource/uap-claw-go && go build ./internal/agentcore/harness/rails/... && go test ./internal/agentcore/harness/rails/security/... ./internal/agentcore/harness/rails/interrupt/... -v -count=1`

预期：编译通过、测试通过

- [ ] **Step 13: Commit**

```bash
git add internal/agentcore/harness/rails/security/ internal/agentcore/harness/rails/interrupt/
git commit -m "refactor: SecurityAllow.NewArgs 和 ApproveResult.NewArgs 从 string 改为 *map[string]any，对齐 Python dict|None"
```

---

## Task 3: ability_manager.go 双向同步适配

**Files:**
- Modify: `internal/agentcore/single_agent/ability/ability_manager.go:515-516` (ToolArgs→Arguments)
- Modify: `internal/agentcore/single_agent/ability/ability_manager.go:529` (Arguments→ToolArgs)
- Test: `internal/agentcore/single_agent/ability/...`

- [ ] **Step 1: 修改 ToolArgs → Arguments 同步**

L515-516 改为：

```go
// 改前
if inputs.ToolArgs != "" {
	toolCall.Arguments = inputs.ToolArgs
}
// 改后：json.Marshal 转回 string，对齐 Python 隐式 str(dict)
if len(inputs.ToolArgs) > 0 {
	if argsJSON, err := json.Marshal(inputs.ToolArgs); err == nil {
		toolCall.Arguments = string(argsJSON)
	}
}
```

- [ ] **Step 2: 修改 Arguments → ToolArgs 同步**

L529 改为：

```go
// 改前
inputs.ToolArgs = toolCall.Arguments
// 改后：json.Unmarshal 解析，对齐 Python json.loads
var args map[string]any
if err := json.Unmarshal([]byte(toolCall.Arguments), &args); err != nil {
	args = map[string]any{}
}
inputs.ToolArgs = args
```

- [ ] **Step 3: 编译并运行测试**

Run: `cd /home/opensource/uap-claw-go && go build ./internal/agentcore/single_agent/ability/... && go test ./internal/agentcore/single_agent/ability/... -v -count=1`

预期：编译通过、测试通过

- [ ] **Step 4: Commit**

```bash
git add internal/agentcore/single_agent/ability/ability_manager.go
git commit -m "refactor: ability_manager 双向同步适配 ToolArgs map 类型"
```

---

## Task 4: user_hook_rail.go 适配 ToolArgs map

**Files:**
- Modify: `internal/swarm/server/hooks/user_hook_rail.go:84` (ModifiedInput 赋值)
- Modify: `internal/swarm/server/hooks/user_hook_rail_test.go` (断言适配)
- Test: `internal/swarm/server/hooks/...`

- [ ] **Step 1: 修改 BeforeToolCall 中 ModifiedInput 赋值**

L84 改为：

```go
// 改前
jsonBytes, _ := json.Marshal(result.ModifiedInput)
toolInputs.ToolArgs = string(jsonBytes)
// 改后：ModifiedInput 本身就是 map[string]any，直接赋值
if m, ok := result.ModifiedInput.(map[string]any); ok {
	toolInputs.ToolArgs = m
}
```

- [ ] **Step 2: 修改 user_hook_rail_test.go 断言**

`TestUserHookRail_BeforeToolCall_修改输入` 中：

```go
// 改前
expected := `{"file_path":"/safe/path"}`
if inputs.ToolArgs != expected {
    t.Errorf("ToolArgs = %q, want %q", inputs.ToolArgs, expected)
}
// 改后
expected := map[string]any{"file_path": "/safe/path"}
if !reflect.DeepEqual(inputs.ToolArgs, expected) {
    t.Errorf("ToolArgs = %v, want %v", inputs.ToolArgs, expected)
}
```

需要在测试文件头部添加 `"reflect"` import。

- [ ] **Step 3: 编译并运行测试**

Run: `cd /home/opensource/uap-claw-go && go build ./internal/swarm/server/hooks/... && go test ./internal/swarm/server/hooks/... -v -count=1 -run "TestUserHookRail_BeforeToolCall_修改输入"`

预期：编译通过、测试通过

- [ ] **Step 4: Commit**

```bash
git add internal/swarm/server/hooks/user_hook_rail.go internal/swarm/server/hooks/user_hook_rail_test.go
git commit -m "refactor: user_hook_rail 适配 ToolArgs map 类型，消除 json.Marshal"
```

---

## Task 5: agent_mode.go / verification_rail.go 消除冗余解析

**Files:**
- Modify: `internal/agentcore/harness/rails/agent_mode.go:584,600` (parseToolArgs 调用)
- Modify: `internal/agentcore/harness/rails/agent_mode.go:608-618` (删除 parseToolArgs 方法)
- Modify: `internal/agentcore/harness/rails/agent_mode_test.go` (删除 parseToolArgs 测试)
- Modify: `internal/agentcore/harness/rails/subagent/verification_rail.go:248-255` (json.Unmarshal)
- Test: `internal/agentcore/harness/rails/...`

- [ ] **Step 1: 修改 agent_mode.go extractFilePath / extractBashCommand**

L584 和 L600：

```go
// 改前
args := r.parseToolArgs(inputs.ToolArgs)
// 改后
args := inputs.ToolArgs
```

`extractFilePath` 中 `if args == nil` 改为 `if len(args) == 0`。`extractBashCommand` 同理。

- [ ] **Step 2: 删除 agent_mode.go 中 parseToolArgs 方法**

删除 L608-618 整个 `parseToolArgs` 方法。

- [ ] **Step 3: 删除 agent_mode_test.go 中 parseToolArgs 测试**

删除 `TestAgentModeRail_parseToolArgs` 测试函数。

- [ ] **Step 4: 修改 verification_rail.go**

L248-255：

```go
// 改前
toolArgs := toolCallInputs.ToolArgs
var args map[string]any
if err := json.Unmarshal([]byte(toolArgs), &args); err != nil {
	return nil
}
// 改后
args := toolCallInputs.ToolArgs
if len(args) == 0 {
	return nil
}
```

- [ ] **Step 5: 编译并运行测试**

Run: `cd /home/opensource/uap-claw-go && go build ./internal/agentcore/harness/rails/... && go test ./internal/agentcore/harness/rails/agent_mode/... ./internal/agentcore/harness/rails/subagent/... -v -count=1`

预期：编译通过、测试通过

- [ ] **Step 6: Commit**

```bash
git add internal/agentcore/harness/rails/agent_mode.go internal/agentcore/harness/rails/agent_mode_test.go internal/agentcore/harness/rails/subagent/verification_rail.go
git commit -m "refactor: 消除 agent_mode/verification_rail 中冗余 parseToolArgs/json.Unmarshal"
```

---

## Task 6: 修复其他 ToolArgs 编译错误

**Files:**
- 逐一搜索并修复所有因 ToolArgs 类型变更导致的编译错误
- Test: 相关测试

- [ ] **Step 1: 全量编译检查**

Run: `cd /home/opensource/uap-claw-go && go build ./... 2>&1 | head -50`

预期：列出所有编译错误，逐步修复

- [ ] **Step 2: 修复编译错误**

根据编译错误输出，逐一修改。预期涉及：
- `response.go` L54：注释更新（ToolArgs 字段描述）
- `response.go` L78：`ToolArgs: toolCall.Arguments` → `ToolArgs: parseToolArguments(toolCall.Arguments)` 或直接传入已解析的 map
- `interrupt/handler_test.go`：断言适配
- 其他因类型变更产生的编译错误

- [ ] **Step 3: 编译通过**

Run: `cd /home/opensource/uap-claw-go && go build ./...`

预期：0 errors

- [ ] **Step 4: 运行全量测试**

Run: `cd /home/opensource/uap-claw-go && go test ./... -count=1 2>&1 | tail -30`

预期：所有测试通过

- [ ] **Step 5: Commit**

```bash
git add -A
git commit -m "refactor: 修复 ToolArgs 类型变更导致的其余编译错误"
```

---

## Task 7: RunAll panic 保护 + 未知类型跳过

**Files:**
- Modify: `internal/swarm/server/hooks/executor.go` (RunAll 方法重写)
- Modify: `internal/swarm/server/hooks/executor_test.go` (测试适配 + 新增)
- Test: `internal/swarm/server/hooks/...`

- [ ] **Step 1: 重写 RunAll 方法**

替换 `RunAll` 方法体：

```go
func (e *HookExecutor) RunAll(ctx context.Context, hookConfigs []map[string]any, hookInput map[string]any, sessionID string) []HookResult {
	if len(hookConfigs) == 0 {
		return nil
	}

	// 过滤有效配置，对齐 Python: 只把 command/prompt 加入 tasks
	validConfigs := make([]map[string]any, 0, len(hookConfigs))
	for _, cfg := range hookConfigs {
		hookType, _ := cfg["type"].(string)
		if hookType == "prompt" || hookType == "command" || hookType == "" {
			validConfigs = append(validConfigs, cfg)
		}
		// 未知类型：对齐 Python 不加入 tasks
	}
	if len(validConfigs) == 0 {
		return nil
	}

	results := make([]HookResult, len(validConfigs))
	var wg sync.WaitGroup
	for i, cfg := range validConfigs {
		wg.Add(1)
		go func(idx int, c map[string]any) {
			defer wg.Done()
			defer func() {
				if r := recover(); r != nil {
					// 对齐 Python: asyncio.gather(return_exceptions=True) → NON_BLOCKING_ERROR
					results[idx] = HookResult{
						Outcome: HookOutcomeNonBlockingError,
						Error:   fmt.Sprintf("hook panic: %v", r),
					}
				}
			}()
			hookType, _ := c["type"].(string)
			if hookType == "prompt" {
				results[idx] = e.runPromptHook(ctx, c, hookInput)
			} else {
				// command 或空（默认 command）
				results[idx] = e.runCommandHook(ctx, c, hookInput)
			}
		}(i, cfg)
	}
	wg.Wait()
	return results
}
```

- [ ] **Step 2: 修改 RunAll_未知类型 测试**

```go
func TestHookExecutor_RunAll_未知类型(t *testing.T) {
	exec := NewHookExecutor(LLMConfig{})
	hookConfigs := []map[string]any{
		{"type": "unknown", "command": "echo hello"},
	}
	results := exec.RunAll(context.Background(), hookConfigs, map[string]any{}, "")
	// 对齐 Python: 未知类型不加入 tasks，返回空结果
	assert.Equal(t, 0, len(results))
}
```

- [ ] **Step 3: 新增 panic 恢复测试**

```go
// TestHookExecutor_RunAll_panic恢复 测试 goroutine panic 被捕获并转为 NON_BLOCKING_ERROR
func TestHookExecutor_RunAll_panic恢复(t *testing.T) {
	exec := NewHookExecutor(LLMConfig{})
	// 构造一个会触发 panic 的 hook 配置
	// 使用 command 类型，但让 command 为空导致内部不会 panic，
	// 所以我们通过注入一个会 panic 的配置来测试
	// 实际上 runCommandHook 不会 panic（有 error 处理），
	// 但为了测试 defer/recover 路径，我们临时替换逻辑
	// 更简单的方式：用一个包含 nil 值的配置触发 nil pointer panic
	hookConfigs := []map[string]any{
		{"type": "command", "command": "echo ok", "timeout": nil}, // nil timeout 会导致 type assertion panic
	}
	results := exec.RunAll(context.Background(), hookConfigs, map[string]any{}, "")
	require.Len(t, results, 1)
	assert.Equal(t, HookOutcomeNonBlockingError, results[0].Outcome)
	assert.Contains(t, results[0].Error, "hook panic")
}
```

注意：需要根据实际 `runCommandHook` 中对 timeout 的 nil 处理调整测试。如果 timeout 的 type assertion 已经有容错处理（case int / float64），则不会 panic。需要构造其他 panic 路径，或在测试中直接注入一个会 panic 的 mock。

- [ ] **Step 4: 编译并运行测试**

Run: `cd /home/opensource/uap-claw-go && go test ./internal/swarm/server/hooks/... -v -count=1 -run "RunAll"`

预期：所有 RunAll 相关测试通过

- [ ] **Step 5: Commit**

```bash
git add internal/swarm/server/hooks/executor.go internal/swarm/server/hooks/executor_test.go
git commit -m "fix: RunAll goroutine panic 保护 + 未知类型跳过，对齐 Python asyncio.gather"
```

---

## Task 8: handleHooksList 错误保护

**Files:**
- Modify: `internal/swarm/server/handle_extensions.go` (handleHooksList 方法)
- Modify: `internal/swarm/server/handle_extensions_test.go` (新增 error 测试)
- Test: `internal/swarm/server/...`

- [ ] **Step 1: 修改 handleHooksList 方法**

```go
// handleHooksList 处理 hooks.list 请求。返回 hooks 配置摘要。
// 对齐 Python: _handle_hooks_list — payload 包含 events、disable_all_hooks、source
// 对齐 Python: try/except 包裹，失败时返回 ok=False
func (s *AgentServer) handleHooksList(_ context.Context, request *schema.AgentRequest) (*schema.AgentResponse, error) {
	configBase, loadErr := s.config.Load()
	if loadErr != nil {
		logger.Warn(logComponent).Err(loadErr).Msg("handleHooksList: config 加载失败")
		return schema.NewAgentResponse(request.RequestID, request.ChannelID,
			schema.WithResponseOK(false),
			schema.WithPayload(map[string]any{"error": loadErr.Error()}),
		), nil
	}
	hooksCfg := hookscfg.LoadHooksConfig(configBase)
	summary := hooksCfg.GetEventSummary()
	return schema.NewAgentResponse(request.RequestID, request.ChannelID,
		schema.WithPayload(map[string]any{
			"events":            summary,
			"disable_all_hooks": hooksCfg.DisableAllHooks,
			"source":            "config.yaml",
		}),
	), nil
}
```

- [ ] **Step 2: 新增 config.Load() 失败测试**

在 `handle_extensions_test.go` 中新增：

```go
// TestAgentServer_handleHooksList_configLoad失败 测试 config.Load() 失败时返回 ok=false
func TestAgentServer_handleHooksList_configLoad失败(t *testing.T) {
	s := &AgentServer{
		config: newMockConfigStoreWithError(fmt.Errorf("config load failed")),
	}
	req := &schema.AgentRequest{
		RequestID:  "test-err",
		ChannelID:  "ch-err",
		ReqMethod:  schema.ReqMethodHooksList,
	}
	resp, err := s.handleHooksList(context.Background(), req)
	require.NoError(t, err)
	assert.False(t, resp.Ok)
	assert.Contains(t, resp.Payload["error"], "config load failed")
}
```

需要在测试文件中添加 mock config store（返回 error 的版本）。

- [ ] **Step 3: 编译并运行测试**

Run: `cd /home/opensource/uap-claw-go && go test ./internal/swarm/server/... -v -count=1 -run "handleHooksList|handleExtensions"`

预期：所有测试通过

- [ ] **Step 4: Commit**

```bash
git add internal/swarm/server/handle_extensions.go internal/swarm/server/handle_extensions_test.go
git commit -m "fix: handleHooksList 正确处理 config.Load() error，对齐 Python try/except ok=False"
```

---

## Task 9: 全量测试验证

**Files:** 无新文件修改

- [ ] **Step 1: 全量编译**

Run: `cd /home/opensource/uap-claw-go && go build ./...`

预期：0 errors

- [ ] **Step 2: 全量测试**

Run: `cd /home/opensource/uap-claw-go && go test -cover ./... -count=1 2>&1 | tail -50`

预期：所有测试通过，覆盖率 ≥ 85%

- [ ] **Step 3: 更新 doc.go（如有变更）**

检查 `internal/agentcore/single_agent/interfaces/doc.go`、`internal/swarm/server/hooks/doc.go` 是否需要更新 ToolArgs 类型变更说明。

- [ ] **Step 4: 最终 Commit**

```bash
git add -A
git commit -m "chore: 10.3.23 hooks 对齐修复全量验证通过"
```

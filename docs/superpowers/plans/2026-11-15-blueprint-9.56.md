# 9.56 Blueprint（团队蓝图定义）完整实现计划

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 完整实现 9.56 Blueprint 两层（TeamAgentBlueprint + TeamAgentSpec），前置实现所有依赖类型 Schema，100% 对齐 Python 逻辑。

**Architecture:** 分 8 个 Task 组按依赖顺序实现：① i18n → ② constants → ③ 前置依赖 Schema（models/memory/messager/database/worktree/workspace）→ ④ schema/blueprint.go 核心类型+校验 → ⑤ schema/team.go 类型替换 → ⑥ agent/blueprint.go 完善 → ⑦ doc.go 更新 → ⑧ IMPLEMENTATION_PLAN.md 状态同步

**Tech Stack:** Go 1.26, testify/assert, 项目内 llmschema/agentschema 包

---

## File Structure

### 新增文件（14 个源码 + 14 个测试）

```
internal/agent_teams/
├── i18n.go                          # i18n 双语字典 + T()/SetLanguage()/GetLanguage()
├── i18n_test.go
├── constants_test.go
├── models/
│   ├── doc.go
│   ├── pool.go                      # ModelPoolEntry + ModelRouterConfig + InheritPoolIDs
│   ├── pool_test.go
│   ├── allocator.go                 # Allocation + ModelAllocator 接口 + BuildModelAllocator/ResolveMemberModel 留桩
│   └── allocator_test.go
├── memory/
│   ├── doc.go
│   ├── config.go                    # TeamMemoryConfig + ResolveEmbeddingConfig
│   └── config_test.go
├── messager/
│   ├── doc.go
│   ├── base.go                      # MessagerPeerConfig + MessagerTransportConfig + SubscriptionHandle + CreateMessager 留桩
│   └── base_test.go
├── team_workspace/
│   ├── doc.go
│   ├── models.go                    # WorkspaceMode + ConflictStrategy + TeamWorkspaceConfig + WorkspaceFileLock
│   └── models_test.go
└── tools/
    ├── doc.go
    └── database/
        ├── doc.go
        ├── config.go                # DatabaseType + DatabaseConfig + MemoryDatabaseConfig
        └── config_test.go

internal/agentcore/harness/tools/worktree/
├── doc.go
├── models.go                        # WorktreeLifecyclePolicy + WorktreeConfig + WorktreeSession + WorktreeCreateResult + WorktreeChangeSummary
└── models_test.go
```

### 修改文件（11 个）

```
internal/agent_teams/schema/blueprint.go    # 补 JSON 标签 + 默认值 + 校验 + Build + 注册表 + DeepAgentSpec 完整字段 + TeamModelConfig
internal/agent_teams/schema/blueprint_test.go  # 新增
internal/agent_teams/schema/team.go         # 补 JSON 标签 + any→具体类型
internal/agent_teams/schema/team_test.go    # 新增
internal/agent_teams/agent/blueprint.go     # 补 JSON 标签 + MemberName→*string
internal/agent_teams/agent/blueprint_test.go  # 新增
internal/agent_teams/constants.go           # 新增 ReservedMemberNames 集合
internal/agent_teams/doc.go                 # 更新文件目录
internal/agent_teams/agent/doc.go           # 更新文件目录
internal/agent_teams/schema/doc.go          # 更新文件目录
IMPLEMENTATION_PLAN.md                      # 状态同步
```

---

### Task 1: i18n 国际化（9.55 前置）

**Files:**
- Create: `internal/agent_teams/i18n.go`
- Create: `internal/agent_teams/i18n_test.go`

- [ ] **Step 1: 编写 i18n.go**

创建 `internal/agent_teams/i18n.go`，对齐 Python `openjiuwen/agent_teams/i18n.py`。

包含：
- `Language` 类型 + `LanguageCN`/`LanguageEN` 常量
- `STRINGS` 双语字典（完整对齐 Python，包含 blueput.default_persona / team.* / dispatcher.* / hitt.* 全部条目）
- `currentLanguage` 包级变量（默认 `cn`）
- `SetLanguage(lang Language) error` — 设置全局语言
- `GetLanguage() Language` — 获取当前语言
- `T(key string, kwargs ...map[string]any) string` — 解析本地化字符串，支持 `format_map` 风格插值

- [ ] **Step 2: 编写 i18n_test.go**

测试用例：
- `TestT_中文默认` — 验证 `T("blueput.default_persona")` 返回 "天才项目管理专家"
- `TestT_英文切换` — SetLanguage("en") 后验证返回 "Genius project management expert"
- `TestT_缺失Key` — 验证缺失 key 报错
- `TestSetLanguage_不支持` — 验证不支持的 lang 报错
- `TestGetLanguage` — 验证默认值和切换后值

- [ ] **Step 3: 运行测试**

```bash
cd /home/opensource/uapclaw-gateway && go test ./internal/agent_teams/ -run "TestT_|TestSetLanguage|TestGetLanguage" -v
```

- [ ] **Step 4: Commit**

```bash
git add internal/agent_teams/i18n.go internal/agent_teams/i18n_test.go
git commit -m "feat(agent_teams): 新增 i18n 双语字典，对齐 Python i18n.py (9.55 前置)"
```

---

### Task 2: constants 补充（ReservedMemberNames）

**Files:**
- Modify: `internal/agent_teams/constants.go`
- Create: `internal/agent_teams/constants_test.go`

- [ ] **Step 1: 修改 constants.go**

在现有常量后新增：

```go
// ReservedMemberNames 保留的成员名集合，用户声明成员不得使用。
// 对齐 Python: RESERVED_MEMBER_NAMES
var ReservedMemberNames = map[string]bool{
    HumanAgentMemberName:  true,
    UserPseudoMemberName:  true,
    DefaultLeaderMemberName: true,
}
```

- [ ] **Step 2: 编写 constants_test.go**

测试用例：
- `TestReservedMemberNames_包含` — 验证三个保留名都在集合中
- `TestReservedMemberNames_不包含` — 验证普通名不在集合中
- `TestConstantValues` — 验证常量值与 Python 一致

- [ ] **Step 3: 运行测试**

```bash
cd /home/opensource/uapclaw-gateway && go test ./internal/agent_teams/ -run "TestReserved|TestConstant" -v
```

- [ ] **Step 4: Commit**

```bash
git add internal/agent_teams/constants.go internal/agent_teams/constants_test.go
git commit -m "feat(agent_teams): 新增 ReservedMemberNames 集合，对齐 Python constants.py"
```

---

### Task 3: models/pool.go — ModelPoolEntry + ModelRouterConfig + InheritPoolIDs

**Files:**
- Create: `internal/agent_teams/models/doc.go`
- Create: `internal/agent_teams/models/pool.go`
- Create: `internal/agent_teams/models/pool_test.go`

- [ ] **Step 1: 创建 models/doc.go**

包文档，说明 models 包提供模型池和分配器相关类型。

- [ ] **Step 2: 编写 pool.go**

对齐 Python `openjiuwen/agent_teams/models/pool.py`：

```go
package models

import (
    "crypto/sha1"
    "encoding/json"
    "fmt"
    "strings"

    "github.com/google/uuid"
    llmschema "github.com/uapclaw/uapclaw-go/internal/agentcore/foundation/llm/schema"
    atschema "github.com/uapclaw/uapclaw-go/internal/agent_teams/schema"
)
```

包含：
- `ModelPoolEntry` 结构体（ModelName/APIKey/APIBaseURL/APIProvider/Description/ModelID/Metadata）+ JSON 标签
- `ModelPoolEntryOption` 选项函数类型
- `NewModelPoolEntry()` 构造函数（ModelID 自动 UUID）
- `ToTeamModelConfig()` 完整实现（合并 metadata.client/request 到 ModelClientConfig/ModelRequestConfig）
- `entrySignature()` 内部方法（JSON 排除 model_id 后 sort_keys 序列化）
- `ModelRouterConfig` 结构体 + JSON 标签
- `Validate()` — 校验 model_names 非空/非空白/唯一
- `ToPoolEntries()` — 展开为 `[]ModelPoolEntry`
- `InheritPoolIDs()` — 完整实现，按 bit-exact signature 匹配

- [ ] **Step 3: 编写 pool_test.go**

测试用例：
- `TestNewModelPoolEntry` — 构造 + ModelID 自动生成
- `TestModelPoolEntry_ToTeamModelConfig` — 基础字段映射 + metadata 合并
- `TestModelPoolEntry_ToTeamModelConfig_MetadataMerge` — client/request 子字典合并
- `TestModelRouterConfig_Validate_正常` — 合法配置
- `TestModelRouterConfig_Validate_空白` — 空白 model_name 报错
- `TestModelRouterConfig_Validate_重复` — 重复 model_name 报错
- `TestModelRouterConfig_ToPoolEntries` — 展开验证
- `TestInheritPoolIDs_完全匹配` — 完全一致时 model_id 继承
- `TestInheritPoolIDs_无匹配` — 无匹配时保留新 id
- `TestInheritPoolIDs_部分匹配` — 部分匹配场景
- `TestInheritPoolIDs_顺序无关` — 重排后仍匹配

- [ ] **Step 4: 运行测试**

```bash
cd /home/opensource/uapclaw-gateway && go test ./internal/agent_teams/models/ -v
```

- [ ] **Step 5: Commit**

```bash
git add internal/agent_teams/models/
git commit -m "feat(agent_teams/models): 新增 ModelPoolEntry/ModelRouterConfig/InheritPoolIDs，对齐 Python pool.py (9.64 前置)"
```

---

### Task 4: models/allocator.go — Allocation + ModelAllocator 接口 + 留桩

**Files:**
- Create: `internal/agent_teams/models/allocator.go`
- Create: `internal/agent_teams/models/allocator_test.go`

- [ ] **Step 1: 编写 allocator.go**

对齐 Python `openjiuwen/agent_teams/models/allocator.py`：

包含：
- `Allocation` 结构体（Entry ModelPoolEntry + GroupIndex int）
- `ToTeamModelConfig()` 完整实现
- `ToDBRef()` 完整实现 → `{"model_name": ..., "model_index": ...}`
- `ModelAllocator` 接口（Allocate/StateDict/LoadStateDict）
- `BuildModelAllocator()` — **留桩**，⤵️ 回填: 9.64 — allocator 运行时逻辑
- `ResolveMemberModel()` — **留桩**，⤵️ 回填: 9.64 — member model 解析
- `poolDigest()` 内部函数（SHA1 摘要）
- `RoundRobinModelAllocator` / `ByModelNameAllocator` / `RouterAllocator` — **留桩结构体**，⤵️ 回填: 9.64

注意：`BuildModelAllocator` 和 `ResolveMemberModel` 函数签名完整，内部 panic 或返回 nil + 日志标注回填。

- [ ] **Step 2: 编写 allocator_test.go**

测试用例：
- `TestAllocation_ToTeamModelConfig` — 委托 Entry.ToTeamModelConfig
- `TestAllocation_ToDBRef` — 返回正确 map
- `TestBuildModelAllocator_留桩` — 验证留桩不 panic（空 pool 返回 nil）

- [ ] **Step 3: 运行测试**

```bash
cd /home/opensource/uapclaw-gateway && go test ./internal/agent_teams/models/ -v
```

- [ ] **Step 4: Commit**

```bash
git add internal/agent_teams/models/allocator.go internal/agent_teams/models/allocator_test.go
git commit -m "feat(agent_teams/models): 新增 Allocation/ModelAllocator 接口，BuildModelAllocator 留桩 (9.64 前置)"
```

---

### Task 5: memory/config.go — TeamMemoryConfig

**Files:**
- Create: `internal/agent_teams/memory/doc.go`
- Create: `internal/agent_teams/memory/config.go`
- Create: `internal/agent_teams/memory/config_test.go`

- [ ] **Step 1: 编写 doc.go + config.go**

对齐 Python `openjiuwen/agent_teams/memory/config.py`：

```go
type TeamMemoryConfig struct {
    Enabled                 bool    `json:"enabled"`
    Scenario                string  `json:"scenario"`
    EmbeddingConfig         any     `json:"-"`                         // ⤵️ 回填: 9.64 — EmbeddingConfig 类型
    AutoExtract             bool    `json:"auto_extract"`
    SharedMemory            bool    `json:"shared_memory"`
    MemberMemoryPromptMode  string  `json:"member_memory_prompt_mode"`
    TimezoneOffsetHours     float64 `json:"timezone_offset_hours"`
    ParentWorkspacePath     string  `json:"-"`                         // 不序列化
    TeamMemoryDir           string  `json:"-"`                         // 不序列化
}

func NewTeamMemoryConfig() TeamMemoryConfig  // 默认值对齐 Python
func ResolveEmbeddingConfig(config *TeamMemoryConfig) any  // ⤵️ 回填: 9.64
```

- [ ] **Step 2: 编写 config_test.go**

- `TestNewTeamMemoryConfig` — 验证默认值
- `TestTeamMemoryConfig_JSON序列化` — 验证 exclude 字段不序列化

- [ ] **Step 3: 运行测试 + Commit**

```bash
cd /home/opensource/uapclaw-gateway && go test ./internal/agent_teams/memory/ -v
git add internal/agent_teams/memory/ && git commit -m "feat(agent_teams/memory): 新增 TeamMemoryConfig，对齐 Python config.py (9.64 前置)"
```

---

### Task 6: messager/base.go — MessagerTransportConfig

**Files:**
- Create: `internal/agent_teams/messager/doc.go`
- Create: `internal/agent_teams/messager/base.go`
- Create: `internal/agent_teams/messager/base_test.go`

- [ ] **Step 1: 编写 doc.go + base.go**

对齐 Python `openjiuwen/agent_teams/messager/base.py`：

包含：
- `MessagerPeerConfig` 结构体（AgentID/PeerID/Addrs/Metadata）+ JSON 标签
- `MessagerTransportConfig` 结构体（全部字段 + JSON 标签 + 默认值）
- `BroadcastTopic()` 完整实现 → `"team:{team_name}:broadcast"`
- `SubscriptionHandle` 结构体
- `CreateMessager()` — **留桩**，⤵️ 回填: 9.65

- [ ] **Step 2: 编写 base_test.go**

- `TestNewMessagerTransportConfig` — 默认值
- `TestMessagerTransportConfig_BroadcastTopic` — topic 格式
- `TestMessagerPeerConfig` — 字段赋值

- [ ] **Step 3: 运行测试 + Commit**

```bash
cd /home/opensource/uapclaw-gateway && go test ./internal/agent_teams/messager/ -v
git add internal/agent_teams/messager/ && git commit -m "feat(agent_teams/messager): 新增 MessagerTransportConfig，对齐 Python base.py (9.65 前置)"
```

---

### Task 7: tools/database/config.go — DatabaseConfig

**Files:**
- Create: `internal/agent_teams/tools/doc.go`
- Create: `internal/agent_teams/tools/database/doc.go`
- Create: `internal/agent_teams/tools/database/config.go`
- Create: `internal/agent_teams/tools/database/config_test.go`

- [ ] **Step 1: 编写全部文件**

对齐 Python `openjiuwen/agent_teams/tools/database/config.py`：

```go
type DatabaseType string
const (
    DatabaseTypeSQLite     DatabaseType = "sqlite"
    DatabaseTypePostgreSQL DatabaseType = "postgresql"
    DatabaseTypeMySQL      DatabaseType = "mysql"
    DatabaseTypeMemory     DatabaseType = "memory"
)

type DatabaseConfig struct {
    DBType           DatabaseType `json:"db_type"`
    ConnectionString string       `json:"connection_string"`
    DBTimeout        int          `json:"db_timeout"`
    DBEnableWAL      bool         `json:"db_enable_wal"`
}

func NewDatabaseConfig() DatabaseConfig  // 默认: sqlite, "", 30, true

type MemoryDatabaseConfig struct {
    DBType           DatabaseType `json:"db_type"`
    ConnectionString string       `json:"connection_string"`
}

func NewMemoryDatabaseConfig() MemoryDatabaseConfig  // 默认: memory, ""
```

- [ ] **Step 2: 编写 config_test.go**

- `TestNewDatabaseConfig` — 默认值验证
- `TestNewMemoryDatabaseConfig` — 默认值验证
- `TestDatabaseConfig_JSON序列化往返` — JSON round-trip

- [ ] **Step 3: 运行测试 + Commit**

```bash
cd /home/opensource/uapclaw-gateway && go test ./internal/agent_teams/tools/database/ -v
git add internal/agent_teams/tools/ && git commit -m "feat(agent_teams/tools/database): 新增 DatabaseConfig/MemoryDatabaseConfig，对齐 Python (9.65 前置)"
```

---

### Task 8: harness/tools/worktree/models.go — WorktreeConfig

**Files:**
- Create: `internal/agentcore/harness/tools/worktree/doc.go`
- Create: `internal/agentcore/harness/tools/worktree/models.go`
- Create: `internal/agentcore/harness/tools/worktree/models_test.go`

- [ ] **Step 1: 编写 doc.go + models.go**

对齐 Python `openjiuwen/harness/tools/worktree/models.py`：

包含：
- `WorktreeLifecyclePolicy` 枚举（Auto/Ephemeral/Durable）
- `WorktreeConfig` 结构体（全部字段 + JSON 标签 + 默认值）
- `WorktreeSession` 结构体
- `WorktreeCreateResult` 结构体
- `WorktreeChangeSummary` 结构体
- `NewWorktreeConfig()` 构造函数

- [ ] **Step 2: 编写 models_test.go**

- `TestNewWorktreeConfig` — 默认值
- `TestWorktreeConfig_JSON序列化` — JSON round-trip

- [ ] **Step 3: 运行测试 + Commit**

```bash
cd /home/opensource/uapclaw-gateway && go test ./internal/agentcore/harness/tools/worktree/ -v
git add internal/agentcore/harness/tools/worktree/ && git commit -m "feat(harness/tools/worktree): 新增 WorktreeConfig 系列，对齐 Python models.py (9.66 前置)"
```

---

### Task 9: team_workspace/models.go — TeamWorkspaceConfig

**Files:**
- Create: `internal/agent_teams/team_workspace/doc.go`
- Create: `internal/agent_teams/team_workspace/models.go`
- Create: `internal/agent_teams/team_workspace/models_test.go`

- [ ] **Step 1: 编写 doc.go + models.go**

对齐 Python `openjiuwen/agent_teams/team_workspace/models.py`：

包含：
- `WorkspaceMode` 枚举（Local/Distributed）
- `ConflictStrategy` 枚举（Lock/Merge/LastWriteWins）
- `TeamWorkspaceConfig` 结构体（全部字段 + JSON 标签 + 默认值）
- `WorkspaceFileLock` 结构体 + `IsExpired()` 方法
- `NewTeamWorkspaceConfig()` 构造函数

- [ ] **Step 2: 编写 models_test.go**

- `TestNewTeamWorkspaceConfig` — 默认值
- `TestWorkspaceFileLock_IsExpired_未过期` — 未超期返回 false
- `TestWorkspaceFileLock_IsExpired_已过期` — 已超期返回 true

- [ ] **Step 3: 运行测试 + Commit**

```bash
cd /home/opensource/uapclaw-gateway && go test ./internal/agent_teams/team_workspace/ -v
git add internal/agent_teams/team_workspace/ && git commit -m "feat(agent_teams/team_workspace): 新增 TeamWorkspaceConfig 系列，对齐 Python models.py (9.66 前置)"
```

---

### Task 10: schema/blueprint.go 核心重写 — TeamModelConfig + DeepAgentSpec + LeaderSpec + TransportSpec/StorageSpec 注册表 + TeamAgentSpec 校验 + Build

**Files:**
- Modify: `internal/agent_teams/schema/blueprint.go`
- Create: `internal/agent_teams/schema/blueprint_test.go`

这是最核心的 Task，修改量最大。

- [ ] **Step 1: 重写 blueprint.go**

完整重写 `schema/blueprint.go`，按声明顺序：

**① 结构体区块：**
- `TeamModelConfig`（ModelClientConfig + ModelRequestConfig + Build 留桩）
- `DeepAgentSpec` 完整字段（20+ 字段，对齐 Python，含 JSON 标签，子规格类型用最小占位结构体）
- `LeaderSpec`（补 JSON 标签 + NewLeaderSpec 构造函数）
- `TransportSpec`（补 JSON 标签 + Build 方法）
- `StorageSpec`（补 JSON 标签 + Build 方法）
- `TeamAgentSpec`（补全所有字段 + JSON 标签 + NewTeamAgentSpec 构造函数）

**DeepAgentSpec 子规格占位类型**（9.57 回填完整字段）：
- `WorkspaceSpec` — 最小骨架（RootPath/Language/StableBase）
- `VisionModelSpec` — 最小骨架
- `AudioModelSpec` — 最小骨架
- `ProgressiveToolSpec` — 最小骨架
- `SysOperationSpec` — 最小骨架
- `RailSpec` — 最小骨架（Type/Params）
- `BuiltinToolSpec` — 最小骨架（Type/Params）
- `SubAgentSpec` — 最小骨架

**② 常量区块：**
- `defaultTeamName` = "agent_team"
- `defaultTeammateMode` = "build_mode"
- `defaultSpawnMode` = "process"
- `defaultModelPoolStrategy` = "round_robin"

**③ 全局变量区块：**
- `transportRegistry` map
- `storageRegistry` map

**④ 导出函数区块：**
- `NewTeamModelConfig()` 构造函数
- `TeamModelConfig.Build()` — ⤵️ 回填: 9.57
- `NewLeaderSpec()` — 默认值，Persona 通过 i18n
- `NewTeamAgentSpec()` — 默认值
- `TransportSpec.Build()` — 通过注册表构建 MessagerTransportConfig
- `StorageSpec.Build()` — 通过注册表构建 DatabaseConfig
- `RegisterTransport()` — 注册传输构建器
- `RegisterStorage()` — 注册存储构建器
- `ensureBuiltinInfraRegistered()` — 注册内置 inprocess/pyzmq/sqlite 等
- `TeamAgentSpec.Validate()` — 组合校验入口
- `TeamAgentSpec.validatePoolRouterExclusive()` — pool/router 互斥
- `TeamAgentSpec.defaultTransportForSpawnMode()` — inprocess 自动填充
- `TeamAgentSpec.validateReservedNames()` — 保留名校验
- `TeamAgentSpec.validateHittConsistency()` — HITT 一致性
- `TeamAgentSpec.validateLeaderModelResolved()` — Leader 模型解析
- `TeamAgentSpec.ResolveDBConfig()` — 数据库配置解析
- `TeamAgentSpec.Build()` — **留桩**，⤵️ 回填: 9.57

**⑤ 非导出函数区块：**
- 注册表初始化逻辑

- [ ] **Step 2: 编写 blueprint_test.go**

测试用例（按校验方法分组）：

**TeamModelConfig:**
- `TestNewTeamModelConfig` — 默认值
- `TestTeamModelConfig_Build_留桩` — 验证留桩返回 nil

**LeaderSpec:**
- `TestNewLeaderSpec` — 默认值验证（MemberName="team_leader", Persona 通过 i18n）
- `TestNewLeaderSpec_英文` — SetLanguage("en") 后验证

**TransportSpec:**
- `TestTransportSpec_Build_未注册` — 未注册 type 报错
- `TestTransportSpec_Build_内置` — ensureBuiltinInfraRegistered 后构建

**StorageSpec:**
- `TestStorageSpec_Build_未注册` — 未注册 type 报错
- `TestStorageSpec_Build_内置` — 构建 sqlite

**注册表:**
- `TestRegisterTransport` — 注册 + 查找
- `TestRegisterStorage` — 注册 + 查找
- `TestEnsureBuiltinInfraRegistered` — 内置类型已注册

**TeamAgentSpec 校验:**
- `TestTeamAgentSpec_Validate_正常` — 合法配置
- `TestTeamAgentSpec_Validate_PoolRouter互斥` — 同时设置报错
- `TestTeamAgentSpec_DefaultTransportForSpawnMode_inprocess` — spawn_mode=inprocess 自动填充
- `TestTeamAgentSpec_DefaultTransportForSpawnMode_process` — spawn_mode=process 不填充
- `TestTeamAgentSpec_ValidateReservedNames_正常` — 合法名
- `TestTeamAgentSpec_ValidateReservedNames_保留名` — 使用保留名报错
- `TestTeamAgentSpec_ValidateReservedNames_HumanAgent允许` — enable_hitt=True 时 human_agent 允许
- `TestTeamAgentSpec_ValidateHittConsistency_正常` — enable_hitt=True 有 HUMAN_AGENT
- `TestTeamAgentSpec_ValidateHittConsistency_未启用` — enable_hitt=False 有 HUMAN_AGENT 报错
- `TestTeamAgentSpec_ValidateLeaderModelResolved_有模型` — 有模型不报错
- `TestTeamAgentSpec_ValidateLeaderModelResolved_无模型` — 无模型报错
- `TestTeamAgentSpec_ResolveDBConfig_默认` — 默认 sqlite
- `TestTeamAgentSpec_ResolveDBConfig_指定` — 指定 storage
- `TestTeamAgentSpec_Build_留桩` — 验证留桩返回 nil

**DeepAgentSpec:**
- `TestDeepAgentSpec_默认值` — NewDeepAgentSpec 默认值
- `TestDeepAgentSpec_JSON序列化` — JSON round-trip

- [ ] **Step 3: 运行测试**

```bash
cd /home/opensource/uapclaw-gateway && go test ./internal/agent_teams/schema/ -v -run "TestNewTeamModel|TestNewLeader|TestTransport|TestStorage|TestRegister|TestEnsure|TestTeamAgentSpec|TestDeepAgentSpec"
```

- [ ] **Step 4: Commit**

```bash
git add internal/agent_teams/schema/blueprint.go internal/agent_teams/schema/blueprint_test.go
git commit -m "feat(agent_teams/schema): 重写 blueprint.go，完整对齐 Python TeamAgentSpec/DeepAgentSpec/校验/注册表 (9.56 核心)"
```

---

### Task 11: schema/team.go — any 替换为具体类型 + JSON 标签

**Files:**
- Modify: `internal/agent_teams/schema/team.go`
- Create: `internal/agent_teams/schema/team_test.go`

- [ ] **Step 1: 修改 team.go**

关键变更：
- `TeamSpec.ModelPool` — `any` → `[]models.ModelPoolEntry`（import models 包）
- `TeamRuntimeContext.MessagerConfig` — `any` → `*messager.MessagerTransportConfig`
- `TeamRuntimeContext.DBConfig` — `any` → `db.DatabaseConfig`（使用 interface：`DBConfig` 字段类型改为 `any` + JSON 标签，因为 Python 中是 `DatabaseConfig | MemoryDatabaseConfig` 联合类型，Go 中用 `any` + json 序列化）
- `TeamRuntimeContext.MemberModel` — `any` → `*TeamModelConfig`
- 所有结构体补全 JSON 标签

注意：`TeamRuntimeContext.DBConfig` 保留为 `any`（因为 Python 是 `DatabaseConfig | MemoryDatabaseConfig` 联合类型），但添加注释说明类型范围。

- [ ] **Step 2: 编写 team_test.go**

- `TestTeamRole_值` — 验证枚举值
- `TestTeamLifecycle_值` — 验证枚举值
- `TestMemberOpResult_成功` — NewMemberOpResultSuccess
- `TestMemberOpResult_失败` — NewMemberOpResultFail
- `TestTeamSpec_JSON序列化` — JSON round-trip
- `TestTeamRuntimeContext_JSON序列化` — JSON round-trip
- `TestTeamMemberSpec_JSON序列化` — JSON round-trip

- [ ] **Step 3: 运行测试**

```bash
cd /home/opensource/uapclaw-gateway && go test ./internal/agent_teams/schema/ -v
```

- [ ] **Step 4: Commit**

```bash
git add internal/agent_teams/schema/team.go internal/agent_teams/schema/team_test.go
git commit -m "feat(agent_teams/schema): team.go 补 JSON 标签 + any→具体类型 (9.56)"
```

---

### Task 12: agent/blueprint.go — 补 JSON 标签 + MemberName 改 *string

**Files:**
- Modify: `internal/agent_teams/agent/blueprint.go`
- Create: `internal/agent_teams/agent/blueprint_test.go`

- [ ] **Step 1: 修改 blueprint.go**

变更：
- TeamAgentBlueprint 所有字段补 JSON 标签
- `MemberName()` 返回值改为 `*string`（对齐 Python `Optional[str]`）

```go
func (b *TeamAgentBlueprint) MemberName() *string {
    if b.Ctx.MemberName == "" {
        return nil
    }
    return &b.Ctx.MemberName
}
```

- [ ] **Step 2: 编写 blueprint_test.go**

- `TestTeamAgentBlueprint_Role` — 验证 Role() 返回值
- `TestTeamAgentBlueprint_MemberName_有值` — 非空返回 *string
- `TestTeamAgentBlueprint_MemberName_空` — 空返回 nil
- `TestTeamAgentBlueprint_Lifecycle` — 验证返回值
- `TestTeamAgentBlueprint_TeamSpec` — 验证返回值

- [ ] **Step 3: 运行测试**

```bash
cd /home/opensource/uapclaw-gateway && go test ./internal/agent_teams/agent/ -v -run "TestTeamAgentBlueprint"
```

- [ ] **Step 4: Commit**

```bash
git add internal/agent_teams/agent/blueprint.go internal/agent_teams/agent/blueprint_test.go
git commit -m "feat(agent_teams/agent): blueprint.go 补 JSON 标签 + MemberName→*string (9.56)"
```

---

### Task 13: doc.go 文件目录更新

**Files:**
- Modify: `internal/agent_teams/doc.go`
- Modify: `internal/agent_teams/agent/doc.go`
- Modify: `internal/agent_teams/schema/doc.go`

- [ ] **Step 1: 更新 agent_teams/doc.go**

文件目录中新增：
- `i18n.go` — 双语字典
- `models/` — 模型池/分配器 (9.64 前置)
- `memory/` — 团队记忆配置 (9.64 前置)
- `messager/` — 消息传输配置 (9.65 前置)
- `team_workspace/` — 团队工作空间 (9.66 前置)
- `tools/` — 团队工具 (9.65 前置)

- [ ] **Step 2: 更新 agent/doc.go**

文件目录中标注 blueprint.go 为 9.56。

- [ ] **Step 3: 更新 schema/doc.go**

文件目录中标注 blueprint.go 为 9.56，新增 deep_agent_spec.go（如果从 blueprint.go 拆出）或标注 bluepring.go 包含 DeepAgentSpec。

- [ ] **Step 4: Commit**

```bash
git add internal/agent_teams/doc.go internal/agent_teams/agent/doc.go internal/agent_teams/schema/doc.go
git commit -m "docs: 更新 agent_teams 包 doc.go 文件目录 (9.56)"
```

---

### Task 14: 全量编译验证 + IMPLEMENTATION_PLAN.md 状态同步

**Files:**
- Modify: `IMPLEMENTATION_PLAN.md`

- [ ] **Step 1: 全量编译**

```bash
cd /home/opensource/uapclaw-gateway && go build ./...
```

确保无编译错误。如有 import 循环或类型不匹配，修复后重试。

- [ ] **Step 2: 全量测试**

```bash
cd /home/opensource/uapclaw-gateway && go test ./internal/agent_teams/... ./internal/agentcore/harness/tools/worktree/... -v -count=1
```

确保所有新增测试通过。

- [ ] **Step 3: 更新 IMPLEMENTATION_PLAN.md**

将 9.56 状态从 `☐` 改为 `✅`。
将 9.55 中 i18n 相关部分标记已完成（如果 IMPLEMENTATION_PLAN.md 中有细分）。
对前置实现的 Schema 部分（9.64/9.65/9.66），在对应行添加注释说明 Schema 部分已在 9.56 前置完成。

- [ ] **Step 4: Commit**

```bash
git add IMPLEMENTATION_PLAN.md
git commit -m "docs: 更新 IMPLEMENTATION_PLAN.md 9.56 状态为 ✅"
```

---

### Task 15: 最终验证 — 覆盖率 + Python 对齐复核

- [ ] **Step 1: 覆盖率检查**

```bash
cd /home/opensource/uapclaw-gateway && go test -cover ./internal/agent_teams/... ./internal/agentcore/harness/tools/worktree/...
```

目标：新增包覆盖率 ≥ 85%。

- [ ] **Step 2: Python 对齐复核清单**

逐项确认 Python 方法在 Go 中有对应实现：

| # | Python 方法/类型 | Go 方法/类型 | 状态 |
|---|---|---|---|
| 1 | TeamAgentBlueprint.role | Role() | ✅ |
| 2 | TeamAgentBlueprint.member_name | MemberName() *string | ✅ |
| 3 | TeamAgentBlueprint.lifecycle | Lifecycle() | ✅ |
| 4 | TeamAgentBlueprint.team_spec | TeamSpec() | ✅ |
| 5 | TransportSpec.build() | Build() | ✅ |
| 6 | StorageSpec.build() | Build() | ✅ |
| 7 | register_transport() | RegisterTransport() | ✅ |
| 8 | register_storage() | RegisterStorage() | ✅ |
| 9 | _ensure_builtin_infra_registered() | ensureBuiltinInfraRegistered() | ✅ |
| 10 | LeaderSpec 默认值 | NewLeaderSpec() | ✅ |
| 11 | TeamAgentSpec._validate_pool_router_exclusive | validatePoolRouterExclusive() | ✅ |
| 12 | TeamAgentSpec._default_transport_for_spawn_mode | defaultTransportForSpawnMode() | ✅ |
| 13 | TeamAgentSpec._validate_reserved_names | validateReservedNames() | ✅ |
| 14 | TeamAgentSpec._validate_hitt_consistency | validateHittConsistency() | ✅ |
| 15 | TeamAgentSpec._validate_leader_model_resolved | validateLeaderModelResolved() | ✅ |
| 16 | TeamAgentSpec.resolve_db_config | ResolveDBConfig() | ✅ |
| 17 | TeamAgentSpec.build | Build() (留桩) | ✅ |
| 18 | ModelPoolEntry.to_team_model_config | ToTeamModelConfig() | ✅ |
| 19 | ModelRouterConfig._validate_model_names | Validate() | ✅ |
| 20 | ModelRouterConfig.to_pool_entries | ToPoolEntries() | ✅ |
| 21 | inherit_pool_ids | InheritPoolIDs() | ✅ |
| 22 | Allocation.to_team_model_config | ToTeamModelConfig() | ✅ |
| 23 | Allocation.to_db_ref | ToDBRef() | ✅ |
| 24 | ModelAllocator Protocol | ModelAllocator interface | ✅ |
| 25 | build_model_allocator | BuildModelAllocator() (留桩) | ✅ |
| 26 | resolve_member_model | ResolveMemberModel() (留桩) | ✅ |
| 27 | TeamModelConfig.build | Build() (留桩) | ✅ |
| 28 | i18n.t() | T() | ✅ |
| 29 | i18n.set_language() | SetLanguage() | ✅ |
| 30 | i18n.get_language() | GetLanguage() | ✅ |
| 31 | DeepAgentSpec 完整字段 | 全部字段+JSON标签 | ✅ |
| 32 | TeamAgentSpec 完整字段 | 全部字段+JSON标签 | ✅ |

- [ ] **Step 3: 最终 Commit**

如有遗漏修复：
```bash
git add -A && git commit -m "fix: 9.56 Blueprint 对齐复核修复"
```

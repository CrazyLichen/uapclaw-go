# 9.7 循环依赖与接口设计修复 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 将 DeepAgentProvider 从 task_loop 提取到 harness/interfaces/ 改名 DeepAgentInterface，消除循环依赖，对齐 Python 访问路径，修复常量重复和 ToolOption nil 问题。

**Architecture:** 新建 harness/interfaces/ 子包定义 DeepAgentInterface + LoopCoordinatorInterface；修改 ControllerInterface 补 TaskScheduler/TaskManager；常量集中到 harness/schema/task_type.go；NewToolCallOptions 加 nil 防御。

**Tech Stack:** Go 1.x, 标准库

---

### Task 1: 新建 harness/interfaces/ 包

**Files:**
- Create: `internal/agentcore/harness/interfaces/doc.go`
- Create: `internal/agentcore/harness/interfaces/deep_agent.go`
- Create: `internal/agentcore/harness/interfaces/deep_agent_test.go`

- [ ] **Step 1: 创建 doc.go**

```go
// Package interfaces 提供 harness 层核心接口定义，解耦子包间的编译依赖。
//
// Python 中 DeepAgent 定义在 harness/deep_agent.py，task_loop 和 subagent
// 通过 TYPE_CHECKING 延迟导入。Go 用此接口包达到等价效果：
// 消费者依赖接口定义而非具体实现，避免循环依赖。
//
// 文件目录：
//
//	interfaces/
//	├── doc.go           # 包文档
//	└── deep_agent.go    # DeepAgentInterface + LoopCoordinatorInterface
//
// 对应 Python 代码：openjiuwen/harness/deep_agent.py
package interfaces
```

- [ ] **Step 2: 创建 deep_agent.go**

```go
package interfaces

import (
	"github.com/uapclaw/uapclaw-go/internal/agentcore/controller"
	"github.com/uapclaw/uapclaw-go/internal/agentcore/controller/modules"
	hschema "github.com/uapclaw/uapclaw-go/internal/agentcore/harness/schema"
	sessioninterfaces "github.com/uapclaw/uapclaw-go/internal/agentcore/session/interfaces"
	"github.com/uapclaw/uapclaw-go/internal/agentcore/single_agent/agents"
)

// ──────────────────────────── 接口 ────────────────────────────

// DeepAgentInterface DeepAgent 运行时能力接口。
// 提供 Executor/Handler/Tool 所需的运行时访问。
// 对齐 Python: DeepAgent 的公开方法集（TYPE_CHECKING 引用）。
// ⤵️ 9.1 回填：9.1 实现 DeepAgent 后，由 *DeepAgent 直接实现此接口。
type DeepAgentInterface interface {
	// ReactAgent 返回内层 ReActAgent 实例
	ReactAgent() *agents.ReActAgent
	// LoopCoordinator 返回循环协调器（可能为 nil）
	LoopCoordinator() LoopCoordinatorInterface
	// LoopController 返回任务循环控制器（对齐 Python: DeepAgent.loop_controller）
	LoopController() controller.ControllerInterface
	// EventHandler 返回事件处理器
	EventHandler() modules.EventHandler
	// LoadState 从会话加载 DeepAgentState
	LoadState(sess sessioninterfaces.SessionFacade) *hschema.DeepAgentState
	// DeepConfig 返回 DeepAgent 配置
	DeepConfig() *hschema.DeepAgentConfig
	// IsInvokeActive 判断是否有活跃的 invoke
	IsInvokeActive() bool
	// IsAutoInvokeScheduled 判断是否已调度自动 invoke
	IsAutoInvokeScheduled() bool
	// SetAutoInvokeScheduled 设置自动 invoke 调度标记
	SetAutoInvokeScheduled(scheduled bool)
	// ScheduleAutoInvokeOnSpawnDone 延迟调度自动 invoke
	// ⤵️ 9.1 回填：实现 SessionSpawn 完成后的自动 invoke 调度
	ScheduleAutoInvokeOnSpawnDone(steerText string) error
	// CreateSubagent 创建子 Agent 实例。
	// ⤵️ 9.1 回填：9.1 实现 DeepAgent 后，由 *DeepAgent.CreateSubagent 实现。
	// 对齐 Python: DeepAgent.create_subagent
	CreateSubagent(subagentType string, subSessionID string) (DeepAgentInterface, error)
}

// LoopCoordinatorInterface 循环协调器接口（最小集）。
// 仅包含 DeepAgentInterface 消费者实际调用的方法。
// 对齐 Python: LoopCoordinator 的消费者方法子集。
type LoopCoordinatorInterface interface {
	// Iteration 返回当前迭代次数
	Iteration() int
	// RequestAbort 请求中止循环
	RequestAbort()
}
```

- [ ] **Step 3: 创建 deep_agent_test.go**

```go
package interfaces

import "testing"

// TestDeepAgentInterface_CompileCheck 确保 DeepAgentInterface 接口定义可编译
func TestDeepAgentInterface_CompileCheck(t *testing.T) {
	// 此测试仅验证接口定义编译通过
	// 编译时接口检查在实现侧（task_loop、harness）执行
	var _ DeepAgentInterface = (DeepAgentInterface)(nil)
}

// TestLoopCoordinatorInterface_CompileCheck 确保 LoopCoordinatorInterface 接口定义可编译
func TestLoopCoordinatorInterface_CompileCheck(t *testing.T) {
	var _ LoopCoordinatorInterface = (LoopCoordinatorInterface)(nil)
}
```

- [ ] **Step 4: 编译验证**

Run: `go build ./internal/agentcore/harness/interfaces/...`
Expected: PASS

- [ ] **Step 5: Commit**

```bash
git add internal/agentcore/harness/interfaces/
git commit -m "feat(interfaces): 新建 harness/interfaces 包 + DeepAgentInterface + LoopCoordinatorInterface"
```

---

### Task 2: 修改 ControllerInterface 补 TaskScheduler + TaskManager

**Files:**
- Modify: `internal/agentcore/controller/interface.go:47-49`
- Modify: `internal/agentcore/controller/interface_test.go`（如有则补充测试）

- [ ] **Step 1: 修改 ControllerInterface 接口**

在 `ControllerInterface` 末尾增加两个方法：

```go
// ControllerInterface 控制器接口，定义事件驱动任务编排的核心能力。
// Controller 和 TaskLoopController 均实现此接口。
// 对齐 Python: openjiuwen/core/controller/base.py::Controller 的公开方法
type ControllerInterface interface {
	// ... 保留现有方法不变 ...
	// EventHandler 获取事件处理器
	EventHandler() modules.EventHandler
	// TaskScheduler 获取任务调度器
	// 对齐 Python: Controller.task_scheduler
	TaskScheduler() *modules.TaskScheduler
	// TaskManager 获取任务管理器
	// 对齐 Python: Controller.task_manager
	TaskManager() *modules.TaskManager
}
```

- [ ] **Step 2: 编译验证**

Run: `go build ./internal/agentcore/controller/...`
Expected: PASS（Controller 和 TaskLoopController 已有这两个方法实现）

- [ ] **Step 3: Commit**

```bash
git add internal/agentcore/controller/interface.go
git commit -m "feat(controller): ControllerInterface 补 TaskScheduler + TaskManager 方法"
```

---

### Task 3: 常量移到 harness/schema/task_type.go

**Files:**
- Create: `internal/agentcore/harness/schema/task_type.go`
- Modify: `internal/agentcore/harness/schema/doc.go`
- Modify: `internal/agentcore/harness/task_loop/executor.go`（删常量 + 改 import）
- Modify: `internal/agentcore/harness/tools/subagent/session_tools.go`（删常量 + 改 import）

- [ ] **Step 1: 创建 task_type.go**

```go
package schema

// ──────────────────────────── 常量 ────────────────────────────

const (
	// DeepTaskType 深层 Agent 任务类型
	DeepTaskType = "deep_agent_task"
	// SessionSpawnTaskType 会话派生任务类型
	SessionSpawnTaskType = "session_spawn_task"
)
```

- [ ] **Step 2: 修改 harness/schema/doc.go**

在文件目录中添加 `task_type.go` 条目。

- [ ] **Step 3: 修改 task_loop/executor.go**

- 删除 `DeepTaskType` 和 `SessionSpawnTaskType` 常量定义（L54-61）
- import 已有 `hschema`，无需新增
- 将代码中 `DeepTaskType` → `hschema.DeepTaskType`，`SessionSpawnTaskType` → `hschema.SessionSpawnTaskType`

- [ ] **Step 4: 修改 subagent/session_tools.go**

- 删除 `SessionSpawnTaskType` 常量定义（L26）
- 新增 `hschema` import：`hschema "github.com/uapclaw/uapclaw-go/internal/agentcore/harness/schema"`
- 将代码中 `SessionSpawnTaskType` → `hschema.SessionSpawnTaskType`

- [ ] **Step 5: 编译验证**

Run: `go build ./internal/agentcore/harness/schema/... ./internal/agentcore/harness/task_loop/... ./internal/agentcore/harness/tools/subagent/...`
Expected: PASS（暂不删除 DeepAgentProvider，后续 Task 统一改）

- [ ] **Step 6: Commit**

```bash
git add internal/agentcore/harness/schema/task_type.go internal/agentcore/harness/schema/doc.go internal/agentcore/harness/task_loop/executor.go internal/agentcore/harness/tools/subagent/session_tools.go
git commit -m "refactor(schema): DeepTaskType + SessionSpawnTaskType 移到 harness/schema/task_type.go"
```

---

### Task 4: NewToolCallOptions nil 防御

**Files:**
- Modify: `internal/agentcore/foundation/tool/base.go:132-140`

- [ ] **Step 1: 修改 NewToolCallOptions**

```go
// NewToolCallOptions 从选项列表构造 ToolCallOptions。
// 默认值：RaiseForStatus=true（与 Python raise_for_status=True 对齐）。
// 忽略 nil 选项，防止 nil(o) panic。
func NewToolCallOptions(opts ...ToolOption) *ToolCallOptions {
	o := &ToolCallOptions{
		RaiseForStatus: true,
	}
	for _, opt := range opts {
		if opt != nil {
			opt(o)
		}
	}
	return o
}
```

- [ ] **Step 2: 运行 foundation/tool 测试**

Run: `go test ./internal/agentcore/foundation/tool/... -count=1`
Expected: PASS

- [ ] **Step 3: Commit**

```bash
git add internal/agentcore/foundation/tool/base.go
git commit -m "fix(tool): NewToolCallOptions 加 nil 防御，防止 nil(o) panic"
```

---

### Task 5: task_loop 包 DeepAgentProvider → DeepAgentInterface

**Files:**
- Modify: `internal/agentcore/harness/task_loop/executor.go`
- Modify: `internal/agentcore/harness/task_loop/handler.go`
- Modify: `internal/agentcore/harness/task_loop/session_spawn_executor.go`
- Modify: `internal/agentcore/harness/task_loop/doc.go`
- Modify: `internal/agentcore/harness/task_loop/executor_test.go`
- Modify: `internal/agentcore/harness/task_loop/handler_test.go`
- Modify: `internal/agentcore/harness/task_loop/session_spawn_executor_test.go`

- [ ] **Step 1: 修改 executor.go**

- 删除 `DeepAgentProvider` 接口定义（L23-50）
- 新增 import：`"github.com/uapclaw/uapclaw-go/internal/agentcore/harness/interfaces"`
- 所有 `DeepAgentProvider` → `interfaces.DeepAgentInterface`
- `LoopCoordinator()` 返回值从 `*LoopCoordinator` 变为 `interfaces.LoopCoordinatorInterface`
- `BuildDeepExecutor(provider DeepAgentProvider)` → `BuildDeepExecutor(provider interfaces.DeepAgentInterface)`
- `NewTaskLoopEventExecutor(deps, provider DeepAgentProvider)` → `NewTaskLoopEventExecutor(deps, provider interfaces.DeepAgentInterface)`
- `CreateSubagent` 返回值 `(DeepAgentProvider, error)` → `(interfaces.DeepAgentInterface, error)`
- executor.go 中 `coordinator.Iteration()` 和 `coordinator.RequestAbort()` 无需修改（LoopCoordinatorInterface 包含这两个方法）

- [ ] **Step 2: 修改 handler.go**

- 新增 import：`"github.com/uapclaw/uapclaw-go/internal/agentcore/harness/interfaces"`
- `provider DeepAgentProvider` → `provider interfaces.DeepAgentInterface`
- `NewTaskLoopEventHandler(provider DeepAgentProvider)` → `NewTaskLoopEventHandler(provider interfaces.DeepAgentInterface)`
- handler.go 中 `coordinator` 只做 nil 检查，无需修改方法调用
- `completeSessionSpawn` 中的 `h.provider.IsInvokeActive()` / `IsAutoInvokeScheduled()` / `SetAutoInvokeScheduled()` / `ScheduleAutoInvokeOnSpawnDone()` 无需修改（DeepAgentInterface 包含这些方法）

- [ ] **Step 3: 修改 session_spawn_executor.go**

- 新增 import：`"github.com/uapclaw/uapclaw-go/internal/agentcore/harness/interfaces"`
- `provider DeepAgentProvider` → `provider interfaces.DeepAgentInterface`
- `NewSessionSpawnExecutor(deps, provider DeepAgentProvider)` → `NewSessionSpawnExecutor(deps, provider interfaces.DeepAgentInterface)`
- `BuildSessionSpawnExecutor(provider DeepAgentProvider)` → `BuildSessionSpawnExecutor(provider interfaces.DeepAgentInterface)`
- `CreateSubagent` 返回值从 `DeepAgentProvider` → `interfaces.DeepAgentInterface`

- [ ] **Step 4: 修改 doc.go**

更新文件目录描述，注明 DeepAgentProvider 已移到 harness/interfaces。

- [ ] **Step 5: 修改 executor_test.go**

- `fakeDeepAgentProvider` 所有 `DeepAgentProvider` → `interfaces.DeepAgentInterface`
- 新增 `LoopController()` 方法返回 `controller.ControllerInterface`（nil 即可）
- `CreateSubagent` 返回值改为 `(interfaces.DeepAgentInterface, error)`
- `LoopCoordinator()` 返回值改为 `interfaces.LoopCoordinatorInterface`（返回 `&fakeLoopCoordinator{}`）
- 新增 `fakeLoopCoordinator` 实现 `interfaces.LoopCoordinatorInterface`

```go
type fakeLoopCoordinator struct {
 iteration int
}

func (f *fakeLoopCoordinator) Iteration() int { return f.iteration }
func (f *fakeLoopCoordinator) RequestAbort()  {}
```

- [ ] **Step 6: 修改 handler_test.go**

- 与 executor_test.go 相同的适配修改
- `fakeDeepAgentProvider` 补 `LoopController()` 方法

- [ ] **Step 7: 修改 session_spawn_executor_test.go**

- 同上适配

- [ ] **Step 8: 编译 + 测试验证**

Run: `go build ./internal/agentcore/harness/task_loop/... && go test ./internal/agentcore/harness/task_loop/... -count=1`
Expected: PASS

- [ ] **Step 9: Commit**

```bash
git add internal/agentcore/harness/task_loop/
git commit -m "refactor(task_loop): DeepAgentProvider → DeepAgentInterface，移到 harness/interfaces"
```

---

### Task 6: subagent 包 SessionToolProvider → DeepAgentInterface + 对齐 Python 路径 + 批量 opts

**Files:**
- Modify: `internal/agentcore/harness/tools/subagent/session_tools.go`
- Modify: `internal/agentcore/harness/tools/subagent/session_tools_test.go`
- Modify: `internal/agentcore/harness/tools/subagent/doc.go`

- [ ] **Step 1: 修改 session_tools.go**

主要修改：

1. **删除 SessionToolProvider 接口**（L32-42）

2. **修改 import**：
   - 新增：`"github.com/uapclaw/uapclaw-go/internal/agentcore/harness/interfaces"`
   - 删除不再需要的 `modules` import（如果只有 SessionToolProvider 用到的话；但 SessionsCancelTool 仍可能需要检查，需确认）

3. **所有 `SessionToolProvider` → `interfaces.DeepAgentInterface`**：
   - `SessionsSpawnTool.provider` 字段类型
   - `SessionsCancelTool.provider` 字段类型
   - `NewSessionsSpawnTool(provider SessionToolProvider, ...)` → `NewSessionsSpawnTool(provider interfaces.DeepAgentInterface, ...)`
   - `NewSessionsCancelTool(provider SessionToolProvider, ...)` → `NewSessionsCancelTool(provider interfaces.DeepAgentInterface, ...)`
   - `BuildSessionTools(provider SessionToolProvider, ...)` → `BuildSessionTools(provider interfaces.DeepAgentInterface, ...)`

4. **SessionsSpawnTool.Invoke 对齐 Python 提交路径**：
```go
// 修改前
handler := t.provider.EventHandler()
if handler == nil { ... }
tm := handler.GetBase().TaskManager
if tm == nil { ... }

// 修改后（对齐 Python: loop_controller.task_manager）
loopCtrl := t.provider.LoopController()
if loopCtrl == nil {
    return nil, exception.BuildError(...)
}
tm := loopCtrl.TaskManager()
if tm == nil {
    return nil, exception.BuildError(...)
}
```

5. **SessionsSpawnTool.Invoke 批量处理 opts**：
```go
// 修改前
var session any
for _, opt := range opts {
    if opt != nil {
        o := tool.NewToolCallOptions(opt)
        if o.Session != nil {
            session = o.Session
        }
    }
}

// 修改后
callOpts := tool.NewToolCallOptions(opts...)
session := callOpts.Session
```

6. **SessionsCancelTool.Invoke 对齐 Python 取消路径**：
```go
// 修改前
handler := t.provider.EventHandler()
if handler == nil { ... }
scheduler := handler.GetBase().TaskScheduler
if scheduler == nil { ... }

// 修改后（对齐 Python: loop_controller.task_scheduler）
loopCtrl := t.provider.LoopController()
if loopCtrl == nil {
    return nil, exception.BuildError(...)
}
scheduler := loopCtrl.TaskScheduler()
if scheduler == nil {
    return nil, exception.BuildError(...)
}
```

- [ ] **Step 2: 修改 session_tools_test.go**

1. **删除 `fakeProvider`**（实现了 SessionToolProvider）
2. **新增 `fakeDeepAgentProvider`**（实现 `interfaces.DeepAgentInterface`）：
   - 包含所有 DeepAgentInterface 方法
   - `LoopController()` 返回 `controller.ControllerInterface`（可为 nil 或 fake 实现）
   - `LoopCoordinator()` 返回 `interfaces.LoopCoordinatorInterface`
3. 更新所有测试用 `fakeProvider` → `fakeDeepAgentProvider`
4. 补充 `LoopController()` 返回带 `TaskManager()` + `TaskScheduler()` 的 fake 实现（用于 SessionsSpawnTool/SessionsCancelTool 成功路径测试）

- [ ] **Step 3: 修改 doc.go**

更新包描述，注明 SessionToolProvider 已删除，改用 DeepAgentInterface。

- [ ] **Step 4: 编译 + 测试验证**

Run: `go build ./internal/agentcore/harness/tools/subagent/... && go test ./internal/agentcore/harness/tools/subagent/... -count=1`
Expected: PASS

- [ ] **Step 5: Commit**

```bash
git add internal/agentcore/harness/tools/subagent/
git commit -m "refactor(subagent): SessionToolProvider → DeepAgentInterface + 对齐 Python 路径 + 批量 opts"
```

---

### Task 7: 全量编译 + 测试 + 覆盖率 + IMPLEMENTATION_PLAN 更新

**Files:**
- Modify: `IMPLEMENTATION_PLAN.md`

- [ ] **Step 1: 全量编译**

Run: `go build ./...`
Expected: PASS

- [ ] **Step 2: 全量测试**

Run: `go test ./internal/agentcore/harness/... ./internal/agentcore/controller/... ./internal/agentcore/foundation/tool/... -count=1`
Expected: PASS

- [ ] **Step 3: 覆盖率检查**

Run:
```bash
go test -cover ./internal/agentcore/harness/task_loop/...
go test -cover ./internal/agentcore/harness/tools/subagent/...
```
Expected: task_loop ≥ 83%, subagent ≥ 80%

- [ ] **Step 4: 更新 IMPLEMENTATION_PLAN.md**

更新 9.7 描述，注明循环依赖修复完成：
- DeepAgentProvider → DeepAgentInterface（harness/interfaces/）
- SessionToolProvider 删除
- ControllerInterface 补 TaskScheduler + TaskManager
- 常量集中到 harness/schema/task_type.go
- NewToolCallOptions nil 防御

- [ ] **Step 5: Commit**

```bash
git add IMPLEMENTATION_PLAN.md
git commit -m "docs: 更新 IMPLEMENTATION_PLAN.md 9.7 循环依赖修复完成"
```

# DeepAgent 9.1 回填修复 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 修复 DeepAgent 9.1 中所有已确认的回填项，严格对齐 Python 逻辑

**Architecture:** 逐项修复 7 个问题，每个问题独立可测试。所有修改严格遵循 Python 源码逻辑，不做创造性发挥。

**Tech Stack:** Go 1.22+, 标准库 os.Getenv, 已有的 ResourceMgr/ContextEngine/ContextUtils

---

## File Structure

| 文件 | 操作 | 职责 |
|------|------|------|
| `internal/agentcore/harness/deep_agent.go` | 修改 | ContextEngine 3方法、ensureInitialized注入、setupTaskLoop、filterDisabledTools、unregisterToolResource、ensureBuiltinToolResource、registerPendingMCPs、ScheduleAutoInvokeOnSpawnDone标记 |
| `internal/agentcore/harness/rails/base.go` | 修改 | `*sys_operation.SysOperation` → `sys_operation.SysOperation`，SetSysOperation签名 |
| `internal/agentcore/harness/rails/base_test.go` | 修改 | 适配 SysOperation 接口值变更 |
| `internal/agentcore/harness/factory.go` | 修改 | isDisabledFreeSearchTool 实现、envFlag/IsFreeSearchEnabled/IsPaidSearchEnabled 函数 |
| `internal/agentcore/harness/factory_test.go` | 修改 | 更新测试 |
| `internal/agentcore/harness/deep_agent_test.go` | 修改 | 更新 ContextEngine 相关测试 |

---

### Task 1: DeepAgentRail SysOperation 从指针改为接口值

**Files:**
- Modify: `internal/agentcore/harness/rails/base.go`
- Modify: `internal/agentcore/harness/rails/base_test.go`

Python 逻辑：Python 中 `DeepAgentRail` 存储 `sys_operation` 为对象引用。Go 中接口本身就是引用语义，`*sys_operation.SysOperation` 是反模式。

- [ ] **Step 1: 修改 base.go 中 DeepAgentRail 结构体和 SetSysOperation/SysOperation 签名**

将 `sysOperation *sys_operation.SysOperation` 改为 `sysOperation sys_operation.SysOperation`，同步修改 SetSysOperation 参数和 SysOperation 返回值：

```go
// DeepAgentRail 结构体字段
sysOperation sys_operation.SysOperation

// SetSysOperation 设置系统操作引用。
func (d *DeepAgentRail) SetSysOperation(op sys_operation.SysOperation) {
	d.sysOperation = op
}

// SysOperation 返回系统操作引用。
func (d *DeepAgentRail) SysOperation() sys_operation.SysOperation {
	return d.sysOperation
}
```

- [ ] **Step 2: 更新 base_test.go 中所有 `*sys_operation.SysOperation` 引用**

测试中所有创建/断言 SysOperation 的地方，去掉指针。

- [ ] **Step 3: 运行 rails 包测试**

Run: `cd /home/opensource/uap-claw-go && go test ./internal/agentcore/harness/rails/... -count=1 -timeout 120s`
Expected: PASS

---

### Task 2: ensureInitialized 中 DeepAgentRail 注入 set_sys_operation / set_workspace

**Files:**
- Modify: `internal/agentcore/harness/deep_agent.go`

Python 逻辑 (deep_agent.py L838-841):
```python
for rail_inst in self._pending_rails:
    if isinstance(rail_inst, TaskCompletionRail):
        self._task_completion_rail = rail_inst
    if isinstance(rail_inst, DeepAgentRail):
        rail_inst.set_sys_operation(self._deep_config.sys_operation)
        rail_inst.set_workspace(self._deep_config.workspace)
    rail_inst.init(self)
    await self._register_rail_selective(rail_inst)
```

Go 等价：使用接口类型断言替代 `isinstance`。定义非导出接口 `deepAgentRailProvider`，包含 `SetSysOperation` + `SetWorkspace` 方法。`DeepAgentRail` 已实现这两个方法，嵌入 `DeepAgentRail` 的子类自动满足。

- [ ] **Step 1: 在 deep_agent.go 的非导出函数区域添加 deepAgentRailProvider 接口定义**

```go
// deepAgentRailProvider DeepAgentRail 提供者接口。
// 对齐 Python: isinstance(rail_inst, DeepAgentRail) 类型检查。
// 嵌入 DeepAgentRail 的子类自动满足此接口。
type deepAgentRailProvider interface {
	SetSysOperation(op sysop.SysOperation)
	SetWorkspace(w *workspace.Workspace)
}
```

- [ ] **Step 2: 修改 ensureInitialized 中 pendingToRegister 循环，添加 DeepAgentRail 注入逻辑**

在 `for _, r := range pendingToRegister` 循环中，在 `r.Init(d)` 之前添加：

```go
// 对齐 Python: isinstance(rail_inst, DeepAgentRail) → set_sys_operation / set_workspace
if provider, ok := r.(deepAgentRailProvider); ok {
	d.configMu.RLock()
	cfg := d.deepConfig
	d.configMu.RUnlock()
	if cfg != nil {
		provider.SetSysOperation(cfg.SysOperation)
		provider.SetWorkspace(cfg.Workspace)
	}
}
```

- [ ] **Step 3: 移除原有的 `⤵️ 9.1 回填：DeepAgentRail.set_sys_operation / set_workspace` 注释**

- [ ] **Step 4: 运行 harness 包测试**

Run: `cd /home/opensource/uap-claw-go && go test ./internal/agentcore/harness/... -count=1 -timeout 120s -run TestDeepAgent`
Expected: PASS

---

### Task 3: setupTaskLoop 中创建真实 ContextEngine 实例

**Files:**
- Modify: `internal/agentcore/harness/deep_agent.go`

Python 逻辑 (deep_agent.py L1717):
```python
context_engine = ContextEngine()
```
Python `ContextEngine()` 无参构造 = `ContextEngine(config=ContextEngineConfig())`。

Go 等价：`NewContextEngine(NewContextEngineConfig())`

- [ ] **Step 1: 在 deep_agent.go import 中添加 context_engine 和 ceschema 包**

```go
ceschema "github.com/uapclaw/uapclaw-go/internal/agentcore/context_engine/schema"
```

注意：`ceinterface` 已在 import 中。

- [ ] **Step 2: 替换 setupTaskLoop 中 `var ce ceinterface.ContextEngine` 为真实创建**

将：
```go
// 创建 ContextEngine
// ⤵️ 9.1 回填：ContextEngine 实例创建
var ce ceinterface.ContextEngine
```

替换为：
```go
// 创建 ContextEngine
// 对齐 Python: context_engine = ContextEngine() (line 1717)
ce := context_engine.NewContextEngine(ceschema.NewContextEngineConfig())
```

需要在 import 中添加：
```go
"github.com/uapclaw/uapclaw-go/internal/agentcore/context_engine"
```

- [ ] **Step 3: 运行 harness 包测试**

Run: `cd /home/opensource/uap-claw-go && go test ./internal/agentcore/harness/... -count=1 -timeout 120s -run TestDeepAgent`
Expected: PASS

---

### Task 4: ContextEngine 3个方法实现（GetContextUsage, GetCurrentContext, CreateNewContextEngine）

**Files:**
- Modify: `internal/agentcore/harness/deep_agent.go`

Python 逻辑（4个辅助方法 + 3个主方法）：

辅助方法：
1. `_resolve_context_session_id(session_id)` → `resolveContextSessionID`
2. `_get_context_or_error(session_id, context_id)` → `getContextOrError`
3. `_resolve_context_window_tokens()` → `resolveContextWindowTokens`
4. `_normalize_context_messages(messages)` → `normalizeContextMessages`

- [ ] **Step 1: 在 deep_agent.go 非导出函数区域添加 4 个辅助方法**

```go
// resolveContextSessionID 解析上下文 API 使用的会话 ID。
// 对齐 Python: DeepAgent._resolve_context_session_id(session_id) (line 483)
func (d *DeepAgent) resolveContextSessionID(sessionID string) (string, error) {
	if sessionID != "" {
		return sessionID, nil
	}
	d.configMu.RLock()
	boundID := d.boundSessionID
	loopSess := d.loopSession
	d.configMu.RUnlock()
	if boundID != "" {
		return boundID, nil
	}
	if loopSess != nil {
		return loopSess.GetSessionID(), nil
	}
	return "", exception.BuildError(exception.StatusDeepagentContextParamError,
		exception.WithMsg("session_id is required when no context is bound."))
}

// getContextOrError 获取内部 ReAct 上下文，不存在则返回错误。
// 对齐 Python: DeepAgent._get_context_or_error(session_id, context_id) (line 501)
func (d *DeepAgent) getContextOrError(sessionID string, contextID string) (ceinterface.ModelContext, error) {
	d.configMu.RLock()
	reactAgent := d.reactAgent
	d.configMu.RUnlock()

	if reactAgent == nil {
		return nil, exception.BuildError(exception.StatusDeepagentRuntimeError,
			exception.WithMsg("DeepAgent not configured. Call configure() first."))
	}

	resolvedSessionID, err := d.resolveContextSessionID(sessionID)
	if err != nil {
		return nil, err
	}

	context := reactAgent.ContextEngine().GetContext(contextID, resolvedSessionID)
	if context == nil {
		return nil, exception.BuildError(exception.StatusDeepagentContextParamError,
			exception.WithMsg(fmt.Sprintf("cannot find context '%s' in session '%s'", contextID, resolvedSessionID)))
	}
	return context, nil
}

// resolveContextWindowTokens 解析配置的模型上下文窗口大小。
// 对齐 Python: DeepAgent._resolve_context_window_tokens() (line 547)
func (d *DeepAgent) resolveContextWindowTokens() int {
	d.configMu.RLock()
	reactAgent := d.reactAgent
	deepCfg := d.deepConfig
	d.configMu.RUnlock()

	var modelName string
	var ceConfig *ceschema.ContextEngineConfig

	if reactAgent != nil {
		if reactCfg, ok := reactAgent.Config().(*saconfig.ReActAgentConfig); ok && reactCfg != nil {
			ceConfig = &reactCfg.ContextEngineConfig
			modelName = reactCfg.ModelNameVal
		}
	}
	if deepCfg != nil && deepCfg.ContextEngineConfig != nil {
		if deepCfg.ContextEngineConfig.ModelName != "" {
			modelName = deepCfg.ContextEngineConfig.ModelName
		}
		ceConfig = deepCfg.ContextEngineConfig
	}

	var fallbackTokens int
	var modelContextWindowTokens map[string]int
	if ceConfig != nil {
		fallbackTokens = ceConfig.ContextWindowTokens
		modelContextWindowTokens = ceConfig.ModelContextWindowTokens
	}

	return cecontext.ResolveContextMax(modelName, fallbackTokens, modelContextWindowTokens)
}

// normalizeContextMessages 规范化上下文消息输入。
// 对齐 Python: DeepAgent._normalize_context_messages(messages) (line 614)
func normalizeContextMessages(messages []llmschema.BaseMessage) []llmschema.BaseMessage {
	if messages == nil {
		return nil
	}
	return messages
}
```

注意：import 中需添加 `cecontext "github.com/uapclaw/uapclaw-go/internal/agentcore/context_engine/context"`。

Python 的 `_normalize_context_messages` 接受 `Optional[List[Any] | Any]`，支持 str/dict/BaseMessage 混合输入。Go 的 `CreateNewContextEngine` 签名已经限定 `[]llmschema.BaseMessage`，所以 normalize 仅处理 nil 情况。str/dict → BaseMessage 的转换由调用方负责。

- [ ] **Step 2: 实现 GetContextUsage**

```go
// GetContextUsage 获取当前上下文占用统计。
// 对齐 Python: DeepAgent.get_context_usage(session_id, context_id) (line 566)
func (d *DeepAgent) GetContextUsage(ctx context.Context, sessionID string, contextID string) (map[string]any, error) {
	modelCtx, err := d.getContextOrError(sessionID, contextID)
	if err != nil {
		return nil, err
	}
	resolvedSessionID, err := d.resolveContextSessionID(sessionID)
	if err != nil {
		return nil, err
	}
	stats := modelCtx.Statistic()
	contextWindowTokens := d.resolveContextWindowTokens()
	var usageRatio float64
	if contextWindowTokens > 0 {
		usageRatio = float64(stats.TotalTokens) / float64(contextWindowTokens)
	}
	return map[string]any{
		"session_id":             resolvedSessionID,
		"context_id":             modelCtx.ContextID(),
		"total_tokens":           stats.TotalTokens,
		"context_window_tokens":  contextWindowTokens,
		"usage_ratio":            usageRatio,
		"usage_percent":          math.Round(usageRatio*10000) / 100,
		"stats": map[string]any{
			"total_messages":    stats.TotalMessages,
			"total_tokens":      stats.TotalTokens,
			"total_dialogues":   stats.TotalDialogues,
			"system_messages":   stats.SystemMessages,
			"user_messages":     stats.UserMessages,
			"assistant_messages": stats.AssistantMessages,
			"tool_messages":     stats.ToolMessages,
		},
	}, nil
}
```

- [ ] **Step 3: 实现 GetCurrentContext**

```go
// GetCurrentContext 返回当前上下文消息。
// 对齐 Python: DeepAgent.get_current_context(session_id, context_id) (line 604)
func (d *DeepAgent) GetCurrentContext(ctx context.Context, sessionID string, contextID string) ([]llmschema.BaseMessage, error) {
	modelCtx, err := d.getContextOrError(sessionID, contextID)
	if err != nil {
		return nil, err
	}
	// 对齐 Python: context.get_messages() → size=0, withHistory=true
	messages, err := modelCtx.GetMessages(0, true)
	if err != nil {
		return nil, err
	}
	return messages, nil
}
```

- [ ] **Step 4: 实现 CreateNewContextEngine**

```go
// CreateNewContextEngine 创建新的上下文引擎。
// 对齐 Python: DeepAgent.create_new_context_engine(session_id, messages) (line 643)
func (d *DeepAgent) CreateNewContextEngine(ctx context.Context, sessionID string, messages []llmschema.BaseMessage) (string, error) {
	d.configMu.RLock()
	reactAgent := d.reactAgent
	card := d.card
	d.configMu.RUnlock()

	if reactAgent == nil {
		return "", exception.BuildError(exception.StatusDeepagentRuntimeError,
			exception.WithMsg("DeepAgent not configured. Call configure() first."))
	}

	// 对齐 Python: new_session_id = session_id or str(uuid.uuid4())
	newSessionID := sessionID
	if newSessionID == "" {
		newSessionID = uuid.New().String()
	}

	normalizedMessages := normalizeContextMessages(messages)

	// 对齐 Python: await self._react_agent.context_engine.create_context(
	//     session=Session(session_id=new_session_id, card=self.card),
	//     history_messages=normalized_messages,
	// )
	sess := session.NewSession(newSessionID, card)
	_, err := reactAgent.ContextEngine().CreateContext(ctx, "default_context_id", sess,
		ceinterface.WithHistoryMessages(normalizedMessages))
	if err != nil {
		return "", err
	}
	return newSessionID, nil
}
```

需要在 import 中添加 `"github.com/google/uuid"` 和检查 `session.NewSession` 的签名。

- [ ] **Step 5: 移除 3 个方法的 `⤵️ 9.1 回填` 注释**

- [ ] **Step 6: 运行测试**

Run: `cd /home/opensource/uap-claw-go && go test ./internal/agentcore/harness/... -count=1 -timeout 120s -run "TestDeepAgent"`
Expected: PASS

---

### Task 5: is_free_search_enabled / is_paid_search_enabled 实现

**Files:**
- Modify: `internal/agentcore/harness/factory.go`

Python 逻辑 (web_tools.py):

```python
def _env_flag(name: str, default: bool = True) -> bool:
    raw = str(os.getenv(name, "") or "").strip().lower()
    if not raw:
        return default
    return raw in {"1", "true", "yes", "on", "enabled"}

_FREE_SEARCH_DDG_ENABLED_ENV = "FREE_SEARCH_DDG_ENABLED"
_FREE_SEARCH_BING_ENABLED_ENV = "FREE_SEARCH_BING_ENABLED"

def is_free_search_enabled() -> bool:
    return (
        _env_flag(_FREE_SEARCH_DDG_ENABLED_ENV, default=False)
        or _env_flag(_FREE_SEARCH_BING_ENABLED_ENV, default=False)
    )

_PAID_SEARCH_API_KEY_ENVS = ("PERPLEXITY_API_KEY", "BOCHA_API_KEY", "JINA_API_KEY", "SERPER_API_KEY")

def is_paid_search_enabled() -> bool:
    return any(str(os.environ.get(key, "") or "").strip() for key in _PAID_SEARCH_API_KEY_ENVS)
```

- [ ] **Step 1: 在 factory.go 的常量区域添加环境变量名**

```go
const (
	// freeSearchDDGEnabledEnv 免费搜索 DDG 启用环境变量
	// 对齐 Python: _FREE_SEARCH_DDG_ENABLED_ENV
	freeSearchDDGEnabledEnv = "FREE_SEARCH_DDG_ENABLED"
	// freeSearchBingEnabledEnv 免费搜索 Bing 启用环境变量
	// 对齐 Python: _FREE_SEARCH_BING_ENABLED_ENV
	freeSearchBingEnabledEnv = "FREE_SEARCH_BING_ENABLED"
)

var (
	// paidSearchAPIKeyEnvs 付费搜索 API Key 环境变量列表
	// 对齐 Python: _PAID_SEARCH_API_KEY_ENVS
	paidSearchAPIKeyEnvs = []string{
		"PERPLEXITY_API_KEY",
		"BOCHA_API_KEY",
		"JINA_API_KEY",
		"SERPER_API_KEY",
	}
)
```

- [ ] **Step 2: 在 factory.go 非导出函数区域添加 3 个函数**

```go
// envFlag 解析布尔型环境变量值，保留空值时的默认值。
// 对齐 Python: _env_flag(name, default) (web_tools.py line 436)
func envFlag(name string, defaultVal bool) bool {
	raw := strings.TrimSpace(strings.ToLower(os.Getenv(name)))
	if raw == "" {
		return defaultVal
	}
	return raw == "1" || raw == "true" || raw == "yes" || raw == "on" || raw == "enabled"
}

// IsFreeSearchEnabled 检查是否至少启用一个免费搜索后端。
// 对齐 Python: is_free_search_enabled() (web_tools.py line 444)
func IsFreeSearchEnabled() bool {
	return envFlag(freeSearchDDGEnabledEnv, false) || envFlag(freeSearchBingEnabledEnv, false)
}

// IsPaidSearchEnabled 检查是否至少配置一个付费搜索 API Key。
// 对齐 Python: is_paid_search_enabled() (web_tools.py line 452)
func IsPaidSearchEnabled() bool {
	for _, key := range paidSearchAPIKeyEnvs {
		if strings.TrimSpace(os.Getenv(key)) != "" {
			return true
		}
	}
	return false
}
```

需要在 import 中添加 `"os"` 和 `"strings"`。

- [ ] **Step 3: 实现 isDisabledFreeSearchTool**

```go
// isDisabledFreeSearchTool 检查工具是否为被禁用的 free_search 工具。
// 对齐 Python: _is_disabled_free_search_tool(tool) (deep_agent.py line 155)
func isDisabledFreeSearchTool(card *tool.ToolCard) bool {
	if card == nil {
		return false
	}
	if card.GetName() != "free_search" {
		return false
	}
	return !IsFreeSearchEnabled()
}
```

- [ ] **Step 4: 实现 filterDisabledTools**

```go
// filterDisabledTools 过滤掉被禁用的工具。
// 对齐 Python: DeepAgent._filter_disabled_tools(config) (line 162)
func (d *DeepAgent) filterDisabledTools(config *hschema.DeepAgentConfig) {
	if config.Tools == nil {
		return
	}
	// 对齐 Python: disabled_tool_names 集合
	disabledNames := make(map[string]bool)
	if !IsFreeSearchEnabled() {
		disabledNames["free_search"] = true
	}
	if !IsPaidSearchEnabled() {
		disabledNames["paid_search"] = true
	}
	if len(disabledNames) == 0 {
		return
	}
	// 对齐 Python: config.tools = [card for card in config.tools if ...]
	var filtered []*tool.ToolCard
	for _, card := range config.Tools {
		if !disabledNames[card.Name] {
			filtered = append(filtered, card)
		}
	}
	config.Tools = filtered
}
```

- [ ] **Step 5: 运行测试**

Run: `cd /home/opensource/uap-claw-go && go test ./internal/agentcore/harness/... -count=1 -timeout 120s -run "TestIsDisabledFreeSearchTool|TestNormalizeTools"`
Expected: PASS

---

### Task 6: MCP 服务器注册实现 (registerPendingMCPs)

**Files:**
- Modify: `internal/agentcore/harness/deep_agent.go`

Python 逻辑 (deep_agent.py L773-811):
```python
async def _register_pending_mcps(self) -> None:
    cfg = self._deep_config
    if cfg is None or not cfg.mcps:
        return
    for mcp_config in cfg.mcps:
        existing_config = Runner.resource_mgr.get_mcp_server_config(mcp_config.server_id)
        if existing_config is None:
            result = await Runner.resource_mgr.add_mcp_server(mcp_config, tag=self.card.id)
            if result.is_err():
                raise result.msg()
        else:
            if existing_config.model_dump() != mcp_config.model_dump():
                raise build_error(...)
            tag_result = Runner.resource_mgr.add_resource_tag(mcp_config.server_id, self.card.id)
            if tag_result.is_err():
                raise tag_result.msg()
            for tool_id in Runner.resource_mgr.get_mcp_tool_ids(mcp_config.server_id):
                tag_result = Runner.resource_mgr.add_resource_tag(tool_id, self.card.id)
                if tag_result.is_err():
                    raise tag_result.msg()
        self.ability_manager.add(mcp_config)
```

Go 的 ResourceMgr API 差异：
- `AddMcpServer(ctx, config, WithMcpTag(tag))` 返回 `([]*McpToolCard, error)` — Python `is_err()` → Go `err != nil`
- `GetMcpServerConfig(serverID)` 返回 `(*McpServerConfig, error)` — nil 表示不存在
- `AddResourceTag(resourceID, []Tag{tag})` 返回 `([]Tag, error)`
- `GetMcpToolIDs(serverID)` 返回 `[]string`

- [ ] **Step 1: 实现 registerPendingMCPs**

```go
// registerPendingMCPs 注册配置的 MCP 服务器。
// 对齐 Python: DeepAgent._register_pending_mcps() (line 773)
func (d *DeepAgent) registerPendingMCPs(ctx context.Context) {
	d.configMu.RLock()
	cfg := d.deepConfig
	d.configMu.RUnlock()

	if cfg == nil || len(cfg.Mcps) == 0 {
		return
	}

	resourceMgr := runner.GetResourceMgr()

	for _, mcpConfig := range cfg.Mcps {
		existingConfig, getErr := resourceMgr.GetMcpServerConfig(mcpConfig.ServerID)

		if getErr != nil || existingConfig == nil {
			// 对齐 Python: existing_config is None → add_mcp_server
			_, addErr := resourceMgr.AddMcpServer(ctx, mcpConfig, resources_manager.WithMcpTag(resources_manager.Tag(d.card.ID)))
			if addErr != nil {
				logger.Error(logComponent).Err(addErr).Str("server_id", mcpConfig.ServerID).Msg("MCP 服务器注册失败")
				continue
			}
		} else {
			// 对齐 Python: existing_config.model_dump() != mcp_config.model_dump() → error
			if !mcpConfigEqual(existingConfig, mcpConfig) {
				logger.Error(logComponent).
					Str("server_id", mcpConfig.ServerID).
					Msg("MCP 服务器已存在但配置不同，跳过")
				continue
			}

			// 对齐 Python: add_resource_tag(server_id, self.card.id)
			_, tagErr := resourceMgr.AddResourceTag(mcpConfig.ServerID, []resources_manager.Tag{resources_manager.Tag(d.card.ID)})
			if tagErr != nil {
				logger.Warn(logComponent).Err(tagErr).Str("server_id", mcpConfig.ServerID).Msg("MCP 资源标签添加失败")
			}

			// 对齐 Python: for tool_id in get_mcp_tool_ids → add_resource_tag
			for _, toolID := range resourceMgr.GetMcpToolIDs(mcpConfig.ServerID) {
				_, tagErr := resourceMgr.AddResourceTag(toolID, []resources_manager.Tag{resources_manager.Tag(d.card.ID)})
				if tagErr != nil {
					logger.Warn(logComponent).Err(tagErr).Str("tool_id", toolID).Msg("MCP 工具标签添加失败")
				}
			}
		}

		// 对齐 Python: self.ability_manager.add(mcp_config)
		d.abilityManager.Add(mcpConfig)
		logger.Debug(logComponent).Str("server_id", mcpConfig.ServerID).Msg("MCP 配置已注册")
	}
}

// mcpConfigEqual 比较两个 McpServerConfig 是否等价。
// 对齐 Python: existing_config.model_dump() != mcp_config.model_dump()
func mcpConfigEqual(a, b *mcptypes.McpServerConfig) bool {
	aJSON, aErr := json.Marshal(a)
	bJSON, bErr := json.Marshal(b)
	if aErr != nil || bErr != nil {
		return false
	}
	return string(aJSON) == string(bJSON)
}
```

需要在 import 中添加：
```go
"encoding/json"
"github.com/uapclaw/uapclaw-go/internal/agentcore/runner"
"github.com/uapclaw/uapclaw-go/internal/agentcore/runner/resources_manager"
mcptypes "github.com/uapclaw/uapclaw-go/internal/agentcore/foundation/mcp/types"
```

- [ ] **Step 2: 移除原有的 stub 注释和 `⤵️ 9.1 回填` 标记**

- [ ] **Step 3: 运行测试**

Run: `cd /home/opensource/uap-claw-go && go test ./internal/agentcore/harness/... -count=1 -timeout 120s -run "TestDeepAgent"`
Expected: PASS

---

### Task 7: unregisterToolResource 实现

**Files:**
- Modify: `internal/agentcore/harness/deep_agent.go`

Python 逻辑 (deep_agent.py L178-204):
```python
def _unregister_tool_resource(self, card: ToolCard) -> None:
    if card.name not in {"free_search", "paid_search"}:
        return
    if not getattr(card, "id", None):
        return
    if Runner.resource_mgr.get_tool(card.id) is None:
        return
    tags = Runner.resource_mgr.get_resource_tag(card.id) or []
    if self.card.id in tags and len(tags) > 1:
        result = Runner.resource_mgr.remove_resource_tag(card.id, self.card.id, skip_if_tag_not_exists=True)
        if result.is_err():
            logger.warning(...)
        return
    if self.card.id in tags or not tags:
        result = Runner.resource_mgr.remove_tool(card.id)
        if result.is_err():
            logger.warning(...)
```

Go ResourceMgr API 对应：
- `GetTool([]string{card.ID})` → `([]tool.Tool, error)` — 空结果表示不存在
- `GetResourceTag(card.ID)` → `[]Tag` (即 `[]string`)
- `RemoveResourceTag(resourceID, []Tag{tag}, WithTagSkipIfNotExists())` → `([]Tag, error)`
- `RemoveTool([]string{card.ID})` → `([]string, error)`

- [ ] **Step 1: 实现 unregisterToolResource**

```go
// unregisterToolResource 注销工具资源。
// 对齐 Python: DeepAgent._unregister_tool_resource(card) (line 178)
func (d *DeepAgent) unregisterToolResource(card *tool.ToolCard) {
	// 对齐 Python: card.name not in {"free_search", "paid_search"} → return
	if card.Name != "free_search" && card.Name != "paid_search" {
		return
	}
	// 对齐 Python: if not getattr(card, "id", None) → return
	if card.ID == "" {
		return
	}

	resourceMgr := runner.GetResourceMgr()

	// 对齐 Python: if Runner.resource_mgr.get_tool(card.id) is None → return
	tools, getErr := resourceMgr.GetTool([]string{card.ID})
	if getErr != nil || len(tools) == 0 {
		return
	}

	// 对齐 Python: tags = Runner.resource_mgr.get_resource_tag(card.id) or []
	tags := resourceMgr.GetResourceTag(card.ID)

	// 对齐 Python: if self.card.id in tags and len(tags) > 1
	agentTag := resources_manager.Tag(d.card.ID)
	agentInTags := false
	for _, t := range tags {
		if t == agentTag {
			agentInTags = true
			break
		}
	}
	if agentInTags && len(tags) > 1 {
		_, err := resourceMgr.RemoveResourceTag(card.ID, []resources_manager.Tag{agentTag}, resources_manager.WithTagSkipIfNotExists())
		if err != nil {
			logger.Warn(logComponent).Err(err).Str("tool_id", card.ID).Msg("移除工具标签失败")
		}
		return
	}

	// 对齐 Python: if self.card.id in tags or not tags
	if agentInTags || len(tags) == 0 {
		_, err := resourceMgr.RemoveTool([]string{card.ID})
		if err != nil {
			logger.Warn(logComponent).Err(err).Str("tool_id", card.ID).Msg("注销工具失败")
		}
	}
}
```

- [ ] **Step 2: 移除 `⤵️ 9.1 回填` 标记**

- [ ] **Step 3: 运行测试**

Run: `cd /home/opensource/uap-claw-go && go test ./internal/agentcore/harness/... -count=1 -timeout 120s -run "TestDeepAgent"`
Expected: PASS

---

### Task 8: ensureBuiltinToolResource 部分实现（Tag管理部分）

**Files:**
- Modify: `internal/agentcore/harness/deep_agent.go`

Python 逻辑 (deep_agent.py L206-228):
```python
def _ensure_builtin_tool_resource(self, card: ToolCard, config: DeepAgentConfig) -> None:
    if card.name not in {"free_search", "paid_search"}:
        return
    existing_tool = Runner.resource_mgr.get_tool(card.id)
    if existing_tool is not None:
        tag_result = Runner.resource_mgr.add_resource_tag(card.id, self.card.id)
        if tag_result.is_err():
            logger.warning(...)
        return
    # WebFreeSearchTool / WebPaidSearchTool 实例化（Go 未实现，保留标记）
    tool_cls = WebPaidSearchTool if card.name == "paid_search" else WebFreeSearchTool
    tool = tool_cls(language=resolve_language(config.language), card=card)
    result = Runner.resource_mgr.add_tool(tool, tag=self.card.id)
    if result.is_err():
        logger.warning(...)
```

Go 可以实现的部分：
- 工具名检查
- 已存在工具的 Tag 管理
- WebSearchTool 实例化部分 **不可做**（Go 的 WebFreeSearchTool/WebPaidSearchTool 尚未实现）

- [ ] **Step 1: 实现 ensureBuiltinToolResource（Tag管理部分，WebSearchTool 部分保留 ⤵️ 标记）**

```go
// ensureBuiltinToolResource 确保内置工具资源已注册。
// 对齐 Python: DeepAgent._ensure_builtin_tool_resource(card, config) (line 206)
func (d *DeepAgent) ensureBuiltinToolResource(card *tool.ToolCard, config *hschema.DeepAgentConfig) {
	// 对齐 Python: card.name not in {"free_search", "paid_search"} → return
	if card.Name != "free_search" && card.Name != "paid_search" {
		return
	}

	resourceMgr := runner.GetResourceMgr()

	// 对齐 Python: existing_tool = Runner.resource_mgr.get_tool(card.id)
	tools, getErr := resourceMgr.GetTool([]string{card.ID})
	if getErr == nil && len(tools) > 0 {
		// 对齐 Python: add_resource_tag(card.id, self.card.id)
		_, tagErr := resourceMgr.AddResourceTag(card.ID, []resources_manager.Tag{resources_manager.Tag(d.card.ID)})
		if tagErr != nil {
			logger.Warn(logComponent).Err(tagErr).Str("tool_id", card.ID).Msg("标记已存在的搜索工具失败")
		}
		return
	}

	// ⤵️ 9.1 回填：WebFreeSearchTool / WebPaidSearchTool Go 实现后补全
	// 对齐 Python: tool_cls = WebPaidSearchTool if card.name == "paid_search" else WebFreeSearchTool
	// tool = tool_cls(language=resolve_language(config.language), card=card)
	// result = Runner.resource_mgr.add_tool(tool, tag=self.card.id)
	logger.Debug(logComponent).Str("tool_name", card.Name).Msg("WebSearchTool Go 实现尚未完成，跳过工具注册")
}
```

- [ ] **Step 2: 运行测试**

Run: `cd /home/opensource/uap-claw-go && go test ./internal/agentcore/harness/... -count=1 -timeout 120s -run "TestDeepAgent"`
Expected: PASS

---

### Task 9: ScheduleAutoInvokeOnSpawnDone 标记清理 + 全量编译验证

**Files:**
- Modify: `internal/agentcore/harness/deep_agent.go`

- [ ] **Step 1: 移除 ScheduleAutoInvokeOnSpawnDone 的 `⤵️ 9.1 回填` 注释**

将：
```go
// ScheduleAutoInvokeOnSpawnDone 延迟调度自动 invoke。
// ⤵️ 9.1 回填：实现 SessionSpawn 完成后的自动 invoke 调度
```

替换为：
```go
// ScheduleAutoInvokeOnSpawnDone 延迟调度自动 invoke。
// 对齐 Python: DeepAgent.schedule_auto_invoke_on_spawn_done(query, delay) (line 1959)
```

- [ ] **Step 2: 全量编译验证**

Run: `cd /home/opensource/uap-claw-go && go build ./...`
Expected: 编译成功

- [ ] **Step 3: 运行 harness 包全量测试**

Run: `cd /home/opensource/uap-claw-go && go test ./internal/agentcore/harness/... -count=1 -timeout 180s`
Expected: PASS

- [ ] **Step 4: 运行相关依赖包测试（rails, factory, resources_manager）**

Run: `cd /home/opensource/uap-claw-go && go test ./internal/agentcore/harness/rails/... ./internal/agentcore/runner/resources_manager/... -count=1 -timeout 180s`
Expected: PASS

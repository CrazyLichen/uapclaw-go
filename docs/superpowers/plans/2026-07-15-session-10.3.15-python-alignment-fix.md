# 10.3.15-18 会话管理 Python 对齐修复 实现计划

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 修复 session 包与 Python 对齐的两个代码差异，并修正 IMPLEMENTATION_PLAN.md 中过时的回填标记。

**Architecture:** 三处独立修改：① readHistoryFile 错误吞掉对齐 Python ② TruncateHistoryRecords 截断前刷盘 ③ 计划文档标记修正。

**Tech Stack:** Go, testify

---

### Task 1: readHistoryFile JSON 解析失败吞掉错误

**Files:**
- Modify: `internal/swarm/server/session/session_history.go:414-415`
- Test: `internal/swarm/server/session/session_history_test.go`

- [ ] **Step 1: 写失败测试**

在 `session_history_test.go` 末尾添加测试，验证损坏 JSON 返回空列表而非 error：

```go
func TestReadHistoryRecords_损坏JSON返回空列表(t *testing.T) {
	tmpDir := t.TempDir()
	t.Setenv("UAPCLAW_DATA_DIR", tmpDir)
	resetHistoryWorkspaceCache()

	sessionID := "sess-corrupt"
	sessionsDir := GetSessionsDir()
	sessionDir := filepath.Join(sessionsDir, sessionID)
	os.MkdirAll(sessionDir, 0o755)

	// 写入损坏的 JSON
	historyPath := filepath.Join(sessionDir, "history.json")
	os.WriteFile(historyPath, []byte("{invalid json}"), 0o644)

	records, err := ReadHistoryRecords(sessionID)
	assert.NoError(t, err, "损坏 JSON 不应返回 error，应对齐 Python 吞掉错误")
	assert.Empty(t, records, "损坏 JSON 应返回空列表")
}
```

- [ ] **Step 2: 运行测试验证失败**

Run: `cd /home/opensource/uap-claw-go && pgrep -f 'go (build|test)' && pkill -f 'go (build|test)' || true; go test ./internal/swarm/server/session/ -run TestReadHistoryRecords_损坏JSON返回空列表 -v -count=1`
Expected: FAIL — 当前 `readHistoryFile` 在 JSON 解析失败时返回 error

- [ ] **Step 3: 修改 readHistoryFile，对齐 Python 吞掉错误**

修改 `internal/swarm/server/session/session_history.go` 第414-415行：

```go
// 修改前：
	var records []map[string]any
	if err := json.Unmarshal(data, &records); err != nil {
		return nil, err
	}

// 修改后：
	var records []map[string]any
	if err := json.Unmarshal(data, &records); err != nil {
		logger.Warn(logComponent).Err(err).Str("path", fpath).Msg("读取 history.json 失败，已忽略并重建")
		return []map[string]any{}, nil
	}
```

- [ ] **Step 4: 运行测试验证通过**

Run: `cd /home/opensource/uap-claw-go && go test ./internal/swarm/server/session/ -run TestReadHistoryRecords_损坏JSON返回空列表 -v -count=1`
Expected: PASS

- [ ] **Step 5: 运行全包测试确认无回归**

Run: `cd /home/opensource/uap-claw-go && go test ./internal/swarm/server/session/ -v -count=1`
Expected: ALL PASS

- [ ] **Step 6: 提交**

```bash
git add internal/swarm/server/session/session_history.go internal/swarm/server/session/session_history_test.go
git commit -m "fix(session): readHistoryFile JSON 解析失败时吞掉错误返回空列表，对齐 Python _read_history"
```

---

### Task 2: TruncateHistoryRecords 截断前刷盘

**Files:**
- Modify: `internal/swarm/server/session/session_history.go:254`
- Test: `internal/swarm/server/session/session_history_test.go`

- [ ] **Step 1: 写失败测试**

在 `session_history_test.go` 末尾添加测试，验证异步写入后再截断，截断结果不被异步写入覆盖：

```go
func TestTruncateHistoryRecords_异步写入后截断不被覆盖(t *testing.T) {
	tmpDir := t.TempDir()
	t.Setenv("UAPCLAW_DATA_DIR", tmpDir)
	resetHistoryWorkspaceCache()
	ResetHistoryWorker()
	ClearAllSessionMetadataCache()

	sessionID := "sess-trunc-flush"

	// 先写入 2 条记录
	AppendHistoryRecord(sessionID, "r1", "web", "user", "第一条", 1.0, "", nil, nil, "")
	AppendHistoryRecord(sessionID, "r2", "web", "user", "第二条", 2.0, "", nil, nil, "")

	// 截断到索引 1，保留第一条
	result, err := TruncateHistoryRecords(sessionID, 1)
	require.NoError(t, err)
	assert.Equal(t, 1, result.RemainingRecords)
	assert.Equal(t, 1, result.RemovedRecords)

	// 验证截断后只剩 1 条
	records, err := ReadHistoryRecords(sessionID)
	require.NoError(t, err)
	assert.Len(t, records, 1)
	assert.Equal(t, "第一条", records[0]["content"])
}
```

- [ ] **Step 2: 运行测试验证当前是否通过**

Run: `cd /home/opensource/uap-claw-go && go test ./internal/swarm/server/session/ -run TestTruncateHistoryRecords_异步写入后截断不被覆盖 -v -count=1`
Expected: 可能 PASS（时序依赖），但这是竞态场景，需要确保 FlushHistoryQueue 语义正确

- [ ] **Step 3: 修改 TruncateHistoryRecords，截断前先刷盘**

修改 `internal/swarm/server/session/session_history.go` 第254行附近：

```go
// 修改前：
func TruncateHistoryRecords(sessionID string, cutIndex int) (TruncateResult, error) {
	sid := NormalizeSessionID(sessionID)
	fpath := historyFilePath(sid)

	historyFileMu.Lock()
	defer historyFileMu.Unlock()

// 修改后：
func TruncateHistoryRecords(sessionID string, cutIndex int) (TruncateResult, error) {
	// 对齐 Python: _WRITE_QUEUE.join()，先等异步队列刷盘再截断
	FlushHistoryQueue()

	sid := NormalizeSessionID(sessionID)
	fpath := historyFilePath(sid)

	historyFileMu.Lock()
	defer historyFileMu.Unlock()
```

同时更新函数注释，将第252-253行改为：

```go
// 修改前：
// 对齐 Python: truncate_history_records(session_id, cut_index: int) → dict
// 先等异步队列刷盘（Go chan 不支持 join，用持锁截断保证顺序），再截断到 cutIndex。

// 修改后：
// 对齐 Python: truncate_history_records(session_id, cut_index: int) → dict
// 先等异步队列刷盘（FlushHistoryQueue 哨兵机制等价 Python _WRITE_QUEUE.join()），再截断到 cutIndex。
```

- [ ] **Step 4: 运行测试验证通过**

Run: `cd /home/opensource/uap-claw-go && go test ./internal/swarm/server/session/ -run TestTruncateHistoryRecords -v -count=1`
Expected: ALL PASS（包括已有的 TestTruncateHistoryRecords 和 TestTruncateHistoryRecords_边界值）

- [ ] **Step 5: 运行全包测试确认无回归**

Run: `cd /home/opensource/uap-claw-go && go test ./internal/swarm/server/session/ -v -count=1`
Expected: ALL PASS

- [ ] **Step 6: 提交**

```bash
git add internal/swarm/server/session/session_history.go internal/swarm/server/session/session_history_test.go
git commit -m "fix(session): TruncateHistoryRecords 截断前先 FlushHistoryQueue 刷盘，对齐 Python _WRITE_QUEUE.join()"
```

---

### Task 3: 修正 IMPLEMENTATION_PLAN.md 回填标记

**Files:**
- Modify: `IMPLEMENTATION_PLAN.md:677`

- [ ] **Step 1: 修改标记**

将第677行中的 `AutoTitle回填⤵️11.x自动标题` 改为 `AutoTitle已实现`：

```
修改前：AutoTitle回填⤵️11.x自动标题
修改后：AutoTitle已实现
```

- [ ] **Step 2: 提交**

```bash
git add IMPLEMENTATION_PLAN.md
git commit -m "docs: 修正 10.3.15-18 AutoTitle 回填标记为已实现"
```

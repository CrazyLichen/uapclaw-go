# 9.24 P6 修复 + Session Memory 集成 + 10.6.3-10 Rail 注入 实现计划

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 修复 ContextEvolutionRail 6 项差异、回填 ContextProcessorRail 的 session memory 集成（含 SessionMemoryAgentUpdater）、回填 10.6.3-10 TeamSkillEvolutionRail + TeamSkillCreateRail 的 DeepAdapter 注册

**Architecture:** 三个独立子系统，按依赖顺序实现：(A) ContextEvolutionRail 直接修复 → (B) Session Memory 集成 → (C) 10.6.3-10 Rail 注入。每个子系统完成后可独立编译和测试。

**Tech Stack:** Go 1.22+, testify/assert, 项目现有 logger/llm/session/rails 框架

**设计文档:** `docs/superpowers/specs/2028-01-15-9.24-p6-session-memory-rail-injection-design.md`

---

## Part A：ContextEvolutionRail 差异修复（6 项）

### Task 1：TaskIterationInputs 加 RetrievalQuery 字段

**Files:**
- Modify: `internal/agentcore/single_agent/interfaces/callback.go:214-227`
- Test: `internal/agentcore/single_agent/interfaces/callback_test.go`

- [x] **Step 1: 写失败测试**

在 `callback_test.go` 中添加：
```go
func TestTaskIterationInputs_RetrievalQuery(t *testing.T) {
    inputs := &TaskIterationInputs{
        Query:          "用户问题",
        RetrievalQuery: "检索用查询",
    }
    assert.Equal(t, "用户问题", inputs.Query)
    assert.Equal(t, "检索用查询", inputs.RetrievalQuery)
}

func TestTaskIterationInputs_RetrievalQuery_默认空(t *testing.T) {
    inputs := &TaskIterationInputs{Query: "问题"}
    assert.Equal(t, "", inputs.RetrievalQuery)
}
```

- [x] **Step 2: 运行测试确认失败**

Run: `cd /home/opensource/uap-claw-go && go test ./internal/agentcore/single_agent/interfaces/... -run TestTaskIterationInputs_RetrievalQuery -v`
Expected: FAIL — `RetrievalQuery` 字段不存在

- [x] **Step 3: 实现 RetrievalQuery 字段**

在 `callback.go` 的 `TaskIterationInputs` 结构体中添加字段：
```go
type TaskIterationInputs struct {
    // Iteration 1-based 外层循环迭代索引
    Iteration int
    // LoopEvent 触发本次迭代的事件对象
    LoopEvent any
    // ConversationID 会话/对话标识
    ConversationID string
    // Result 迭代结果（迭代完成后填充）
    Result map[string]any
    // Query 本次迭代的有效查询（before_task_iteration 钩子可修改）
    Query string
    // RetrievalQuery 检索用查询（优先于 Query）
    // 对齐 Python: retrieval_query = getattr(ctx.inputs, "retrieval_query", None) or query
    RetrievalQuery string
    // IsFollowUp 是否由 controller follow-up 触发
    IsFollowUp bool
}
```

- [x] **Step 4: 运行测试确认通过**

Run: `go test ./internal/agentcore/single_agent/interfaces/... -run TestTaskIterationInputs_RetrievalQuery -v`
Expected: PASS

- [x] **Step 5: 提交**

```bash
git add internal/agentcore/single_agent/interfaces/callback.go internal/agentcore/single_agent/interfaces/callback_test.go
git commit -m "feat(interfaces): TaskIterationInputs 加 RetrievalQuery 字段 (S-01)"
```

---

### Task 2：beforeTaskIteration 优先用 retrievalQuery + 缓存键修复

**Files:**
- Modify: `internal/agentcore/harness/rails/evolution/context_evolution_rail.go:208-317`
- Test: `internal/agentcore/harness/rails/evolution/context_evolution_rail_test.go`

- [x] **Step 1: 写失败测试**

```go
func TestBeforeTaskIteration_RetrievalQuery优先(t *testing.T) {
    mock := &mockTaskMemoryService{
        retrieveResult: &ceservice.RetrieveResult{
            MemoryString:    "检索结果",
            RetrievedMemory: []ceschema.MemoryItem{&mockRetrievedMemory{text: "经验1"}},
            Query:           "检索用查询",
            UserID:          "alice",
            Algorithm:       "ACE",
        },
    }
    r := NewContextEvolutionRail(context.Background(), "alice", nil, WithMemoryService(mock))

    cbc := &agentinterfaces.AgentCallbackContext{}
    cbc.SetInputs(&agentinterfaces.TaskIterationInputs{
        Query:          "用户问题",
        RetrievalQuery: "检索用查询",
    })

    err := r.beforeTaskIteration(context.Background(), cbc)
    assert.NoError(t, err)
    assert.Equal(t, 1, r.memoriesUsed)
    // 验证缓存键用的是 retrievalQuery
    assert.Equal(t, "检索用查询", r.lastRetrievedQuery)
}
```

- [x] **Step 2: 运行测试确认失败**

Run: `go test ./internal/agentcore/harness/rails/evolution/... -run TestBeforeTaskIteration_RetrievalQuery优先 -v`
Expected: FAIL — 当前 `lastRetrievedQuery` 值为 `"用户问题"`（因为只用了 `query`）

- [x] **Step 3: 修改 beforeTaskIteration**

在 `context_evolution_rail.go` 的 `beforeTaskIteration` 方法中，将第 229 行后添加 retrievalQuery 优先逻辑：

```go
// 3. 获取 query 和 retrievalQuery
// 对齐 Python: query = getattr(ctx.inputs, "query", None) or ""
// 对齐 Python: retrieval_query = getattr(ctx.inputs, "retrieval_query", None) or query
inputs := cbc.Inputs()
taskInputs, ok := inputs.(*agentinterfaces.TaskIterationInputs)
if !ok || taskInputs == nil {
    return nil
}
query := taskInputs.Query
if query == "" {
    return nil
}

// retrievalQuery 优先于 query（对齐 Python）
retrievalQuery := query
if taskInputs.RetrievalQuery != "" {
    retrievalQuery = taskInputs.RetrievalQuery
}
```

然后将后续所有使用 `query` 做检索键和缓存键的地方替换为 `retrievalQuery`：
- 第 244 行: `r.lastRetrievedQuery == query` → `r.lastRetrievedQuery == retrievalQuery`
- 第 248 行: `r.memoryService.Retrieve(ctx, r.userID, query)` → `r.memoryService.Retrieve(ctx, r.userID, retrievalQuery)`
- 第 254 行: `r.lastRetrievedQuery = query` → `r.lastRetrievedQuery = retrievalQuery`

保留 `r.currentQuery = query`（对齐 Python: `self._current_query = query`，currentQuery 用原始 query 用于后续 auto_summarize）

- [x] **Step 4: 运行测试确认通过**

Run: `go test ./internal/agentcore/harness/rails/evolution/... -run TestBeforeTaskIteration -v`
Expected: ALL PASS

- [x] **Step 5: 提交**

```bash
git add internal/agentcore/harness/rails/evolution/context_evolution_rail.go internal/agentcore/harness/rails/evolution/context_evolution_rail_test.go
git commit -m "fix(evolution): beforeTaskIteration 优先用 retrievalQuery 做检索和缓存键 (S-01)"
```

---

### Task 3：memories_used 标注目标改为 taskInputs.Result

**Files:**
- Modify: `internal/agentcore/harness/rails/evolution/context_evolution_rail.go:328-379`
- Test: `internal/agentcore/harness/rails/evolution/context_evolution_rail_test.go`

- [x] **Step 1: 写失败测试**

```go
func TestAfterTaskIteration_MemoriesUsed写入Result(t *testing.T) {
    r := NewContextEvolutionRail(context.Background(), "alice", nil)
    r.memoriesUsed = 5

    result := make(map[string]any)
    cbc := &agentinterfaces.AgentCallbackContext{}
    cbc.SetInputs(&agentinterfaces.TaskIterationInputs{Result: result})

    err := r.afterTaskIteration(context.Background(), cbc)
    assert.NoError(t, err)
    assert.Equal(t, 5, result["memories_used"])
}
```

- [x] **Step 2: 运行测试确认失败**

Run: `go test ./internal/agentcore/harness/rails/evolution/... -run TestAfterTaskIteration_MemoriesUsed写入Result -v`
Expected: FAIL — 当前 `result["memories_used"]` 不存在

- [x] **Step 3: 修改 afterTaskIteration**

替换 `afterTaskIteration` 中第 352-354 行的 `cbc.Extra()` 写入逻辑：

旧代码:
```go
// 2. 标注 memoriesUsed（对齐 Python: result["memories_used"] = self.memories_used）
if cbc.Extra() != nil {
    cbc.Extra()["memories_used"] = r.memoriesUsed
}
```

新代码:
```go
// 2. 标注 memoriesUsed（对齐 Python: result["memories_used"] = self.memories_used）
if inputs := cbc.Inputs(); inputs != nil {
    if taskInputs, ok := inputs.(*agentinterfaces.TaskIterationInputs); ok && taskInputs.Result != nil {
        taskInputs.Result["memories_used"] = r.memoriesUsed
    }
}
```

- [x] **Step 4: 运行测试确认通过**

Run: `go test ./internal/agentcore/harness/rails/evolution/... -run TestAfterTaskIteration -v`
Expected: ALL PASS

- [x] **Step 5: 提交**

```bash
git add internal/agentcore/harness/rails/evolution/context_evolution_rail.go internal/agentcore/harness/rails/evolution/context_evolution_rail_test.go
git commit -m "fix(evolution): memories_used 写入 taskInputs.Result 对齐 Python (S-02)"
```

---

### Task 4：NewContextEvolutionRail 签名加 ctx 参数

**Files:**
- Modify: `internal/agentcore/harness/rails/evolution/context_evolution_rail.go:97-146`
- Test: `internal/agentcore/harness/rails/evolution/context_evolution_rail_test.go`

- [x] **Step 1: 修改构造函数签名**

将 `NewContextEvolutionRail` 签名从:
```go
func NewContextEvolutionRail(userID string, memoryService *ceservice.TaskMemoryService, opts ...ContextEvolutionRailOption) *ContextEvolutionRail
```
改为:
```go
func NewContextEvolutionRail(ctx context.Context, userID string, memoryService *ceservice.TaskMemoryService, opts ...ContextEvolutionRailOption) *ContextEvolutionRail
```

将第 134 行 `context.Background()` 替换为 `ctx`:
```go
if err := r.memoryService.LoadMemories(ctx, r.userID); err != nil {
```

- [x] **Step 2: 更新所有测试中的调用点**

在 `context_evolution_rail_test.go` 中将所有 `NewContextEvolutionRail("alice", nil, ...)` 改为 `NewContextEvolutionRail(context.Background(), "alice", nil, ...)`。

- [x] **Step 3: 运行全部 evolution 测试**

Run: `go test ./internal/agentcore/harness/rails/evolution/... -v`
Expected: ALL PASS

- [x] **Step 4: 提交**

```bash
git add internal/agentcore/harness/rails/evolution/context_evolution_rail.go internal/agentcore/harness/rails/evolution/context_evolution_rail_test.go
git commit -m "fix(evolution): NewContextEvolutionRail 签名加 ctx 参数 (S-03)"
```

---

### Task 5：auto_summarize_matts_mode 限制 + strip + WithUserID

**Files:**
- Modify: `internal/agentcore/harness/rails/evolution/context_evolution_rail.go`
- Test: `internal/agentcore/harness/rails/evolution/context_evolution_rail_test.go`

- [x] **Step 1: 写失败测试**

```go
func TestAfterTaskIteration_MattsMode非None时跳过(t *testing.T) {
    mock := &mockTaskMemoryService{summarizeResult: &ceservice.SummarizeResult{}}
    r := NewContextEvolutionRail(context.Background(), "alice", nil,
        WithMemoryService(mock),
        WithAutoSummarize(true),
        WithAutoSummarizeMattsMode("parallel"),
    )
    r.currentQuery = "查询"

    cbc := &agentinterfaces.AgentCallbackContext{}
    err := r.afterTaskIteration(context.Background(), cbc)
    assert.NoError(t, err)
    assert.False(t, mock.summarizeCalled, "matts_mode 非 none 时不应调用 Summarize")
}
```

- [x] **Step 2: 运行测试确认失败**

Run: `go test ./internal/agentcore/harness/rails/evolution/... -run TestAfterTaskIteration_MattsMode非None时跳过 -v`
Expected: FAIL — 当前 `summarizeCalled` 为 true（因为未检查 matts_mode）

- [x] **Step 3: 修改 afterTaskIteration 的 auto_summarize 条件**

将第 359 行:
```go
if r.autoSummarize && r.currentQuery != "" && r.memoryService != nil {
```
改为:
```go
if r.autoSummarize && r.autoSummarizeMattsMode == "none" && r.currentQuery != "" && r.memoryService != nil {
```

在该 if 块之后添加:
```go
} else if r.autoSummarize && r.autoSummarizeMattsMode != "none" && r.currentQuery != "" {
    logger.Warn(logComponent).Str("matts_mode", r.autoSummarizeMattsMode).Msg("auto_summarize 仅支持 matts_mode=none，跳过")
}
```

- [x] **Step 4: 修改 beforeTaskIteration 中 strip**

在第 302 行:
```go
newMsg["content"] = content + "\n\n" + memoryBlock
```
改为:
```go
newMsg["content"] = strings.TrimSpace(content + "\n\n" + memoryBlock)
```

确保 import 中有 `"strings"`。

- [x] **Step 5: 添加 WithUserID 选项**

在导出函数区域添加:
```go
// WithUserID 设置用户标识（允许延迟覆盖构造时的 userID）。
func WithUserID(userID string) ContextEvolutionRailOption {
    return func(r *ContextEvolutionRail) { r.userID = userID }
}
```

- [x] **Step 6: 运行全部测试**

Run: `go test ./internal/agentcore/harness/rails/evolution/... -v`
Expected: ALL PASS

- [x] **Step 7: 提交**

```bash
git add internal/agentcore/harness/rails/evolution/context_evolution_rail.go internal/agentcore/harness/rails/evolution/context_evolution_rail_test.go
git commit -m "fix(evolution): matts_mode限制+strip+WithUserID (M-02/T-01/T-02)"
```

---

## Part B：Session Memory 集成回填（5 项）

### Task 6：WithSessionMemoryConfig 选项 + 构造时初始化 SessionMemoryManager

**Files:**
- Modify: `internal/agentcore/harness/rails/context_engineer/context_processor_rail.go:57-83`
- Test: `internal/agentcore/harness/rails/context_engineer/context_processor_rail_test.go`

- [x] **Step 1: 写失败测试**

```go
func TestNewContextProcessorRail_WithSessionMemoryConfig(t *testing.T) {
    cfg := cecontext.NewSessionMemoryConfig()
    cfg.TriggerTokens = 20000
    r := NewContextProcessorRail(WithSessionMemoryConfig(&cfg))
    assert.True(t, r.sessionMemoryEnabled)
    assert.NotNil(t, r.sessionMemoryConfig)
    assert.Equal(t, 20000, r.sessionMemoryConfig.TriggerTokens)
    assert.NotNil(t, r.sessionMemoryMgr)
}
```

- [x] **Step 2: 运行测试确认失败**

Run: `go test ./internal/agentcore/harness/rails/context_engineer/... -run TestNewContextProcessorRail_WithSessionMemoryConfig -v`
Expected: FAIL — `WithSessionMemoryConfig` 不存在

- [x] **Step 3: 实现 WithSessionMemoryConfig 选项**

在 `context_processor_rail.go` 导出函数区域添加:
```go
// WithSessionMemoryConfig 设置会话记忆配置。
// 传入非 nil config 时自动启用 session memory 并创建 SessionMemoryManager。
// 对齐 Python: ContextProcessorRail(session_memory=SessionMemoryConfig(...)) 或 session_memory={...}
func WithSessionMemoryConfig(config *cecontext.SessionMemoryConfig) ContextProcessorRailOption {
    return func(r *ContextProcessorRail) {
        r.sessionMemoryEnabled = config != nil
        r.sessionMemoryConfig = config
        if config != nil {
            r.sessionMemoryMgr = cecontext.NewSessionMemoryManager(*config)
        }
    }
}
```

- [x] **Step 4: 运行测试确认通过**

Run: `go test ./internal/agentcore/harness/rails/context_engineer/... -run TestNewContextProcessorRail_WithSessionMemoryConfig -v`
Expected: PASS

- [x] **Step 5: 提交**

```bash
git add internal/agentcore/harness/rails/context_engineer/context_processor_rail.go internal/agentcore/harness/rails/context_engineer/context_processor_rail_test.go
git commit -m "feat(context_engineer): WithSessionMemoryConfig 选项 + 构造时初始化 SessionMemoryManager (SM-01)"
```

---

### Task 7：Init 中 BindModelDefaults

**Files:**
- Modify: `internal/agentcore/harness/rails/context_engineer/context_processor_rail.go:88-123`
- Test: `internal/agentcore/harness/rails/context_engineer/context_processor_rail_test.go`

- [x] **Step 1: 写失败测试**

```go
func TestContextProcessorRail_Init_BindModelDefaults(t *testing.T) {
    cfg := cecontext.NewSessionMemoryConfig()
    r := NewContextProcessorRail(WithSessionMemoryConfig(&cfg))
    // 初始时 Model 为 nil
    assert.Nil(t, r.sessionMemoryConfig.Model)

    // 创建 mock agent，config 有 model 设置
    agent := newMockDeepAgentForProcessor()
    r.Init(context.Background(), agent)

    // Init 后应从 agent 的 config 绑定 model 默认值
    assert.NotNil(t, r.sessionMemoryConfig.Model)
}
```

- [x] **Step 2: 运行测试确认失败**

Run: `go test ./internal/agentcore/harness/rails/context_engineer/... -run TestContextProcessorRail_Init_BindModelDefaults -v`
Expected: FAIL — Init 中未调用 BindModelDefaults

- [x] **Step 3: 在 Init 末尾添加 BindModelDefaults 调用**

在 `Init` 方法第 120 行（return nil 之前）添加:
```go
// 对齐 Python: if self._session_memory_config is not None:
//     self._session_memory_config.set_model_defaults(model_config, model_client_config)
//     self._session_memory_mgr.bind_model_defaults(model_config, model_client_config)
if r.sessionMemoryConfig != nil {
    r.sessionMemoryConfig.SetModelDefaults(modelConfig, modelClientConfig)
}
if r.sessionMemoryMgr != nil {
    r.sessionMemoryMgr.BindModelDefaults(modelConfig, modelClientConfig)
}
```

- [x] **Step 4: 运行测试确认通过**

Run: `go test ./internal/agentcore/harness/rails/context_engineer/... -run TestContextProcessorRail_Init -v`
Expected: ALL PASS

- [x] **Step 5: 提交**

```bash
git add internal/agentcore/harness/rails/context_engineer/context_processor_rail.go internal/agentcore/harness/rails/context_engineer/context_processor_rail_test.go
git commit -m "feat(context_engineer): Init 中 BindModelDefaults 对齐 Python (SM-02)"
```

---

### Task 8：AfterModelCall 中 session memory 调度

**Files:**
- Modify: `internal/agentcore/harness/rails/context_engineer/context_processor_rail.go:166-174`
- Test: `internal/agentcore/harness/rails/context_engineer/context_processor_rail_test.go`

- [x] **Step 1: 写失败测试**

```go
func TestContextProcessorRail_AfterModelCall_SessionMemory调度(t *testing.T) {
    cfg := cecontext.NewSessionMemoryConfig()
    r := NewContextProcessorRail(WithSessionMemoryConfig(&cfg))
    assert.NotNil(t, r.sessionMemoryMgr)

    cbc := &agentinterfaces.AgentCallbackContext{}
    // 设置 cbc 的 agent/session 等...
    err := r.AfterModelCall(context.Background(), cbc)
    assert.NoError(t, err)
}
```

- [x] **Step 2: 运行测试确认编译通过**

Run: `go build ./internal/agentcore/harness/rails/context_engineer/...`
Expected: 编译通过（方法已存在，只是 TODO 逻辑不同）

- [x] **Step 3: 在 AfterModelCall 中替换 TODO 为实际调用**

替换第 170-173 行:
```go
RefreshTaskStateRuntime(cbc)
// TODO(#通用): 后续回填 session memory update_inherited_system_prompt
// TODO(#通用): 后续回填 session memory maybe_schedule_update
return nil
```

为:
```go
RefreshTaskStateRuntime(cbc)

// 对齐 Python: if self._session_memory_mgr is not None:
//     self._session_memory_mgr.update_inherited_system_prompt(ctx)
if r.sessionMemoryMgr != nil {
    if deepAgent, ok := cbc.Agent().(hinterfaces.DeepAgentInterface); ok && deepAgent != nil {
        reactAgent := deepAgent.ReactAgent()
        if reactAgent != nil {
            ce := reactAgent.ContextEngine()
            if ce != nil {
                sess := cbc.Session()
                if sess != nil {
                    mc := ce.GetContext("default_context_id", sess.GetSessionID())
                    if mc != nil {
                        messages, _ := mc.GetMessages(0, true)
                        r.sessionMemoryMgr.UpdateInheritedSystemPrompt(messages)
                    }
                }
            }
        }
    }
}

// 对齐 Python: await self._maybe_schedule_session_memory_update(ctx)
if r.sessionMemoryEnabled && r.sessionMemoryMgr != nil {
    if deepAgent, ok := cbc.Agent().(hinterfaces.DeepAgentInterface); ok && deepAgent != nil {
        reactAgent := deepAgent.ReactAgent()
        if reactAgent != nil {
            ce := reactAgent.ContextEngine()
            if ce != nil {
                sess := cbc.Session()
                if sess != nil {
                    mc := ce.GetContext("default_context_id", sess.GetSessionID())
                    if mc != nil {
                        workspaceDir := ""
                        if ws := deepAgent.Workspace(); ws != nil {
                            workspaceDir = ws.RootDir()
                        }
                        r.sessionMemoryMgr.MaybeScheduleUpdate(ctx, sess, mc, workspaceDir)
                    }
                }
            }
        }
    }
}

return nil
```

注意：需要添加 `hinterfaces` 导入。

- [x] **Step 4: 运行测试确认通过**

Run: `go test ./internal/agentcore/harness/rails/context_engineer/... -v`
Expected: ALL PASS

- [x] **Step 5: 提交**

```bash
git add internal/agentcore/harness/rails/context_engineer/context_processor_rail.go internal/agentcore/harness/rails/context_engineer/context_processor_rail_test.go
git commit -m "feat(context_engineer): AfterModelCall 中 session memory 调度对齐 Python (SM-03)"
```

---

### Task 9：Uninit 中 Shutdown

**Files:**
- Modify: `internal/agentcore/harness/rails/context_engineer/context_processor_rail.go:125-147`
- Test: `internal/agentcore/harness/rails/context_engineer/context_processor_rail_test.go`

- [x] **Step 1: 替换 Uninit 中的 TODO**

将第 129-130 行:
```go
// 关闭会话记忆管理器（预留）
// TODO(#通用): 后续回填 session memory manager shutdown
```

替换为:
```go
// 对齐 Python: if self._session_memory_mgr is not None:
//     self._session_memory_mgr.shutdown()
if r.sessionMemoryMgr != nil {
    r.sessionMemoryMgr.Shutdown()
}
```

- [x] **Step 2: 写测试验证**

```go
func TestContextProcessorRail_Uninit_Shutdown(t *testing.T) {
    cfg := cecontext.NewSessionMemoryConfig()
    r := NewContextProcessorRail(WithSessionMemoryConfig(&cfg))
    assert.NotNil(t, r.sessionMemoryMgr)

    agent := newMockDeepAgentForProcessor()
    r.Uninit(agent)
    // Shutdown 不返回 error，只验证不 panic
}
```

- [x] **Step 3: 运行测试确认通过**

Run: `go test ./internal/agentcore/harness/rails/context_engineer/... -v`
Expected: ALL PASS

- [x] **Step 4: 提交**

```bash
git add internal/agentcore/harness/rails/context_engineer/context_processor_rail.go internal/agentcore/harness/rails/context_engineer/context_processor_rail_test.go
git commit -m "feat(context_engineer): Uninit 中 SessionMemoryManager.Shutdown 对齐 Python (SM-04)"
```

---

### Task 10：SessionMemoryAgentUpdater 完整实现

**Files:**
- Modify: `internal/agentcore/context_engine/context/session_memory_manager.go:78-459`
- Test: `internal/agentcore/context_engine/context/session_memory_manager_test.go`

这是最大的单任务。Python 中 `SessionMemoryUpdateAgent`（L214-490）是一个专用 ReActAgent，使用 `read_file` + `edit_file` 工具编辑笔记文件。

- [x] **Step 1: 写失败测试**

```go
func TestSessionMemoryAgentUpdater_Invoke(t *testing.T) {
    cfg := NewSessionMemoryConfig()
    cfg.UpdateMode = "agent_edit"
    // 需要创建 mock ReActAgent 或 mock LLM
    u := NewSessionMemoryAgentUpdater(cfg)
    err := u.Invoke(context.Background(), SessionMemoryUpdateOptions{
        NotesPath:    "/tmp/test_notes.md",
        CurrentNotes: "# Session Title\n_Test_",
    })
    // 当前应返回 "agent_edit 模式尚未实现" 错误
    assert.Error(t, err)
}
```

- [x] **Step 2: 运行测试确认当前行为**

Run: `go test ./internal/agentcore/context_engine/context/... -run TestSessionMemoryAgentUpdater -v`
Expected: PASS — 当前返回 error

- [x] **Step 3: 实现 SessionMemoryAgentUpdater 完整逻辑**

修改 `SessionMemoryAgentUpdater` 结构体，添加必要字段：
```go
// SessionMemoryAgentUpdater agent_edit 模式的会话记忆更新器。
// 创建专用 ReActAgent 执行笔记编辑操作。
// 对齐 Python: SessionMemoryUpdateAgent (session_memory_manager.py L214-490)
type SessionMemoryAgentUpdater struct {
    config                SessionMemoryConfig
    modelConfig           *llm_schema.ModelRequestConfig
    modelClientConfig     *llm_schema.ModelClientConfig
    inheritedSystemPrompt string
}
```

实现 `BindModelDefaults`:
```go
func (u *SessionMemoryAgentUpdater) BindModelDefaults(
    modelConfig *llm_schema.ModelRequestConfig,
    clientConfig *llm_schema.ModelClientConfig,
) {
    if u.modelConfig == nil && modelConfig != nil {
        u.modelConfig = modelConfig
    }
    if u.modelClientConfig == nil && clientConfig != nil {
        u.modelClientConfig = clientConfig
    }
}
```

实现 `SetInheritedSystemPrompt`:
```go
func (u *SessionMemoryAgentUpdater) SetInheritedSystemPrompt(prompt string) {
    u.inheritedSystemPrompt = prompt
}
```

实现 `Invoke` — agent_edit 模式核心流程（对齐 Python `_invoke_agent_edit`）：
1. 校验 modelConfig/modelClientConfig 非 nil
2. 构建 agent_edit 提示词（替换 `{{notesPath}}` 和 `{{currentNotes}}` 占位符）
3. 创建模型实例
4. 构建消息列表（inheritedSystemPrompt + FullContextMessages + agent_edit prompt）
5. 调用 LLM 获取 `edit_file` 指令
6. 解析 `edit_file` 调用参数（file_path + diff/old_string/new_string）
7. 执行文件编辑操作
8. 更新 session memory 运行时状态

```go
func (u *SessionMemoryAgentUpdater) Invoke(ctx context.Context, opts SessionMemoryUpdateOptions) error {
    if u.modelConfig == nil || u.modelClientConfig == nil {
        return fmt.Errorf("agent_edit 模式需要 model 配置")
    }

    // 构建提示词
    prompt := buildAgentSessionMemoryPrompt(opts.NotesPath, opts.CurrentNotes)

    // 创建模型实例（延迟初始化）
    model, err := llm.NewModel(u.modelClientConfig, u.modelConfig)
    if err != nil {
        return fmt.Errorf("创建模型实例失败: %w", err)
    }

    // 构建消息列表
    var messages []llm_schema.BaseMessage
    if inheritedPrompt := strings.TrimSpace(u.inheritedSystemPrompt); inheritedPrompt != "" {
        messages = append(messages, llm_schema.NewSystemMessage(inheritedPrompt))
    }
    messages = append(messages, opts.FullContextMessages...)
    messages = append(messages, llm_schema.NewUserMessage(prompt))

    // 调用 LLM
    response, err := model.Invoke(ctx, model_clients.NewMessagesParam(messages...),
        model_clients.WithInvokeModel(u.modelConfig.ModelName))
    if err != nil {
        return fmt.Errorf("agent_edit LLM 调用失败: %w", err)
    }

    // 解析 tool_calls（edit_file 调用）
    // 对齐 Python: 解析 assistant response 中的 tool_calls，
    // 提取 edit_file 参数（file_path, old_string, new_string），
    // 然后执行文件编辑
    return parseAndApplyFileEdits(response, opts.NotesPath)
}
```

需要实现辅助函数 `buildAgentSessionMemoryPrompt` 和 `parseAndApplyFileEdits`。

- [x] **Step 4: 运行测试确认通过**

Run: `go test ./internal/agentcore/context_engine/context/... -v`
Expected: ALL PASS

- [x] **Step 5: 提交**

```bash
git add internal/agentcore/context_engine/context/session_memory_manager.go internal/agentcore/context_engine/context/session_memory_manager_test.go
git commit -m "feat(context_engine): SessionMemoryAgentUpdater agent_edit 模式完整实现 (SM-05)"
```

---

## Part C：10.6.3-10 Rail 实例化/注入回填（5 项）

### Task 11：TeamSkillCreateRail 类型定义

**Files:**
- Create: `internal/agentcore/harness/rails/evolution/team_skill_create_rail.go`
- Create: `internal/agentcore/harness/rails/evolution/team_skill_create_rail_test.go`
- Modify: `internal/agentcore/harness/rails/evolution/doc.go`

Python 参考: `openjiuwen/harness/rails/skills/team_skill_create_rail.py`（TeamSkillCreateRail 继承 EvolutionRail，priority=85，evolution_trigger=NONE）

- [x] **Step 1: 写失败测试**

```go
func TestNewTeamSkillCreateRail(t *testing.T) {
    r := NewTeamSkillCreateRail("/tmp/skills",
        WithLanguage("cn"),
        WithAutoTrigger(true),
        WithMinTeamMembers(2),
    )
    assert.NotNil(t, r)
    assert.Equal(t, 85, r.Priority())
    assert.Equal(t, "/tmp/skills", r.SkillsDir())
    assert.True(t, r.AutoTrigger())
    assert.Equal(t, 2, r.MinTeamMembers())
}
```

- [x] **Step 2: 运行测试确认失败**

Run: `go test ./internal/agentcore/harness/rails/evolution/... -run TestNewTeamSkillCreateRail -v`
Expected: FAIL — `NewTeamSkillCreateRail` 不存在

- [x] **Step 3: 实现 TeamSkillCreateRail**

创建 `team_skill_create_rail.go`，包含:
- `TeamSkillCreateRail` 结构体（嵌入 `*EvolutionRail`）
- 构造选项: `WithLanguage`, `WithAutoTrigger`, `WithMinTeamMembers`
- `EvolutionExtension` 接口实现（`_onAfterTaskIteration` + `_onAfterInvoke` + `notifyTeamCompleted` + `_maybeEnqueueCreationFollowUp`）
- 双语提示词常量 `_FOLLOW_UP_PROMPT_CN` / `_FOLLOW_UP_PROMPT_EN`
- `Priority()` 返回 85
- evolution_trigger=NONE

- [x] **Step 4: 运行测试确认通过**

Run: `go test ./internal/agentcore/harness/rails/evolution/... -run TestNewTeamSkillCreateRail -v`
Expected: PASS

- [x] **Step 5: 更新 doc.go 文件目录**

在 `doc.go` 的文件目录中添加:
```
//	├── team_skill_create_rail.go       # TeamSkillCreateRail 团队技能创建轨道（Priority=85）
```

- [x] **Step 6: 提交**

```bash
git add internal/agentcore/harness/rails/evolution/team_skill_create_rail.go internal/agentcore/harness/rails/evolution/team_skill_create_rail_test.go internal/agentcore/harness/rails/evolution/doc.go
git commit -m "feat(evolution): TeamSkillCreateRail 类型定义对齐 Python (RA-02)"
```

---

### Task 12：buildTeamSkillEvolutionRail + buildSkillCreateRail

**Files:**
- Modify: `internal/swarm/server/adapter/deep_adapter_rails.go`

- [x] **Step 1: 实现 buildTeamSkillEvolutionRail**

在 `deep_adapter_rails.go` 中添加方法：
```go
// buildTeamSkillEvolutionRail 构建团队技能演进护栏。
// 对齐 Python: _build_team_skill_evolution_rail()
func (d *DeepAdapter) buildTeamSkillEvolutionRail() sainterfaces.AgentRail {
    if d.model == nil {
        logger.Warn(logComponent).Msg("buildTeamSkillEvolutionRail: model is nil, skipping")
        return nil
    }

    evolutionConfig, _ := d.configCache["evolution"].(map[string]any)
    language := d.resolveRuntimeLanguage()

    skillsDir := workspace.AgentSkillsDir()
    if skillsDir == "" {
        logger.Warn(logComponent).Msg("buildTeamSkillEvolutionRail: skills dir is empty, skipping")
        return nil
    }

    modelName := d.defaultModelName
    if modelName == "" {
        modelName = "qwen-max"
    }

    return evolution.NewTeamSkillEvolutionRail(
        []string{skillsDir},
        d.model,
        modelName,
        language,
    )
}
```

- [x] **Step 2: 实现 buildSkillCreateRail**

替换第 393-399 行的 stub：
```go
// buildSkillCreateRail 构建技能创建护栏。
// 对齐 Python: _build_skill_create_rail() (line 2011-2050)
func (d *DeepAdapter) buildSkillCreateRail() sainterfaces.AgentRail {
    skillsDir := workspace.AgentSkillsDir()
    if skillsDir == "" {
        logger.Warn(logComponent).Msg("buildSkillCreateRail: skills dir is empty, skipping")
        return nil
    }

    language := d.resolveRuntimeLanguage()
    autoTrigger := true
    minTeamMembers := 2

    if evolutionConfig, ok := d.configCache["evolution"].(map[string]any); ok {
        if v, ok := evolutionConfig["skill_create_auto_trigger"].(bool); ok {
            autoTrigger = v
        }
        if v, ok := evolutionConfig["min_team_members_for_create"].(int); ok && v > 0 {
            minTeamMembers = v
        }
    }

    return evolution.NewTeamSkillCreateRail(skillsDir,
        evolution.WithLanguage(language),
        evolution.WithAutoTrigger(autoTrigger),
        evolution.WithMinTeamMembers(minTeamMembers),
    )
}
```

注意：`buildSkillCreateRail` 的返回类型从 `sainterfaces.AgentRail` 保持不变（因为 DeepAdapter.skillCreateRail 字段类型仍是接口）。

- [x] **Step 3: 在 buildRailsMethodMap 中注册新方法**

在 `buildRailsMethodMap` 中添加:
```go
case "buildTeamSkillEvolutionRail":
    return d.buildTeamSkillEvolutionRail()
```

- [x] **Step 4: 运行编译确认**

Run: `go build ./internal/swarm/server/adapter/...`
Expected: 编译通过

- [x] **Step 5: 提交**

```bash
git add internal/swarm/server/adapter/deep_adapter_rails.go
git commit -m "feat(adapter): buildTeamSkillEvolutionRail + buildSkillCreateRail 实现 (RA-01/RA-02)"
```

---

### Task 13：buildAgentRails 中存储 + DeepAdapter 字段类型修改

**Files:**
- Modify: `internal/swarm/server/adapter/deep_adapter.go:156-161`
- Modify: `internal/swarm/server/adapter/deep_adapter_rails.go:164-173`

- [x] **Step 1: 修改 DeepAdapter 字段类型**

在 `deep_adapter.go` 中将第 156-158 行:
```go
// skillCreateRail 技能创建护栏
// ⤵️ 10.6.3-10: SkillCreateRail
skillCreateRail sainterfaces.AgentRail
```

改为:
```go
// skillCreateRail 技能创建护栏
// 对齐 Python: _skill_create_rail: TeamSkillCreateRail | None
skillCreateRail *evolution.TeamSkillCreateRail
```

- [x] **Step 2: 在 buildAgentRails 中添加 TeamSkillEvolutionRail 和 SkillCreateRail 存储**

在第 168-173 行之后添加:
```go
// teamSkillEvolutionRail — 仅构建实例存储到字段，不注册
teamEvolve := d.buildTeamSkillEvolutionRail()
if teamEvolve != nil {
    if rail, ok := teamEvolve.(*evolution.TeamSkillEvolutionRail); ok {
        d.teamSkillEvolutionRail = rail
    }
}

// skillCreateRail — 仅构建实例存储到字段，不注册
createRail := d.buildSkillCreateRail()
if createRail != nil {
    if rail, ok := createRail.(*evolution.TeamSkillCreateRail); ok {
        d.skillCreateRail = rail
    }
}
```

同时修改原来的 `_ = d.buildSkillCreateRail()` 行为实际存储。

- [x] **Step 3: 运行编译确认**

Run: `go build ./internal/swarm/server/adapter/...`
Expected: 编译通过

- [x] **Step 4: 提交**

```bash
git add internal/swarm/server/adapter/deep_adapter.go internal/swarm/server/adapter/deep_adapter_rails.go
git commit -m "feat(adapter): buildAgentRails 中存储 TeamSkillEvolutionRail + SkillCreateRail (RA-03)"
```

---

### Task 14：findTeamSkillRail + findSkillCreateRail 实现查找

**Files:**
- Modify: `internal/swarm/server/adapter/deep_adapter_team.go:22-31`

- [x] **Step 1: 实现 findTeamSkillRail 从 instance.rails 查找**

替换第 22-31 行:
```go
func (d *DeepAdapter) findTeamSkillRail() *evolution.TeamSkillEvolutionRail {
    if d.teamSkillEvolutionRail != nil {
        return d.teamSkillEvolutionRail
    }
    // 对齐 Python: harness.find_rails(TeamSkillEvolutionRail)
    if d.instance != nil {
        for _, rail := range d.instance.Rails() {
            if tsr, ok := rail.(*evolution.TeamSkillEvolutionRail); ok {
                d.teamSkillEvolutionRail = tsr
                return tsr
            }
        }
    }
    return nil
}
```

- [x] **Step 2: 添加 findSkillCreateRail 方法**

```go
// findSkillCreateRail 查找 TeamSkillCreateRail。
func (d *DeepAdapter) findSkillCreateRail() *evolution.TeamSkillCreateRail {
    if d.skillCreateRail != nil {
        return d.skillCreateRail
    }
    if d.instance != nil {
        for _, rail := range d.instance.Rails() {
            if scr, ok := rail.(*evolution.TeamSkillCreateRail); ok {
                d.skillCreateRail = scr
                return scr
            }
        }
    }
    return nil
}
```

- [x] **Step 3: 运行编译确认**

Run: `go build ./internal/swarm/server/adapter/...`
Expected: 编译通过

- [x] **Step 4: 提交**

```bash
git add internal/swarm/server/adapter/deep_adapter_team.go
git commit -m "feat(adapter): findTeamSkillRail + findSkillCreateRail 从 instance.rails 查找 (RA-04)"
```

---

### Task 15：updateRailsForMode 按需注册 TeamSkillEvolutionRail + SkillCreateRail

**Files:**
- Modify: `internal/swarm/server/adapter/deep_adapter_rails.go`

- [x] **Step 1: 在 updatePlanModeRails 第 8 步替换 TODO**

替换第 8 步的:
```go
// 8. SkillCreateRail
// Python: skill_create_enabled = _get_skill_create_enabled(self._config_cache)
// ⤵️ 待回填: SkillCreateRail 处理
```

为:
```go
// 8. TeamSkillEvolutionRail — 团队模式启用时注册
teamEvolutionConfig, _ := d.configCache["team_evolution"].(map[string]any)
teamEvolutionEnabled, _ := teamEvolutionConfig["enabled"].(bool)
if teamEvolutionEnabled {
    if d.teamSkillEvolutionRail == nil {
        rail := d.buildTeamSkillEvolutionRail()
        if rail != nil {
            var ok bool
            d.teamSkillEvolutionRail, ok = rail.(*evolution.TeamSkillEvolutionRail)
            if ok && d.teamSkillEvolutionRail != nil && d.instance != nil {
                if err := d.instance.RegisterRail(ctx, d.teamSkillEvolutionRail); err != nil {
                    logger.Error(logComponent).Err(err).Msg("Failed to register TeamSkillEvolutionRail")
                } else {
                    logger.Info(logComponent).Msg("TeamSkillEvolutionRail registered (plan mode)")
                }
            }
        }
    }
} else {
    if d.teamSkillEvolutionRail != nil && d.instance != nil {
        if err := d.instance.UnregisterRail(ctx, d.teamSkillEvolutionRail); err != nil {
            logger.Error(logComponent).Err(err).Msg("Failed to unregister TeamSkillEvolutionRail")
        }
        d.teamSkillEvolutionRail = nil
    }
}

// 9. SkillCreateRail — 按 skill_create 配置注册/注销
skillCreateEnabled := getSkillCreateEnabled(d.configCache)
if skillCreateEnabled {
    if d.skillCreateRail == nil {
        rail := d.buildSkillCreateRail()
        if rail != nil {
            var ok bool
            d.skillCreateRail, ok = rail.(*evolution.TeamSkillCreateRail)
            if ok && d.skillCreateRail != nil && d.instance != nil {
                if err := d.instance.RegisterRail(ctx, d.skillCreateRail); err != nil {
                    logger.Error(logComponent).Err(err).Msg("Failed to register SkillCreateRail")
                } else {
                    logger.Info(logComponent).Msg("SkillCreateRail registered (plan mode)")
                }
            }
        }
    }
} else {
    if d.skillCreateRail != nil && d.instance != nil {
        if err := d.instance.UnregisterRail(ctx, d.skillCreateRail); err != nil {
            logger.Error(logComponent).Err(err).Msg("Failed to unregister SkillCreateRail")
        }
        d.skillCreateRail = nil
    }
}
```

添加辅助函数:
```go
// getSkillCreateEnabled 从配置缓存读取 skill_create 是否启用。
// 对齐 Python: _get_skill_create_enabled()
func getSkillCreateEnabled(configCache map[string]any) bool {
    evolutionConfig, _ := configCache["evolution"].(map[string]any)
    if enabled, ok := evolutionConfig["skill_create_enabled"].(bool); ok {
        return enabled
    }
    return false
}
```

- [x] **Step 2: 更新 updateAgentModeRails 中的 SkillCreateRail 注销**

确认 `updateAgentModeRails` 第 888-894 行的 SkillCreateRail 注销逻辑已正确处理 `*evolution.TeamSkillCreateRail` 类型（字段类型已从 `sainterfaces.AgentRail` 改为具体类型，UnregisterRail 调用应兼容）。

- [x] **Step 3: 运行编译确认**

Run: `go build ./internal/swarm/server/adapter/...`
Expected: 编译通过

- [x] **Step 4: 提交**

```bash
git add internal/swarm/server/adapter/deep_adapter_rails.go
git commit -m "feat(adapter): updateRailsForMode 按需注册 TeamSkillEvolutionRail + SkillCreateRail (RA-05)"
```

---

### Task 16：更新 IMPLEMENTATION_PLAN.md

**Files:**
- Modify: `IMPLEMENTATION_PLAN.md`

- [x] **Step 1: 更新 9.19-23 行**

在 9.19-23 行中，将 `⤵️ session memory 集成` 替换为 `✅ session memory 集成`。

- [x] **Step 2: 更新 9.24 P4 行**

在 9.24 行中，将 `⤵️ 10.6.3-10 Rail实例化/注入` 替换为 `✅ 10.6.3-10 Rail实例化/注入`。

- [x] **Step 3: 提交**

```bash
git add IMPLEMENTATION_PLAN.md
git commit -m "docs: 更新 9.19-23 session memory + 9.24 P4 Rail 注入状态为已完成"
```

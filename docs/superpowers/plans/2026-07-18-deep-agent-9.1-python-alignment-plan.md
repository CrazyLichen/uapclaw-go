# DeepAgent 9.1 Python 对齐修复实现计划

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 修复 Go DeepAgent 与 Python 参考实现的 18 处逻辑不一致，确保功能正确性。

**Architecture:** 按 P0 → P1 → P2 优先级分 8 个 Task 修复。每个 Task 修改 1-3 个文件，含测试验证。修改集中在 `deep_agent.go`、`task_loop/executor.go`、`task_loop/handler.go`、`rail/context.go` 四个文件。

**Tech Stack:** Go 1.22+, testify/assert, 项目现有测试框架

**Design Spec:** `docs/superpowers/specs/2026-07-18-deep-agent-9.1-python-alignment-design.md`

---

## File Structure

| 文件 | 职责 | 操作 |
|------|------|------|
| `internal/agentcore/harness/deep_agent.go` | DeepAgent 核心实现 | Modify |
| `internal/agentcore/harness/deep_agent_test.go` | DeepAgent 测试 | Modify |
| `internal/agentcore/harness/task_loop/executor.go` | 任务循环事件执行器 | Modify |
| `internal/agentcore/harness/task_loop/handler.go` | 任务循环事件处理器 | Modify |
| `internal/agentcore/harness/task_loop/handler_test.go` | handler 测试 | Modify |
| `internal/agentcore/single_agent/rail/context.go` | FireLifecycle 实现 | Modify |
| `internal/agentcore/single_agent/rail/context_test.go` | FireLifecycle 测试 | Modify |

---

### Task 1: runTaskLoop follow-up drain + 退出检查（P0 #1+#2）

**Files:**
- Modify: `internal/agentcore/harness/deep_agent.go:1737-1802`
- Modify: `internal/agentcore/harness/deep_agent_test.go`

- [ ] **Step 1: 补充 follow-up drain 完整逻辑**

在 `runTaskLoop` 中替换 L1739-1741：
```go
// 旧代码：
// 排空 follow-up（对齐 Python line 2026-2040）
_ = ctrl.DrainFollowUp()

// 新代码：
// 排空 follow-up，合并到 state 缓冲（对齐 Python line 2026-2040）
newFollowUps := ctrl.DrainFollowUp()
st := d.LoadState(sess)
if len(newFollowUps) > 0 {
    st.PendingFollowUps = append(st.PendingFollowUps, newFollowUps...)
}
isFollowUp := len(st.PendingFollowUps) > 0
if isFollowUp {
    currentQuery = rail.InvokeQueryString(st.PendingFollowUps[0])
    st.PendingFollowUps = st.PendingFollowUps[1:]
    d.saveState(sess, st)
}
```

- [ ] **Step 2: 修改 SubmitRound 传 isFollowUp**

替换 L1748 的 `false` 为 `isFollowUp`：
```go
// 旧：
if err = ctrl.SubmitRound(ctx, sess, string(currentQuery.PlainText()), false, isStreaming, modified.RunKind, modified.RunContext); err != nil {

// 新：
if err = ctrl.SubmitRound(ctx, sess, string(currentQuery.PlainText()), isFollowUp, isStreaming, modified.RunKind, modified.RunContext); err != nil {
```

- [ ] **Step 3: 补充退出检查**

在 L1800（`if coord.IsAborted()` 块之后）加入：
```go
if ctrl.HasFollowUp() || len(st.PendingFollowUps) > 0 {
    continue
}
if !d.hasRemainingTasks(sess) {
    logLoop("no remaining tasks, loop finished", "", 0)
    break
}
```

注意：此处 `st` 需要重新加载（因为之前 increment 后的 state 更新可能改变了 pending_follow_ups）。加入退出检查前先刷新：
```go
st = d.LoadState(sess)
```

- [ ] **Step 4: 补充 stop_reason 日志（#18）**

在 for 循环结束后（`currentQuery = modified.Query` 之后，goroutine defer 之前）加入：
```go
stopReason := coord.StopReason()
if stopReason != "" {
    logLoop("loop stopped by: %s", "", stopReason)
}
```

- [ ] **Step 5: 删除 result_type=="error" 死代码（#16 部分）**

删除 L1754-1760 的错误检查块：
```go
// 删除：
resultType, _ := result["result_type"].(string)
if resultType == "error" {
    errOutput, _ := result["output"].(string)
    err = fmt.Errorf("轮次完成返回错误: %s", errOutput)
    logger.Error(logComponent).Err(err).Int("round", outerRound).Msg("等待轮次完成失败")
    break
}
```

- [ ] **Step 6: 补充 result_type 默认值和 stop_reason 日志（#17+#18）**

在 WaitRoundCompletion 之后，补充 result_type 取值逻辑：
```go
result := ctrl.WaitRoundCompletion(ctx, &timeout)
resultType, _ := result["result_type"].(string)
if resultType == "" {
    resultType = "N/A"
}
```

- [ ] **Step 7: 编译验证**

Run: `cd /home/opensource/uap-claw-go && go build ./internal/agentcore/harness/...`

- [ ] **Step 8: 运行现有测试**

Run: `cd /home/opensource/uap-claw-go && go test ./internal/agentcore/harness/... -count=1 -timeout 120s`

- [ ] **Step 9: 补充 follow-up drain 测试**

在 `deep_agent_test.go` 中添加测试验证 follow-up drain 完整流程、退出条件组合。

- [ ] **Step 10: Commit**

```
feat(harness): runTaskLoop 补全 follow-up drain、退出检查、stop_reason 日志
```

---

### Task 2: FollowUp() 方法体实现（P0 #3）

**Files:**
- Modify: `internal/agentcore/harness/deep_agent.go:794-816`
- Modify: `internal/agentcore/harness/deep_agent_test.go`

- [ ] **Step 1: 实现 FollowUp 核心逻辑**

替换 L794-816 中 FollowUp 方法体：
```go
func (d *DeepAgent) FollowUp(ctx context.Context, msg string, taskID string, sess *session.Session) {
	d.configMu.RLock()
	ctrl := d.loopController
	loopSess := d.loopSession
	d.configMu.RUnlock()

	if ctrl == nil {
		return
	}
	sessToUse := sess
	if sessToUse == nil {
		sessToUse = loopSess
	}
	if sessToUse == nil {
		return
	}

	// 对齐 Python: FollowUpEvent.from_text(msg)
	event := cschema.FromText(msg)
	if taskID != "" {
		event.SetMetadata(map[string]any{"task_id": taskID})
	}

	// 对齐 Python: controller.event_queue.publish_event_async(card.id, sess, event)
	if err := ctrl.PublishEventAsync(ctx, d.card.ID, sessToUse, event); err != nil {
		logger.Error(logComponent).Err(err).Str("msg", msg).Msg("FollowUp 发布事件失败")
	}
}
```

移除 `⤵️ 9.6 回填` 标记。

- [ ] **Step 2: 编译验证**

Run: `cd /home/opensource/uap-claw-go && go build ./internal/agentcore/harness/...`

- [ ] **Step 3: 运行测试**

Run: `cd /home/opensource/uap-claw-go && go test ./internal/agentcore/harness/... -count=1 -timeout 120s`

- [ ] **Step 4: 补充 FollowUp 测试**

在 `deep_agent_test.go` 中添加测试验证 FollowUp 事件发布。

- [ ] **Step 5: Commit**

```
feat(harness): 实现 FollowUp 方法体，通过 PublishEventAsync 发布事件
```

---

### Task 3: FireLifecycle ctx 参数 + AFTER 错误处理 + 取消保护（P1 #4, P2 #10）

**Files:**
- Modify: `internal/agentcore/single_agent/rail/context.go` FireLifecycle 方法
- Modify: `internal/agentcore/single_agent/rail/context_test.go`
- Modify: `internal/agentcore/single_agent/agents/react_invoke.go` — 调用方传 ctx
- Modify: `internal/agentcore/single_agent/agents/react_stream.go` — 调用方传 ctx
- 搜索其他 `FireLifecycle` 调用方并更新

- [ ] **Step 1: 搜索所有 FireLifecycle 调用方**

Run: `cd /home/opensource/uap-claw-go && grep -rn "FireLifecycle" --include="*.go" internal/`

- [ ] **Step 2: 修改 FireLifecycle 签名和实现**

修改 `rail/context.go`：
```go
// 旧签名：
// func (c *AgentCallbackContext) FireLifecycle(before, after AgentCallbackEvent, fn func() error) error

// 新签名：
func (c *AgentCallbackContext) FireLifecycle(ctx context.Context, before, after AgentCallbackEvent, fn func() error) error {
    savedInputs := c.inputs
    if err := c.Fire(before); err != nil {
        return err
    }
    var origErr error
    err := fn()
    if err != nil {
        origErr = err
        c.exception = err
    }
    c.inputs = savedInputs

    // context 已取消 → 跳过 after 事件（对齐 Python CancelledError 保护）
    if ctx.Err() != nil {
        if origErr != nil {
            return origErr
        }
        return nil
    }

    // 触发 after 钩子，对齐 Python lifecycle() 错误处理
    afterErr := c.Fire(after)
    if afterErr != nil {
        if origErr != nil {
            // after 回调出错但有原始异常 → log 不掩盖
            logger.Error(logger.ComponentAgentCore).
                Str("event", string(after)).
                Err(afterErr).
                Msg("after 回调出错，掩盖原始异常")
            return origErr
        }
        // after 回调出错且无原始异常 → 返回 after 错误
        return afterErr
    }

    if origErr != nil {
        return origErr
    }
    return nil
}
```

- [ ] **Step 3: 更新所有调用方传入 ctx**

每个调用方将 `cbc.FireLifecycle(before, after, fn)` 改为 `cbc.FireLifecycle(ctx, before, after, fn)`。

- [ ] **Step 4: 编译验证**

Run: `cd /home/opensource/uap-claw-go && go build ./internal/agentcore/... -timeout 300s`

- [ ] **Step 5: 运行测试**

Run: `cd /home/opensource/uap-claw-go && go test ./internal/agentcore/single_agent/rail/... -count=1 -timeout 120s`

- [ ] **Step 6: 补充 FireLifecycle 测试**

测试 AFTER 错误传播、ctx 取消跳过 AFTER、有原始异常时 AFTER 错误被 log 但不掩盖。

- [ ] **Step 7: Commit**

```
fix(rail): FireLifecycle 增加 ctx 参数，修复 AFTER 错误处理和取消保护
```

---

### Task 4: normalizeInputs 修复（P1 #6+#7）

**Files:**
- Modify: `internal/agentcore/harness/deep_agent.go` normalizeInputs 方法
- Modify: `internal/agentcore/harness/deep_agent_test.go`

- [ ] **Step 1: 修复 run.kind 默认值**

在 normalizeInputs 中，替换 run.kind 提取逻辑：
```go
// 旧：
if kind, kOk := run["kind"].(string); kOk {
    runKind = rail.RunKind(kind)
}

// 新：
if kind, kOk := run["kind"].(string); kOk && kind != "" {
    runKind = rail.RunKind(kind)
} else {
    runKind = rail.RunKindNormal
}
```

- [ ] **Step 2: 修复不支持类型处理**

在 normalizeInputs 的 default 分支，替换容错转换为返回错误：
```go
// 旧：
return &rail.InvokeInputs{Query: rail.InvokeQueryString(fmt.Sprintf("%v", v))}

// 新：
return nil, exception.BuildError(exception.StatusDeepagentContextParamError,
    exception.WithMsg(fmt.Sprintf("不支持的输入类型: %T，必须是 string、[]Message、map 或 InvokeInputs", v)))
```

- [ ] **Step 3: hotReconfigure 差异注释（#5）**

在 `hotReconfigure` 方法末尾加入注释：
```go
// 注：Python 在 hotReconfigure 后调用 _sync_builder_to_active_rails() 将新 builder
// 同步到已注册 rail。Go 的策略不同：agent 通过 SystemPromptBuilder() 接口返回最新
// builder，rail 调用接口获取，无需显式同步。若后续 rail 体系需要持有 builder 引用，
// 再补充 syncBuilderToActiveRails 逻辑。
```

- [ ] **Step 4: 编译验证 + 运行测试**

Run: `cd /home/opensource/uap-claw-go && go test ./internal/agentcore/harness/... -count=1 -timeout 120s`

- [ ] **Step 5: Commit**

```
fix(harness): normalizeInputs run.kind 默认 normal、不支持类型返回错误、hotReconfigure 注释
```

---

### Task 5: executor BaseAgent 断言 + handler run_context 类型断言（P1 #13+#14）

**Files:**
- Modify: `internal/agentcore/harness/task_loop/executor.go:159-168`
- Modify: `internal/agentcore/harness/task_loop/handler.go:139-148,203-205`

- [ ] **Step 1: executor provider 断言成 BaseAgent**

替换 `executor.go` L159-168：
```go
// 旧：
// ⤵️ 9.1 回填：agent 参数传 nil，Fire 为空操作
cbCtx := agentinterfaces.NewAgentCallbackContext(nil, iterInputs, sess)

// 新：
var baseAgent agentinterfaces.BaseAgent
if ba, ok := e.provider.(agentinterfaces.BaseAgent); ok {
    baseAgent = ba
} else {
    logger.Warn(logComponent).Msg("provider 不满足 BaseAgent 接口，回调将不触发")
}
cbCtx := agentinterfaces.NewAgentCallbackContext(baseAgent, iterInputs, sess)
```

- [ ] **Step 2: 修复 handler run_context 类型断言**

替换 `handler.go` L139-148 中变量声明和断言：
```go
// 旧：
var taskID, runKind, runContext string
if v, ok := metadata["task_id"]; ok {
    taskID, _ = v.(string)
}
if v, ok := metadata["run_kind"]; ok {
    runKind, _ = v.(string)
}
if v, ok := metadata["run_context"]; ok {
    runContext, _ = v.(string)  // ❌ 错误断言
}

// 新：
var taskID string
var runKind string
var runContext *agentinterfaces.RunContext
if v, ok := metadata["task_id"]; ok {
    taskID, _ = v.(string)
}
if v, ok := metadata["run_kind"]; ok {
    runKind, _ = v.(string)
}
if v, ok := metadata["run_context"]; ok {
    runContext, _ = v.(*agentinterfaces.RunContext)
}
```

替换 `handler.go` L203-205 写入 coreTask.Metadata：
```go
// 旧：
if runContext != "" {
    coreTask.Metadata["run_context"] = runContext
}

// 新：
if runContext != nil {
    coreTask.Metadata["run_context"] = runContext
}
```

- [ ] **Step 3: 移除 ⤵️ 回填标记**

移除 `executor.go` L159 的 `⤵️ 9.1 回填` 注释，移除 `handler.go` L29 的 `⤵️ 9.1 回填` 注释。

- [ ] **Step 4: 编译验证 + 运行测试**

Run: `cd /home/opensource/uap-claw-go && go test ./internal/agentcore/harness/task_loop/... -count=1 -timeout 120s`

- [ ] **Step 5: Commit**

```
fix(task_loop): executor BaseAgent 断言、handler run_context 类型断言修复
```

---

### Task 6: WaitCompletion 对齐 Python（P2 #15+#16 部分）

**Files:**
- Modify: `internal/agentcore/harness/task_loop/handler.go` WaitCompletion 方法

- [ ] **Step 1: 修复 WaitCompletion 返回值**

在 `WaitCompletion` 方法中：
1. 超时返回加 `result_type`：
```go
// 旧：
return map[string]any{"status": "timeout"}

// 新：
return map[string]any{"status": "timeout", "result_type": "error"}
```

2. 取消返回加 `result_type`：
```go
// 旧：
return map[string]any{"status": "cancelled", "error": ctx.Err().Error()}

// 新：
return map[string]any{"status": "cancelled", "result_type": "error", "error": ctx.Err().Error()}
```

3. 正常路径：空结果替换 + 更新 lastResult：
```go
// 在每个 return result 之前加入：
if result == nil || len(result) == 0 {
    result = map[string]any{"status": "completed"}
}
h.mu.Lock()
h.lastResult = result
h.mu.Unlock()
return result
```

- [ ] **Step 2: 编译验证 + 运行测试**

Run: `cd /home/opensource/uap-claw-go && go test ./internal/agentcore/harness/task_loop/... -count=1 -timeout 120s`

- [ ] **Step 3: Commit**

```
fix(task_loop): WaitCompletion 对齐 Python，空结果替换、更新 lastResult、补 result_type
```

---

### Task 7: runTaskLoopStream context 取消处理（P2 #9）

**Files:**
- Modify: `internal/agentcore/harness/deep_agent.go` runTaskLoopStream 方法

- [ ] **Step 1: 用 WithCancel 派生子 context**

修改 `runTaskLoopStream` 方法：
```go
func (d *DeepAgent) runTaskLoopStream(ctx context.Context, ...) (<-chan stream.Schema, error) {
    // ...
    
    // 派生子 context，用于取消后台 goroutine
    streamCtx, streamCancel := context.WithCancel(ctx)
    d.streamMu.Lock()
    d.streamCancel = streamCancel
    d.streamMu.Unlock()

    // 后台 goroutine 使用 streamCtx
    go func() {
        defer func() {
            d.streamMu.Lock()
            d.streamCancel = nil
            d.streamMu.Unlock()
            _ = sessConcrete.CloseStream()
        }()

        loopCh, loopErr := d.runTaskLoop(streamCtx, cbc, sessConcrete, true)
        // ... 后续使用 streamCtx
    }()

    // 前台 goroutine：读取 stream 时，退出后取消后台
    go func() {
        defer close(outCh)
        defer d.cancelStreamProcess() // 前台退出时取消后台
        for chunk := range sessConcrete.StreamIterator() {
            select {
            case outCh <- chunk:
            case <-ctx.Done():
                return
            }
        }
    }()

    return outCh, nil
}
```

- [ ] **Step 2: 编译验证 + 运行测试**

Run: `cd /home/opensource/uap-claw-go && go test ./internal/agentcore/harness/... -count=1 -timeout 120s`

- [ ] **Step 3: Commit**

```
fix(harness): runTaskLoopStream 用 WithCancel 派生子 context，确保取消时后台 goroutine 退出
```

---

### Task 8: StripRailsByType 计数逻辑修复（P2 #12）

**Files:**
- Modify: `internal/agentcore/harness/deep_agent.go:700-733`
- Modify: `internal/agentcore/harness/deep_agent_test.go`

- [ ] **Step 1: 修复 StripRailsByType 计数逻辑**

替换 `StripRailsByType` 方法中 pending 移除逻辑：
```go
// 旧：
removed := 0
before := len(d.pendingRails)
var newPending []agentinterfaces.AgentRail
for _, r := range d.pendingRails {
    if matchType(r, railTypes) {
        removed++
    } else {
        newPending = append(newPending, r)
    }
}
d.pendingRails = newPending
removed -= (before - len(d.pendingRails) - (before - len(newPending)))
removed = before - len(newPending)

// 新：
removed := 0
before := len(d.pendingRails)
var newPending []agentinterfaces.AgentRail
for _, r := range d.pendingRails {
    if !matchType(r, railTypes) {
        newPending = append(newPending, r)
    }
}
d.pendingRails = newPending
removed = before - len(newPending)
```

- [ ] **Step 2: 编译验证 + 运行测试**

Run: `cd /home/opensource/uap-claw-go && go test ./internal/agentcore/harness/... -count=1 -timeout 120s`

- [ ] **Step 3: Commit**

```
fix(harness): StripRailsByType 对齐 Python 差值计算，删除冗余行
```

---

## 自检清单

### Spec 覆盖

| Spec # | Task | 状态 |
|--------|------|------|
| 1 | Task 1 | ✅ |
| 2 | Task 1 | ✅ |
| 3 | Task 2 | ✅ |
| 4 | Task 3 | ✅ |
| 5 | Task 4 | ✅ |
| 6 | Task 4 | ✅ |
| 7 | Task 4 | ✅ |
| 8 | N/A | ✅ 无需修复 |
| 9 | Task 7 | ✅ |
| 10 | Task 3 | ✅ |
| 11 | N/A | ✅ 暂不修复 |
| 12 | Task 8 | ✅ |
| 13 | Task 5 | ✅ |
| 14 | Task 5 | ✅ |
| 15 | Task 6 | ✅ |
| 16 | Task 1 + Task 6 | ✅ |
| 17 | Task 1 | ✅ |
| 18 | Task 1 | ✅ |

### Placeholder 扫描

无 TBD/TODO 占位符。

### 类型一致性

- `isFollowUp` 类型为 `bool`，与 `SubmitRound` 参数一致
- `runContext` 类型从 `string` 改为 `*agentinterfaces.RunContext`，与 `SubmitRound` 存入类型一致
- `FireLifecycle` 新签名 `ctx context.Context` 参数，与 `RailExecutor.Execute` 风格一致

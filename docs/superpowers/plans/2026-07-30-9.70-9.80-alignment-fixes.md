# 9.70-9.80 对齐修复实现计划

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 修复 9.70-9.80 章节中对齐 Python 的 7 个已确认问题，包括补齐 9.77 Trajectory 缺失模块、修复 Trainer ResumeIfNeeded 回填不完整、Updater 返回类型改为切片支持候选列表等。

**Architecture:** 7 个问题按依赖关系分为 4 个批次：批次1（9.77 Trajectory 5个新模块）独立于其他问题；批次2（Updater 返回类型切片化）是 Trainer 候选列表分支和 ResumeIfNeeded 修复的前置；批次3（Trainer 两处修复）依赖批次2；批次4（小修复：signal/doc.go/IMPLEMENTATION_PLAN）独立于其他批次。

**Tech Stack:** Go 1.22+, 标准库 + 项目内已有依赖

---

## Task 1: 9.77 TrajectoryBuilder

**Files:**
- Create: `internal/evolving/trajectory/builder.go`
- Create: `internal/evolving/trajectory/builder_test.go`
- Reference: `/home/opensource/agent-core/openjiuwen/agent_evolving/trajectory/builder.py` (118行)

- [ ] **Step 1: 写 builder.go**

```go
package trajectory

import (
	"fmt"

	"github.com/google/uuid"
)

// ──────────────────────────── 结构体 ────────────────────────────

// TrajectoryBuilder 轨迹组装器，用于在线和离线路径的轨迹构建。
//
// 职责：
//   - 步骤累积（按插入顺序）
//   - Cost 累积（input_tokens/output_tokens）
//   - 最终 Trajectory 组装
//
// 对应 Python: openjiuwen/agent_evolving/trajectory/builder.py TrajectoryBuilder
type TrajectoryBuilder struct {
	// sessionID 会话标识
	sessionID string
	// source 执行来源："online" 或 "offline"
	source string
	// caseID 离线模式下的用例标识（可选）
	caseID string
	// memberID 团队成员标识（可选）
	memberID string
	// maxSteps 最大保留步骤数（可选，nil 表示不限制）
	maxSteps *int
	// meta 扩展元数据
	meta map[string]any
	// steps 已累积的步骤
	steps []*TrajectoryStep
	// cost 聚合成本指标
	cost map[string]int
	// startTimeMs 首个步骤的开始时间（毫秒）
	startTimeMs *int
}

// TrajectoryBuilderOption TrajectoryBuilder 构造选项函数。
type TrajectoryBuilderOption func(*TrajectoryBuilder)

// ──────────────────────────── 枚举 ────────────────────────────

// ──────────────────────────── 常量 ────────────────────────────

// ──────────────────────────── 全局变量 ────────────────────────────

// ──────────────────────────── 导出函数 ────────────────────────────

// NewTrajectoryBuilder 创建 TrajectoryBuilder 实例。
//
// 对应 Python: TrajectoryBuilder(session_id, source, case_id, member_id, meta, max_steps)
func NewTrajectoryBuilder(sessionID, source string, opts ...TrajectoryBuilderOption) *TrajectoryBuilder {
	b := &TrajectoryBuilder{
		sessionID: sessionID,
		source:    source,
		meta:      map[string]any{},
		steps:     []*TrajectoryStep{},
		cost:      map[string]int{"input_tokens": 0, "output_tokens": 0},
	}
	for _, opt := range opts {
		opt(b)
	}
	return b
}

// WithCaseID 设置离线模式下的用例标识。
func WithCaseID(caseID string) TrajectoryBuilderOption {
	return func(b *TrajectoryBuilder) { b.caseID = caseID }
}

// WithMemberID 设置团队成员标识。
func WithMemberID(memberID string) TrajectoryBuilderOption {
	return func(b *TrajectoryBuilder) {
		b.memberID = memberID
		if b.meta == nil {
			b.meta = map[string]any{}
		}
		if _, exists := b.meta["member_id"]; !exists {
			b.meta["member_id"] = memberID
		}
	}
}

// WithMeta 设置扩展元数据。
func WithMeta(meta map[string]any) TrajectoryBuilderOption {
	return func(b *TrajectoryBuilder) {
		if meta != nil {
			b.meta = meta
		}
	}
}

// WithMaxSteps 设置最大保留步骤数。
// maxSteps 必须 >= 1，否则返回 error。
func WithMaxSteps(maxSteps int) TrajectoryBuilderOption {
	return func(b *TrajectoryBuilder) {
		if maxSteps >= 1 {
			b.maxSteps = &maxSteps
		}
	}
}

// RecordStep 记录一个步骤并累积成本。
//
// 对应 Python: TrajectoryBuilder.record_step(step)
func (b *TrajectoryBuilder) RecordStep(step *TrajectoryStep) {
	b.steps = append(b.steps, step)

	// maxSteps 滑窗截断
	if b.maxSteps != nil && len(b.steps) > *b.maxSteps {
		b.steps = b.steps[len(b.steps)-*b.maxSteps:]
	}

	// 从 LLM 步骤累积 token 使用量
	if step.Kind == StepKindLLM {
		if detail, ok := step.Detail.(*LLMCallDetail); ok && detail != nil {
			if detail.Usage != nil {
				if pt, ok := detail.Usage["prompt_tokens"]; ok {
					b.cost["input_tokens"] += toInt(pt)
				}
				if ct, ok := detail.Usage["completion_tokens"]; ok {
					b.cost["output_tokens"] += toInt(ct)
				}
			}
		}
	}

	// 记录首个步骤的开始时间
	if b.startTimeMs == nil && step.StartTimeMs != 0 {
		ms := step.StartTimeMs
		b.startTimeMs = &ms
	}
}

// Build 组装最终 Trajectory。
//
// 对应 Python: TrajectoryBuilder.build()
func (b *TrajectoryBuilder) Build() *Trajectory {
	meta := map[string]any{}
	for k, v := range b.meta {
		meta[k] = v
	}
	if b.memberID != "" {
		meta["member_id"] = b.memberID
	}

	var cost CostInfo
	if b.cost["input_tokens"] > 0 || b.cost["output_tokens"] > 0 {
		cost = map[string]int{
			"input_tokens":  b.cost["input_tokens"],
			"output_tokens": b.cost["output_tokens"],
		}
	}

	return &Trajectory{
		ExecutionID: generateUUID(),
		SessionID:   b.sessionID,
		Source:      b.source,
		CaseID:      b.caseID,
		Steps:       b.steps,
		Cost:        cost,
		Meta:        meta,
	}
}

// ──────────────────────────── 非导出函数 ────────────────────────────

// generateUUID 生成唯一执行标识符。
//
// 对应 Python: _generate_uuid() -> str(uuid.uuid4())
func generateUUID() string {
	return uuid.New().String()
}

// toInt 安全地将 any 转换为 int。
func toInt(v any) int {
	switch n := v.(type) {
	case int:
		return n
	case int64:
		return int(n)
	case float64:
		return int(n)
	case json_number:
		if i, err := n.Int64(); err == nil {
			return int(i)
		}
		return 0
	default:
		return 0
	}
}
```

注意：`json_number` 类型需要 import `encoding/json`，`toInt` 中 `json.Number` 的引用。如果 `toInt` 不需要处理 `json.Number`，可以简化为只处理 `int/int64/float64`。

- [ ] **Step 2: 写 builder_test.go**

测试覆盖：
- NewTrajectoryBuilder 基本构造
- WithCaseID/WithMemberID/WithMaxSteps/WithMeta 选项
- RecordStep 累积 + maxSteps 滑窗
- RecordStep LLM cost 累积
- Build 组装（cost 为零时为 nil）
- WithMaxSteps < 1 时忽略（不 panic）

- [ ] **Step 3: 运行测试确认通过**

```bash
cd /home/opensource/uap-claw-go && go test ./internal/evolving/trajectory/ -run TestTrajectoryBuilder -v
```

- [ ] **Step 4: 提交**

```bash
git add internal/evolving/trajectory/builder.go internal/evolving/trajectory/builder_test.go
git commit -m "feat(evolving/trajectory): 实现 TrajectoryBuilder 对齐 Python builder.py"
```

---

## Task 2: 9.77 TracerTrajectoryExtractor 完整实现

**Files:**
- Modify: `internal/evolving/trajectory/extractor.go`（从4行接口扩展为完整实现）
- Create: `internal/evolving/trajectory/extractor_test.go`
- Reference: `/home/opensource/agent-core/openjiuwen/agent_evolving/trajectory/extractor.py` (271行)

- [ ] **Step 1: 重写 extractor.go**

在保留 `TrajectoryExtractor` 接口的基础上，新增 `TracerTrajectoryExtractor` 具体实现。

核心方法：
- `NewTracerTrajectoryExtractor(resourceManager)` 构造函数
- `Extract(sess, caseID)` → `*Trajectory`：从 Session.tracer() 获取 spans → 逐个 buildStep → TrajectoryBuilder 组装
- `buildStep(span)` → `*TrajectoryStep`：classifyKind + buildDetail + buildMeta
- `buildLLMDetail(span)` → `*LLMCallDetail`：从 on_invoke_data 提取 llm_params
- `buildToolDetail(span)` → `*ToolCallDetail`：从 resourceManager 获取 tool 信息
- `buildMeta(span, baseMeta, kind, detail)` → `map[string]any`
- `classifyKind(span)` → StepKind：plugin→tool, llm/workflow/memory→各自, 默认→agent
- `getOperatorID(span, meta)` → string：优先级链
- `extractInputs/extractOutputs`：解包嵌套

**关键适配**：Python 的 span 用 `getattr` 动态访问，Go 中 span 需要定义一个 `Span` 接口或用 `map[string]any` 表示。查看 `session.Tracer()` 返回类型决定。

先确认 `session.Session.Tracer()` 返回类型和 `tracer` 包的 Span 结构：

```bash
grep -rn "func.*Tracer()" internal/agentcore/session/ | head -5
grep -rn "type.*Span struct" internal/agentcore/ | head -5
```

根据实际返回类型决定 extractor 的 span 参数类型。如果 tracer 包的 Span 是结构体，直接用字段访问；如果是接口，用方法调用。

- [ ] **Step 2: 写 extractor_test.go**

使用 mock Session/Tracer/Span 测试完整提取流程。覆盖：
- 空 tracer 返回空轨迹
- LLM 步骤提取（on_invoke_data → LLMCallDetail）
- Tool 步骤提取（resourceManager → ToolCallDetail）
- classifyKind 各分支
- getOperatorID 优先级链
- extractInputs/extractOutputs 嵌套解包

- [ ] **Step 3: 运行测试确认通过**

```bash
cd /home/opensource/uap-claw-go && go test ./internal/evolving/trajectory/ -run TestTracerTrajectoryExtractor -v
```

- [ ] **Step 4: 提交**

```bash
git add internal/evolving/trajectory/extractor.go internal/evolving/trajectory/extractor_test.go
git commit -m "feat(evolving/trajectory): 实现 TracerTrajectoryExtractor 对齐 Python extractor.py"
```

---

## Task 3: 9.77 TrajectoryStore + InMemoryTrajectoryStore + FileTrajectoryStore

**Files:**
- Create: `internal/evolving/trajectory/store.go`
- Create: `internal/evolving/trajectory/store_test.go`
- Reference: `/home/opensource/agent-core/openjiuwen/agent_evolving/trajectory/store.py` (265行)

- [ ] **Step 1: 写 store.go**

核心内容：
- `TrajectoryStore` 接口：`Save(trajectory, version)` / `Load(executionID, version)` / `Query(version, filters...)`
- `InMemoryTrajectoryStore`：`map[string]map[string]*Trajectory`，Query 用字段匹配
- `FileTrajectoryStore`：JSONL 文件存储，`trajectoryToDict` / `dictToTrajectory` 互转

- [ ] **Step 2: 写 store_test.go**

覆盖：
- InMemoryTrajectoryStore: Save/Load/Query/多版本隔离
- FileTrajectoryStore: Save/Load/Query（用 t.TempDir()）

- [ ] **Step 3: 运行测试确认通过**

```bash
cd /home/opensource/uap-claw-go && go test ./internal/evolving/trajectory/ -run TestTrajectoryStore -v
```

- [ ] **Step 4: 提交**

```bash
git add internal/evolving/trajectory/store.go internal/evolving/trajectory/store_test.go
git commit -m "feat(evolving/trajectory): 实现 TrajectoryStore/InMemory/File 对齐 Python store.py"
```

---

## Task 4: 9.77 TeamTrajectoryAggregator + aggregate/filter

**Files:**
- Create: `internal/evolving/trajectory/aggregator.go`
- Create: `internal/evolving/trajectory/aggregator_test.go`
- Reference: `/home/opensource/agent-core/openjiuwen/agent_evolving/trajectory/aggregator.py` (299行)

- [ ] **Step 1: 写 aggregator.go**

核心内容：
- `TeamTrajectory` 结构体：teamID/sessionID/combined/members
- `TeamTrajectoryAggregator` 结构体：store + teamID，aggregate 方法
- `AggregateMemberTrajectories()` 包级导出函数
- `FilterMemberTrajectory()` 包级导出函数
- `CollaborativeTools` 包级变量（`map[string]bool`）
- `isCollaborativeStep` / `isTeamSkillFileAccess` / `isLeaderTrajectory`
- `buildCombinedTrajectory` / `mergeMemberTrajectory` / `stepsArePrefix` / `mergeCost`

- [ ] **Step 2: 写 aggregator_test.go**

覆盖：
- AggregateMemberTrajectories: 多成员合并 + 按时间排序
- FilterMemberTrajectory: 协作步骤过滤
- CollaborativeTools 常量
- mergeMemberTrajectory: 前缀检测 + 追加
- stepsArePrefix
- mergeCost
- isLeaderTrajectory
- TeamTrajectoryAggregator: aggregate + empty_combined

- [ ] **Step 3: 运行测试确认通过**

```bash
cd /home/opensource/uap-claw-go && go test ./internal/evolving/trajectory/ -run TestAggregator -v
```

- [ ] **Step 4: 提交**

```bash
git add internal/evolving/trajectory/aggregator.go internal/evolving/trajectory/aggregator_test.go
git commit -m "feat(evolving/trajectory): 实现 TeamTrajectoryAggregator 对齐 Python aggregator.py"
```

---

## Task 5: 9.77 TrajectorySink/Source + InMemoryTrajectoryRegistry

**Files:**
- Create: `internal/evolving/trajectory/registry.go`
- Create: `internal/evolving/trajectory/registry_test.go`
- Reference: `/home/opensource/agent-core/openjiuwen/agent_evolving/trajectory/registry.py` (153行)

- [ ] **Step 1: 写 registry.go**

核心内容：
- `TrajectorySink` 接口：`PublishMemberTrajectory(snapshot)`
- `TrajectorySource` 接口：`GetTrajectory(teamID, sessionID, filterCollaborative) → *Trajectory`
- `MemberTrajectorySnapshot` 结构体 + `NewMemberTrajectorySnapshot` 工厂
- `InMemoryTrajectoryRegistry`：同时实现 Sink + Source
- `snapshotEntry` 内部结构体（sequence 排序）
- `shouldKeepCurrent`：recordedAtMs 降序 + sequence 降序
- `nowMs()`：`time.Now().UnixMilli()`
- `ClearSession` 方法

- [ ] **Step 2: 写 registry_test.go**

覆盖：
- InMemoryTrajectoryRegistry: Publish/Get/ClearSession
- shouldKeepCurrent: 时间戳/sequence 排序
- MemberTrajectorySnapshot: Make 工厂
- 并发安全（goroutine 并发 publish）

- [ ] **Step 3: 运行测试确认通过**

```bash
cd /home/opensource/uap-claw-go && go test ./internal/evolving/trajectory/ -run TestRegistry -v
```

- [ ] **Step 4: 提交**

```bash
git add internal/evolving/trajectory/registry.go internal/evolving/trajectory/registry_test.go
git commit -m "feat(evolving/trajectory): 实现 TrajectorySink/Source/InMemoryTrajectoryRegistry 对齐 Python registry.py"
```

---

## Task 6: 9.77 更新 trajectory/doc.go + StepKind 补充

**Files:**
- Modify: `internal/evolving/trajectory/doc.go`
- Modify: `internal/evolving/trajectory/types.go`（补充 StepKind 枚举值：workflow/memory/agent）

- [ ] **Step 1: 更新 doc.go 文件目录**

在 doc.go 中添加新文件条目：builder.go / extractor.go(完整实现) / registry.go / aggregator.go / store.go

- [ ] **Step 2: 补充 StepKind 枚举值**

Python `extractor.py` 中 `_classify_kind` 返回 `"tool"` / `"llm"` / `"workflow"` / `"memory"` / `"agent"`，当前 Go types.go 只有 `StepKindLLM` 和 `StepKindTool`，需要补充：

```go
const (
	StepKindLLM      StepKind = "llm"
	StepKindTool     StepKind = "tool"
	StepKindWorkflow StepKind = "workflow"
	StepKindMemory   StepKind = "memory"
	StepKindAgent    StepKind = "agent"
)
```

- [ ] **Step 3: 运行全部 trajectory 测试**

```bash
cd /home/opensource/uap-claw-go && go test ./internal/evolving/trajectory/ -v
```

- [ ] **Step 4: 提交**

```bash
git add internal/evolving/trajectory/doc.go internal/evolving/trajectory/types.go
git commit -m "feat(evolving/trajectory): 更新 doc.go 文件目录 + 补充 StepKind 枚举值"
```

---

## Task 7: Updater 返回类型改为切片

**Files:**
- Modify: `internal/evolving/updater/protocol.go`（Update/Process 返回 `[]map[UpdateKey]any`）
- Modify: `internal/evolving/updater/single_dim/single_dim.go`
- Modify: `internal/evolving/updater/multi_dim/multi_dim.go`
- Modify: `internal/evolving/trainer/trainer.go`（normalizeUpdates + Train 循环 + Forward）

- [ ] **Step 1: 修改 updater/protocol.go**

```go
// 修改前
Update(ctx context.Context, trajectories []*trajectory.Trajectory, evaluatedCases []*dataset.EvaluatedCase, config map[string]any) (map[schema.UpdateKey]any, error)
Process(ctx context.Context, trajectories []*trajectory.Trajectory, signals []*signal.EvolutionSignal, config map[string]any) (map[schema.UpdateKey]any, error)

// 修改后
Update(ctx context.Context, trajectories []*trajectory.Trajectory, evaluatedCases []*dataset.EvaluatedCase, config map[string]any) ([]map[schema.UpdateKey]any, error)
Process(ctx context.Context, trajectories []*trajectory.Trajectory, signals []*signal.EvolutionSignal, config map[string]any) ([]map[schema.UpdateKey]any, error)
```

同时删除两处 ⤵️ 差异标注注释（第33-35行和第39-40行）。

- [ ] **Step 2: 修改 single_dim/single_dim.go**

`Process` 和 `Update` 返回值包裹成切片：

```go
// Process 修改
func (u *SingleDimUpdater) Process(ctx context.Context, ...) ([]map[schema.UpdateKey]any, error) {
    // ... 现有逻辑 ...
    result := u.opt.Step()
    return []map[schema.UpdateKey]any{result}, nil
}

// Update 修改
func (u *SingleDimUpdater) Update(ctx context.Context, ...) ([]map[schema.UpdateKey]any, error) {
    // ... 现有逻辑 ...
    return u.Process(ctx, trajectories, signals, config)
}
```

- [ ] **Step 3: 修改 multi_dim/multi_dim.go**

```go
// Process 修改
func (u *MultiDimUpdater) Process(ctx context.Context, ...) ([]map[schema.UpdateKey]any, error) {
    return []map[schema.UpdateKey]any{{}}, nil
}

// Update 修改
func (u *MultiDimUpdater) Update(ctx context.Context, ...) ([]map[schema.UpdateKey]any, error) {
    // ... 现有逻辑 ...
    return u.Process(ctx, trajectories, signals, config)
}
```

- [ ] **Step 4: 修改 trainer/trainer.go**

4a. 修改 `normalizeUpdates` 签名：

```go
// 修改前
func normalizeUpdates(updated map[schema.UpdateKey]any) map[schema.UpdateKey]schema.UpdateValue

// 修改后
func normalizeUpdates(updated map[schema.UpdateKey]any) map[schema.UpdateKey]schema.UpdateValue
// 签名不变，因为切片中的每个元素是 map[schema.UpdateKey]any
```

4b. 修改 Train 循环中 `updated` 处理（第190-209行）：

```go
// 修改前
updated, updateErr := t.updater.Update(ctx, trajectories, evaluated, config)
// ...
if updateErr == nil {
    updates := normalizeUpdates(updated)
    ApplyUpdates(operators, updates)
    valScore, valEvaluated, _ = t.Evaluate(ctx, agent, valCases)
}

// 修改后
updated, updateErr := t.updater.Update(ctx, trajectories, evaluated, config)
// ...
if updateErr == nil {
    if len(updated) == 1 {
        // 单方案：直接 apply
        updates := normalizeUpdates(updated[0])
        ApplyUpdates(operators, updates)
        valScore, valEvaluated, _ = t.Evaluate(ctx, agent, valCases)
    } else if len(updated) > 1 {
        // 多候选方案：验证集评估每个候选，选最优
        candidates := make([]map[schema.UpdateKey]schema.UpdateValue, len(updated))
        for i, u := range updated {
            candidates[i] = normalizeUpdates(u)
        }
        valScore, valEvaluated, _ = t.SelectBestCandidateOnVal(ctx, agent, operators, candidates, valCases)
    }
}
```

4c. Forward 方法中 `updated` 使用（第190行）改为 `updated[0]`（如果需要用 updated 的话）。

- [ ] **Step 5: 运行全部 evolving 测试确认通过**

```bash
cd /home/opensource/uap-claw-go && go test ./internal/evolving/... -v -count=1
```

- [ ] **Step 6: 提交**

```bash
git add internal/evolving/updater/protocol.go internal/evolving/updater/single_dim/single_dim.go internal/evolving/updater/multi_dim/multi_dim.go internal/evolving/trainer/trainer.go
git commit -m "feat(evolving): Updater.Update/Process 返回类型改为 []map[UpdateKey]any，Trainer 候选列表分支"
```

---

## Task 8: Trainer ResumeIfNeeded 修复

**Files:**
- Modify: `internal/evolving/trainer/trainer.go`

- [ ] **Step 1: 修改 ResumeIfNeeded 签名和实现**

```go
// 修改前
func (t *Trainer) ResumeIfNeeded(_ context.Context, agent evolving.TrainableAgent) error

// 修改后
func (t *Trainer) ResumeIfNeeded(_ context.Context, agent evolving.TrainableAgent, progress *Progress) error
```

在方法内添加 progress 恢复逻辑：

```go
restored := t.checkpointManager.Restore(agent, ckpt)
// 对齐 Python: progress.start_epoch / progress.best_score 从 restored 恢复
if progress != nil {
    if v, ok := restored["start_epoch"]; ok {
        progress.StartEpoch = toIntFromAny(v)
    }
    if v, ok := restored["best_score"]; ok {
        progress.BestScore = toFloat64FromAny(v)
    }
}
// 对齐 Python: updater.load_state(ckpt.updater_state)
if t.updater != nil && ckpt.UpdaterState != nil {
    t.updater.LoadState(ckpt.UpdaterState)
}
```

- [ ] **Step 2: 添加辅助函数 toIntFromAny / toFloat64FromAny**

在 trainer.go 非导出函数区添加：

```go
// toIntFromAny 安全地将 any 转换为 int。
func toIntFromAny(v any) int {
	switch n := v.(type) {
	case int:
		return n
	case int64:
		return int(n)
	case float64:
		return int(n)
	default:
		return 0
	}
}

// toFloat64FromAny 安全地将 any 转换为 float64。
func toFloat64FromAny(v any) float64 {
	switch n := v.(type) {
	case float64:
		return n
	case int:
		return float64(n)
	case int64:
		return float64(n)
	default:
		return 0.0
	}
}
```

- [ ] **Step 3: 修改 Train() 调用处**

```go
// 修改前（第142行）
_ = t.ResumeIfNeeded(ctx, agent)

// 修改后
if err := t.ResumeIfNeeded(ctx, agent, progress); err != nil {
    logger.Warn(logComponent).Err(err).Msg("[Trainer] 恢复检查点失败，从零开始训练")
}
```

- [ ] **Step 4: 运行测试确认通过**

```bash
cd /home/opensource/uap-claw-go && go test ./internal/evolving/trainer/ -v
```

- [ ] **Step 5: 提交**

```bash
git add internal/evolving/trainer/trainer.go
git commit -m "fix(evolving/trainer): ResumeIfNeeded 补齐 progress 参数恢复 + updater.LoadState"
```

---

## Task 9: 9.73 matchFailureKeyword 误判修复

**Files:**
- Modify: `internal/evolving/signal/from_conv.go`
- Modify: `internal/evolving/signal/from_conv_test.go`

- [ ] **Step 1: 修改 from_conv.go:615 行**

```go
// 修改前
hasFailure := content != "" && matchFailureKeyword(content)

// 修改后
hasFailure := content != "" && findFailureKeywordIndex(content) != nil
```

- [ ] **Step 2: 删除 matchFailureKeyword 函数**

删除 from_conv.go 中 `matchFailureKeyword` 函数定义（第439-444行）。

- [ ] **Step 3: 删除 matchFailureKeyword 测试**

删除 from_conv_test.go 中 `matchFailureKeyword` 的 2 个测试用例（约第1417-1428行）。

- [ ] **Step 4: 运行测试确认通过**

```bash
cd /home/opensource/uap-claw-go && go test ./internal/evolving/signal/ -v
```

- [ ] **Step 5: 提交**

```bash
git add internal/evolving/signal/from_conv.go internal/evolving/signal/from_conv_test.go
git commit -m "fix(evolving/signal): 脚本产物检测改用 findFailureKeywordIndex 避免 matchFailureKeyword 误判"
```

---

## Task 10: 9.73 MakeTeamTrajectorySignal excerpt 语言修复

**Files:**
- Modify: `internal/evolving/signal/team.go`

- [ ] **Step 1: 修改 excerpt**

```go
// 修改前（team.go:331）
"检测到团队技能轨迹问题，需要进行进化。"

// 修改后
"Detected team skill trajectory issues requiring evolution."
```

- [ ] **Step 2: 运行测试确认通过**

```bash
cd /home/opensource/uap-claw-go && go test ./internal/evolving/signal/ -v
```

- [ ] **Step 3: 提交**

```bash
git add internal/evolving/signal/team.go
git commit -m "fix(evolving/signal): MakeTeamTrajectorySignal excerpt 改为英文对齐 Python"
```

---

## Task 11: trainer/doc.go ⤵️ 标记更新 + IMPLEMENTATION_PLAN.md 清理

**Files:**
- Modify: `internal/evolving/trainer/doc.go`
- Modify: `IMPLEMENTATION_PLAN.md`

- [ ] **Step 1: 更新 trainer/doc.go**

```go
// 修改前
//   - ✅ Forward（前向推理+评估+轨迹提取，轨迹提取 ⤵️ 待 9.77 回填）
//   - ✅ Train（训练主循环，检查点 ⤵️ 待 9.78 回填）
//   - ⤵️ ResumeIfNeeded / SaveCheckpointIfNeeded（待 9.78 EvolveCheckpoint 回填）

// 修改后
//   - ✅ Forward（前向推理+评估+轨迹提取，✅ 9.77 已回填 TracerTrajectoryExtractor）
//   - ✅ Train（训练主循环，✅ 9.78 已回填检查点保存/恢复）
//   - ✅ ResumeIfNeeded / SaveCheckpointIfNeeded（✅ 9.78 已回填，✅ progress 恢复已补齐）
```

- [ ] **Step 2: 更新 IMPLEMENTATION_PLAN.md 9.72c 描述**

```markdown
<!-- 修改前 -->
✅ 回填 ToolOptimizerBase.Bind()（⤵️ 9.70 → 委托 BaseOptimizerMixin.Bind + DefaultTargets 回退）

<!-- 修改后 -->
✅ 回填 ToolOptimizerBase.Bind()（✅ 9.70 已回填，委托 BaseOptimizerMixin.Bind + DefaultTargets 回退）
```

- [ ] **Step 3: 提交**

```bash
git add internal/evolving/trainer/doc.go IMPLEMENTATION_PLAN.md
git commit -m "docs: 更新 trainer/doc.go ⤵️ 标记为已回填 + 清理 IMPLEMENTATION_PLAN 9.72c 描述"
```

---

## 自审检查

1. **Spec coverage**: 7 个问题全部有对应 Task（#1-6 对问题1，#7 对问题3，#8 对问题2，#9 对问题4，#10 对问题5，#11 对问题6+7）
2. **Placeholder scan**: 无 TBD/TODO/未完成段落
3. **Type consistency**: 
   - Task 7 中 Updater 返回 `[]map[UpdateKey]any`，与 Task 8 中 Trainer 使用 `updated[0]` / `len(updated)` 一致
   - Task 8 中 `ResumeIfNeeded` 签名加入 `*Progress`，与 Train() 调用处传入 `progress` 一致
   - `normalizeUpdates` 接受 `map[UpdateKey]any`，与切片元素类型一致

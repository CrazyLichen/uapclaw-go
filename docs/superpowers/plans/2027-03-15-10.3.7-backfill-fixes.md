# 10.3.7-11 回填审查修复 实现计划

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 修复 10.3.7-11 实现中遗留的 6 个问题：1 个 ⤵️ 回填、1 个热重载缺陷、3 个 ⤵️ 标记指向修正、1 个 modelCache 传 nil

**Architecture:** 逐个修复，每个 Task 独立可测试。先处理标记修正（低风险），再处理逻辑修复（需测试），最后处理动态构建（改动最大）。

**Tech Stack:** Go 1.22+, testify/assert

**Design Spec:** `docs/superpowers/specs/2027-03-15-10.3.7-backfill-fixes-design.md`

---

### Task 1: 修正 ⤵️ 标记指向（3 处）

**Files:**
- Modify: `internal/swarm/server/adapter/deep_adapter.go:839,1073,1124-1130`

- [ ] **Step 1: 修正 TeamHelpers ⤵️ 标记（L839）**

将 `deep_adapter.go:839` 从：
```go
// ⤵️ 10.3.7-11 TeamHelpers
```
改为：
```go
// ⤵️ 9.55-9.65 TeamHelpers
```

- [ ] **Step 2: 修正取消进化监听 ⤵️ 标记（L1073）**

将 `deep_adapter.go:1073` 从：
```go
// ⤵️ 10.3.7-11 EvolutionHelpers（进化助手）: 取消进化监听任务
```
改为：
```go
// ⤵️ 9.55-9.65 EvolutionHelpers（进化助手）: 取消进化监听任务
```

- [ ] **Step 3: 修正 Evolution 审批 ⤵️ 标记（L1124-1130）**

将 `deep_adapter.go:1124` 从：
```go
// ⤵️ 10.6.3-10 / 10.3.7-11: handle_team_skill_evolve_approval(requestID, answers, sessionID, channelID)
```
改为：
```go
// ⤵️ 10.6.3-10: handle_team_skill_evolve_approval(requestID, answers, sessionID, channelID)
```

将 `deep_adapter.go:1127` 从：
```go
// ⤵️ 10.6.3-10 / 10.3.7-11: _handle_governance_approval(requestID, answers, "simplify")
```
改为：
```go
// ⤵️ 10.6.3-10: _handle_governance_approval(requestID, answers, "simplify")
```

将 `deep_adapter.go:1130` 从：
```go
// ⤵️ 10.6.3-10 / 10.3.7-11: _handle_evolution_approval(requestID, answers)
```
改为：
```go
// ⤵️ 10.6.3-10: _handle_evolution_approval(requestID, answers)
```

- [ ] **Step 4: 编译验证**

Run: `cd /home/opensource/uap-claw-go && go build ./internal/swarm/server/adapter/...`
Expected: 编译通过

- [ ] **Step 5: 提交**

```bash
git add internal/swarm/server/adapter/deep_adapter.go
git commit -m "fix(adapter): 修正 3 处 ⤵️ 标记指向（10.3.7-11→9.55-9.65/10.6.3-10）"
```

---

### Task 2: AgentTool modelCache 类型断言获取

**Files:**
- Modify: `internal/swarm/server/adapter/agent_tool.go:230-231,247`
- Modify: `internal/swarm/server/adapter/deep_adapter.go` — 添加 `ModelCache()` 方法
- Modify: `internal/swarm/server/adapter/agent_tool_test.go` — 添加测试

- [ ] **Step 1: DeepAdapter 添加 ModelCache() 方法**

在 `deep_adapter.go` 的导出函数区块末尾添加：

```go
// ModelCache 返回模型缓存。
// 对齐 Python: self._model_cache (interface_deep.py L543)
// AgentTool 通过类型断言获取，拿不到则 fallback nil。
func (d *DeepAdapter) ModelCache() map[string]*llm.Model {
	return d.modelCache
}
```

- [ ] **Step 2: AgentTool.createSubAgent 中使用类型断言获取 modelCache**

将 `agent_tool.go:230-231` 从：
```go
	var model *llm.Model
	var modelCache map[string]*llm.Model
```
改为：
```go
	var model *llm.Model
	// 对齐 Python: getattr(self._parent_agent, "_model_cache", None)
	// 通过匿名接口类型断言获取 modelCache，拿不到则 fallback nil
	var modelCache map[string]*llm.Model
	if t.parentAgent != nil {
		if v, ok := t.parentAgent.(interface{ ModelCache() map[string]*llm.Model }); ok {
			modelCache = v.ModelCache()
		}
	}
```

- [ ] **Step 3: 编译验证**

Run: `cd /home/opensource/uap-claw-go && go build ./internal/swarm/server/adapter/...`
Expected: 编译通过

- [ ] **Step 4: 运行现有测试**

Run: `cd /home/opensource/uap-claw-go && go test ./internal/swarm/server/adapter/... -run TestAgentTool -v`
Expected: 全部通过

- [ ] **Step 5: 提交**

```bash
git add internal/swarm/server/adapter/deep_adapter.go internal/swarm/server/adapter/agent_tool.go
git commit -m "fix(adapter): AgentTool 通过类型断言获取 modelCache，对齐 Python getattr"
```

---

### Task 3: CodeAgentRail 热重载修复

**Files:**
- Modify: `internal/swarm/server/adapter/code_adapter.go:377-380`
- Modify: `internal/swarm/server/adapter/code_adapter_test.go` — 添加测试

- [ ] **Step 1: 覆写 CodeAdapter.ReloadAgentConfig**

将 `code_adapter.go:377-380` 从：
```go
// ReloadAgentConfig 委托 DeepAdapter。
func (c *CodeAdapter) ReloadAgentConfig(ctx context.Context, configBase map[string]any, envOverrides map[string]any) error {
	return c.deep.ReloadAgentConfig(ctx, configBase, envOverrides)
}
```
改为：
```go
// ReloadAgentConfig 热重载配置，不重启进程。
// 对齐 Python: JiuwenClawCodeAdapter._get_current_agent_rails() 覆写 (interface_code.py L836-848)
//
// Python 通过覆写 _get_current_agent_rails() 将 CodeAgentRail 纳入热重载范围。
// Go 使用组合模式，无法覆写 getter，因此在 ReloadAgentConfig 中额外调用 CodeAgentRail.Reload。
func (c *CodeAdapter) ReloadAgentConfig(ctx context.Context, configBase map[string]any, envOverrides map[string]any) error {
	if err := c.deep.ReloadAgentConfig(ctx, configBase, envOverrides); err != nil {
		return err
	}

	// 对齐 Python: _get_current_agent_rails() 覆写中追加 CodeAgentRail
	if c.codeAgentRail != nil {
		if c.deep.instance != nil {
			if err := c.codeAgentRail.Reload(c.deep.instance); err != nil {
				logger.Warn(logComponent).
					Err(err).
					Msg("CodeAgentRail Reload 失败，继续执行")
			}
		}
	}

	return nil
}
```

- [ ] **Step 2: 编译验证**

Run: `cd /home/opensource/uap-claw-go && go build ./internal/swarm/server/adapter/...`
Expected: 编译通过

- [ ] **Step 3: 运行现有测试**

Run: `cd /home/opensource/uap-claw-go && go test ./internal/swarm/server/adapter/... -run TestCodeAdapter -v`
Expected: 全部通过

- [ ] **Step 4: 提交**

```bash
git add internal/swarm/server/adapter/code_adapter.go
git commit -m "fix(adapter): CodeAdapter 覆写 ReloadAgentConfig，追加 CodeAgentRail.Reload"
```

---

### Task 4: ListAvailableTools 动态构建（对齐 Python）

**Files:**
- Modify: `internal/swarm/server/types/agent_tools.go` — 新增 `ToolDescriptions`
- Modify: `internal/swarm/server/runtime/agent_config.go:369-424` — 重写 `ListAvailableTools()`
- Modify: `internal/swarm/server/runtime/agent_config_test.go` — 更新测试

- [ ] **Step 1: types/agent_tools.go 新增 ToolDescriptions**

在 `agent_tools.go` 的全局变量区块添加（对齐 Python `_TOOL_DESCRIPTIONS` agent_config_service.py L28-52）：

```go
// ToolDescriptions 工具描述映射（显示名→描述）。
// 对齐 Python: _TOOL_DESCRIPTIONS (agent_config_service.py L28-52)
// ListAvailableTools() 动态构建工具列表时使用此映射。
var ToolDescriptions = map[string]string{
	"Read":           "读取文件内容",
	"Write":          "写入文件",
	"Edit":           "编辑文件（精准替换）",
	"Bash":           "执行 shell 命令",
	"LS":             "列出目录内容",
	"Grep":           "搜索文件内容",
	"Glob":           "按模式搜索文件名",
	"WebSearch":      "网络搜索",
	"WebFetch":       "获取网页内容",
	"LSP":            "代码智能（定义跳转、引用查找）",
	"TodoWrite":      "创建/更新任务列表",
	"TodoList":       "查看任务列表",
	"MemorySearch":   "搜索记忆",
	"MemoryGet":      "获取记忆条目",
	"WriteMemory":    "写入记忆",
	"EditMemory":     "编辑记忆",
	"CronCreate":     "创建定时任务",
	"CronList":       "列出定时任务",
	"CronDelete":     "删除定时任务",
	"SkillTool":      "调用 Skill",
	"VisionQA":       "视觉问答",
	"ImageOCR":       "图片文字识别",
	"AudioTranscribe": "音频转录",
}
```

- [ ] **Step 2: 重写 ListAvailableTools() 方法**

将 `agent_config.go:369-424` 中的 `ListAvailableTools()` 方法替换为：

```go
// ListAvailableTools 返回可用工具及其分组信息。
// 对齐 Python: AgentConfigService.list_available_tools() (agent_config_service.py L351-401)
//
// 从 3 个共享数据源动态构建：
//   - types.ToolGroups（group→display names）
//   - displayToInternal（display→internal，反向构建 internal→display）
//   - types.ToolDescriptions（display→description）
func (s *AgentConfigService) ListAvailableTools() *AvailableToolsResult {
	// 步骤 1: 从 displayToInternal 构建 internal→display 映射（去重）
	// 对齐 Python: internal_to_display = {}; for internal_name, display_name in _TOOL_DISPLAY_NAMES.items()
	internalToDisplay := make(map[string]string)
	for display, internal := range displayToInternal {
		if _, exists := internalToDisplay[internal]; !exists {
			internalToDisplay[internal] = display
		}
	}

	// 步骤 2: 从 ToolGroups 构建 display→group 映射
	// 对齐 Python: display_to_group = {}; for group_name, display_names in TOOL_GROUPS.items()
	displayToGroup := make(map[string]string)
	for group, displayNames := range types.ToolGroups {
		for _, dn := range displayNames {
			displayToGroup[dn] = group
		}
	}

	// 步骤 3: 从 internalToDisplay 构建工具列表（去重）
	// 对齐 Python: for internal_name, display_name in internal_to_display.items()
	tools := make([]ToolInfo, 0, len(internalToDisplay))
	seenDisplay := make(map[string]bool)
	for internalName, displayName := range internalToDisplay {
		if seenDisplay[displayName] {
			continue
		}
		seenDisplay[displayName] = true
		group := displayToGroup[displayName]
		if group == "" {
			group = "高级"
		}
		description := types.ToolDescriptions[displayName]
		if description == "" {
			description = displayName
		}
		tools = append(tools, ToolInfo{
			Name:         displayName,
			InternalName: internalName,
			Description:  description,
			Group:        group,
		})
	}

	// 步骤 4: 补充 ToolGroups 中有但 displayToInternal 中没有的工具
	// 对齐 Python: for group_name, display_names in TOOL_GROUPS.items(): if dn not in seen_display
	for group, displayNames := range types.ToolGroups {
		for _, dn := range displayNames {
			if seenDisplay[dn] {
				continue
			}
			seenDisplay[dn] = true
			description := types.ToolDescriptions[dn]
			if description == "" {
				description = dn
			}
			tools = append(tools, ToolInfo{
				Name:         dn,
				InternalName: strings.ToLower(dn),
				Description:  description,
				Group:        group,
			})
		}
	}

	// 步骤 5: 构建分组列表
	// 对齐 Python: groups = list(TOOL_GROUPS.keys())
	groups := make([]string, 0, len(types.ToolGroups))
	for group := range types.ToolGroups {
		groups = append(groups, group)
	}
	sort.Strings(groups)

	// 步骤 6: 子 agent 禁用工具列表
	// 对齐 Python: disallowed_for_subagents = list(DISALLOWED_FOR_SUBAGENTS)
	disallowedForSubagents := types.DisallowedForSubagents

	return &AvailableToolsResult{
		Tools:                  tools,
		Groups:                 groups,
		DisallowedForSubagents: disallowedForSubagents,
	}
}
```

注意：`displayToInternal` 定义在 `adapter` 包中，`runtime` 包无法直接导入（会循环依赖）。
需要在 `runtime` 包中定义一份等价的映射，或者将 `displayToInternal` 提取到 `types` 包。
选择：**在 `agent_config.go` 中定义一份本地映射**（与 `code_agent_rail.go` 中的定义独立，
但内容一致，对齐 Python 中 `code_agent_rail.py` 和 `tool_display.py` 也是两个独立文件）。

在 `agent_config.go` 的全局变量区块添加：

```go
// displayToInternal 显示名→内部名映射。
// 对齐 Python: _DISPLAY_TO_INTERNAL (code_agent_rail.py L48-66)
// 与 adapter 包中的 displayToInternal 内容一致，但因 adapter↔runtime 循环依赖无法共享。
var displayToInternal = map[string]string{
	"Read": "read_file", "Write": "write_file", "Edit": "edit_file",
	"Bash": "bash", "Grep": "grep", "Glob": "glob",
	"LS": "ls", "ListDir": "ls",
	"TodoWrite": "todo_create", "TodoList": "todo_list",
	"WebSearch": "web_search", "WebFetch": "web_fetch",
	"ImageOCR": "image_ocr", "VisionQA": "visual_question_answering",
	"AudioTranscribe": "audio_transcription",
	"AudioQA":         "audio_question_answering",
	"AudioMetadata":   "audio_metadata",
}
```

- [ ] **Step 3: 更新测试**

将 `agent_config_test.go:648-661` 的测试替换为更精确的验证：

```go
// TestAgentConfigService_ListAvailableTools 测试列出可用工具（动态构建）
func TestAgentConfigService_ListAvailableTools(t *testing.T) {
	svc := NewAgentConfigService("/tmp")
	result := svc.ListAvailableTools()

	// 验证工具数量与 ToolGroups 中定义的数量一致
	assert.NotEmpty(t, result.Tools, "工具列表不应为空")
	assert.GreaterOrEqual(t, len(result.Tools), 20, "应至少有 20 个工具")

	// 验证分组列表与 ToolGroups 一致
	assert.Contains(t, result.Groups, "核心")
	assert.Contains(t, result.Groups, "搜索")
	assert.Contains(t, result.Groups, "代码智能")
	assert.Contains(t, result.Groups, "高级")
	assert.Contains(t, result.Groups, "可视化")

	// 验证 ToolGroups 中每个工具都出现在结果中
	for group, displayNames := range types.ToolGroups {
		for _, dn := range displayNames {
			found := false
			for _, ti := range result.Tools {
				if ti.Name == dn {
					found = true
					assert.Equal(t, group, ti.Group, "工具 %s 的分组应为 %s", dn, group)
					assert.NotEmpty(t, ti.Description, "工具 %s 应有描述", dn)
					assert.NotEmpty(t, ti.InternalName, "工具 %s 应有内部名", dn)
					break
				}
			}
			assert.True(t, found, "ToolGroups 中的工具 %s 应出现在结果中", dn)
		}
	}

	// 验证 disallowed_for_subagents
	assert.NotEmpty(t, result.DisallowedForSubagents, "应包含 disallowed_for_subagents")
	assert.Contains(t, result.DisallowedForSubagents, "Agent")
}
```

- [ ] **Step 4: 编译验证**

Run: `cd /home/opensource/uap-claw-go && go build ./internal/swarm/server/runtime/...`
Expected: 编译通过

- [ ] **Step 5: 运行测试**

Run: `cd /home/opensource/uap-claw-go && go test ./internal/swarm/server/runtime/... -run TestAgentConfigService_ListAvailableTools -v`
Expected: PASS

- [ ] **Step 6: 运行 adapter 包测试确认无循环依赖**

Run: `cd /home/opensource/uap-claw-go && go build ./internal/swarm/server/adapter/...`
Expected: 编译通过

- [ ] **Step 7: 提交**

```bash
git add internal/swarm/server/types/agent_tools.go internal/swarm/server/runtime/agent_config.go internal/swarm/server/runtime/agent_config_test.go
git commit -m "feat(runtime): ListAvailableTools 从共享常量动态构建，移除 ⤵️ 10.3.7-11 标记"
```

---

### Task 5: 全量编译和测试验证

**Files:** 无修改

- [ ] **Step 1: 检查残留进程**

Run: `pgrep -f 'go (build|test)'`
Expected: 无输出（如有则 kill）

- [ ] **Step 2: 全量编译**

Run: `cd /home/opensource/uap-claw-go && go build ./...`
Expected: 编译通过

- [ ] **Step 3: 运行 adapter 和 runtime 包测试**

Run: `cd /home/opensource/uap-claw-go && go test ./internal/swarm/server/adapter/... ./internal/swarm/server/runtime/... ./internal/swarm/server/types/... -v -count=1`
Expected: 全部通过

- [ ] **Step 4: 提交（如有任何遗漏修复）**

如发现编译或测试问题，修复后提交。

---

### Task 6: 更新 IMPLEMENTATION_PLAN.md

**Files:**
- Modify: `IMPLEMENTATION_PLAN.md`

- [ ] **Step 1: 更新 10.3.7-11 行状态**

确认所有修复已完成，将 10.3.7-11 行中 TeamHelpers 的状态保持 ☐（已修正 ⤵️ 指向），
其余 ✅ 项保持不变。

- [ ] **Step 2: 提交**

```bash
git add IMPLEMENTATION_PLAN.md
git commit -m "docs: 更新 IMPLEMENTATION_PLAN.md 10.3.7-11 回填审查修复状态"
```

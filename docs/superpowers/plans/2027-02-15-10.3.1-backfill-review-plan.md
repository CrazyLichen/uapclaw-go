# 10.3.1 回填标记审查与修正 实现计划

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 修正 10.3.1 章节中三类 ⤵️ 回填标记问题：清理过时注释、修正指向、回填 PermissionContext 链路

**Architecture:** 三类修改互不依赖，按类别分任务执行。PermissionContext 回填涉及新增代码（schema/permission.go 追加 context key + 工具函数）和修改 deep_adapter.go 调用点。注释清理和指向修正为纯文本编辑。

**Tech Stack:** Go 1.x, context.WithValue 不可变值模式

---

### Task 1: 清理 deep_adapter.go 中过时的 ⤵️/⤴️ 注释

**Files:**
- Modify: `internal/swarm/server/adapter/deep_adapter.go`

- [ ] **Step 1: 修改 line 92 — instanceOverrides 注释**

将 `// ⤵️ 10.3.5 CreateInstance: create_instance 传入的 config 字典` 改为 `// instanceOverrides CreateInstance 传入的 config 字典`

- [ ] **Step 2: 修改 line 117 — contextAssembleRail 注释**

将 `// ⤵️ 10.6.3-10: ContextAssembleRail` 改为 `// contextAssembleRail 上下文组装轨道`

- [ ] **Step 3: 修改 line 123 — contextProcessorRail 注释**

将 `// ⤵️ 10.6.3-10: ContextProcessorRail` 改为 `// contextProcessorRail 上下文处理轨道`

- [ ] **Step 4: 修改 line 144 — heartbeatRail 注释**

将 `// ⤴️ 9.15 回填：HeartbeatRail` 改为 `// heartbeatRail 心跳轨道`

- [ ] **Step 5: 修改 line 174 — visionToolsRegistered 注释**

将 `// ⤵️ 10.6.24 多模态工具` 改为 `// visionToolsRegistered 视觉工具注册状态`

- [ ] **Step 6: 修改 line 179 — audioToolsRegistered 注释**

将 `// ⤵️ 10.6.24 多模态工具` 改为 `// audioToolsRegistered 音频工具注册状态`

- [ ] **Step 7: 修改 line 184 — videoToolRegistered 注释**

将 `// ⤵️ 10.6.24 多模态工具` 改为 `// videoToolRegistered 视频工具注册状态`

- [ ] **Step 8: 验证编译**

Run: `cd /home/opensource/uap-claw-go && pgrep -f 'go (build|test)' && pkill -f 'go (build|test)' || true`
Then: `export GOPROXY=https://goproxy.cn,direct && go build ./internal/swarm/server/...`
Expected: 编译成功，无错误

---

### Task 2: 清理 deep_adapter_tools.go 中过时的 ⤵️ 注释

**Files:**
- Modify: `internal/swarm/server/adapter/deep_adapter_tools.go`

- [ ] **Step 1: 修改 line 23 — 工具名称常量注释**

将 `// ⤵️ 9.38-49 Harness 工具集: 具体工具名称待回填` 改为 `// 工具名称常量`

- [ ] **Step 2: 验证编译**

Run: `cd /home/opensource/uap-claw-go && export GOPROXY=https://goproxy.cn,direct && go build ./internal/swarm/server/...`
Expected: 编译成功

---

### Task 3: 清理 code_adapter.go 中过时的 ⤵️ 注释

**Files:**
- Modify: `internal/swarm/server/adapter/code_adapter.go`

- [ ] **Step 1: 修改 line 49 — codeAgentRail 注释**

将 `// ⤵️ 10.3.7-11 CodeAgentRail` 改为 `// codeAgentRail Code 模式专有护栏`

- [ ] **Step 2: 验证编译**

Run: `cd /home/opensource/uap-claw-go && export GOPROXY=https://goproxy.cn,direct && go build ./internal/swarm/server/...`
Expected: 编译成功

---

### Task 4: 清理 runtime/agent_config.go 中过时的 ⤴️ 注释

**Files:**
- Modify: `internal/swarm/server/runtime/agent_config.go`

- [ ] **Step 1: 修改 line 406 — ToolGroups 注释**

将 `// ⤴️ 10.3.7-11: 对齐 Python: groups = list(TOOL_GROUPS.keys())` 改为 `// 对齐 Python: groups = list(TOOL_GROUPS.keys())`

- [ ] **Step 2: 修改 line 415 — DisallowedForSubagents 注释**

将 `// ⤴️ 10.3.7-11: 对齐 Python: DISALLOWED_FOR_SUBAGENTS` 改为 `// 对齐 Python: DISALLOWED_FOR_SUBAGENTS`

- [ ] **Step 3: 验证编译**

Run: `cd /home/opensource/uap-claw-go && export GOPROXY=https://goproxy.cn,direct && go build ./internal/swarm/server/runtime/...`
Expected: 编译成功

---

### Task 5: 修正 deep_adapter_tools.go 中 ⤵️ 9.38-49 指向为 ⤵️ 10.6.24

**Files:**
- Modify: `internal/swarm/server/adapter/deep_adapter_tools.go`

所有 `⤵️ 9.38-49` 替换为 `⤵️ 10.6.24`，`9.38-49 Harness 工具集` 替换为 `10.6.24 Swarm 内置工具集`。

- [ ] **Step 1: 修改 line 87**

将 `// ⤵️ 9.38-49: 根据 toolGroup 解析 configBase 中的工具配置` 改为 `// ⤵️ 10.6.24: 根据 toolGroup 解析 configBase 中的工具配置`

- [ ] **Step 2: 修改 line 91**

将 `_ = toolInstancesToAdd // ⤵️ 9.38-49: 从 configBase 解析待添加的工具实例` 改为 `_ = toolInstancesToAdd // ⤵️ 10.6.24: 从 configBase 解析待添加的工具实例`

- [ ] **Step 3: 修改 line 92**

将 `_ = toolCardsToAdd     // ⤵️ 9.38-49: 从 configBase 解析待添加的工具卡片` 改为 `_ = toolCardsToAdd     // ⤵️ 10.6.24: 从 configBase 解析待添加的工具卡片`

- [ ] **Step 4: 修改 line 93**

将 `_ = toolIDsToRemove    // ⤵️ 9.38-49: 从 AbilityManager 查询当前已注册的同组工具` 改为 `_ = toolIDsToRemove    // ⤵️ 10.6.24: 从 AbilityManager 查询当前已注册的同组工具`

- [ ] **Step 5: 修改 line 297**

将 `// TODO(#9.38-49): ⤵️ 确保图片生成工具已注册` 改为 `// TODO(#10.6.24): ⤵️ 确保图片生成工具已注册`

- [ ] **Step 6: 修改 line 304**

将 `// ⤵️ 9.38-49 Harness 工具集: 付费搜索工具热同步` 改为 `// ⤵️ 10.6.24 Swarm 内置工具集: 付费搜索工具热同步`

- [ ] **Step 7: 修改 line 310**

将 `// ⤵️ 9.38-49: 根据 paidSearchRegistered 状态注册/注销付费搜索工具` 改为 `// ⤵️ 10.6.24: 根据 paidSearchRegistered 状态注册/注销付费搜索工具`

- [ ] **Step 8: 修改 line 316**

将 `logger.Info(logComponent).Bool("registered", d.paidSearchRegistered).Msg("syncPaidSearchToolForRuntime 等待回填")` 改为 `logger.Info(logComponent).Bool("registered", d.paidSearchRegistered).Msg("syncPaidSearchToolForRuntime 等待 10.6.24 回填")`

- [ ] **Step 9: 修改 line 403**

将 `// ⤵️ 9.38-49 Harness 工具集: apply_image_gen_model_config_from_yaml` 改为 `// ⤵️ 10.6.24 Swarm 内置工具集: apply_image_gen_model_config_from_yaml`

- [ ] **Step 10: 修改 line 405**

将 `// ⤵️ 9.38-49: apply_image_gen_model_config_from_yaml(configBase) — 将 YAML 配置映射到环境变量` 改为 `// ⤵️ 10.6.24: apply_image_gen_model_config_from_yaml(configBase) — 将 YAML 配置映射到环境变量`

- [ ] **Step 11: 修改 line 442**

将 `// ⤵️ 9.38-49: wiki_ingest / wiki_query / wiki_lint 工具类尚未实现` 改为 `// ⤵️ 10.6.24: wiki_ingest / wiki_query / wiki_lint 工具类尚未实现`

- [ ] **Step 12: 修改 line 457**

将 `// ⤵️ 9.38-49: WebPaidSearchTool + is_paid_search_enabled() 尚未实现` 改为 `// ⤵️ 10.6.24: WebPaidSearchTool + is_paid_search_enabled() 尚未实现`

- [ ] **Step 13: 修改 line 472**

将 `// ⤵️ 9.38-49: WebFreeSearchTool / WebFetchWebpageTool 尚未实现` 改为 `// ⤵️ 10.6.24: WebFreeSearchTool / WebFetchWebpageTool 尚未实现`

- [ ] **Step 14: 修改 line 541**

将 `// ⤵️ 9.38-49: generate_image 工具实例尚未实现` 改为 `// ⤵️ 10.6.24: generate_image 工具实例尚未实现`

- [ ] **Step 15: 修改 line 544**

将 `logger.Info(logComponent).Msg("getToolCards: 图片生成工具配置已就绪，等待 9.38-49 回填 generate_image")` 改为 `logger.Info(logComponent).Msg("getToolCards: 图片生成工具配置已就绪，等待 10.6.24 回填 generate_image")`

- [ ] **Step 16: 修改 line 556**

将 `// ⤵️ 9.38-49: 小艺手机端工具类 (28个) 尚未实现` 改为 `// ⤵️ 10.6.24: 小艺手机端工具类 (28个) 尚未实现`

- [ ] **Step 17: 修改 line 582**

将 `// ⤵️ 9.38-49: SkillToolkit 工具尚未实现` 改为 `// ⤵️ 10.6.24: SkillToolkit 工具尚未实现`

- [ ] **Step 18: 修改 line 590**

将 `logger.Info(logComponent).Msg("getToolCards: SkillManager 已就绪，等待 9.38-49 回填 SkillToolkit")` 改为 `logger.Info(logComponent).Msg("getToolCards: SkillManager 已就绪，等待 10.6.24 回填 SkillToolkit")`

- [ ] **Step 19: 修改 line 600**

将 `// ⤵️ 9.38-49: acp_chat 工具尚未实现` 改为 `// ⤵️ 10.6.24: acp_chat 工具尚未实现`

- [ ] **Step 20: 验证编译**

Run: `cd /home/opensource/uap-claw-go && export GOPROXY=https://goproxy.cn,direct && go build ./internal/swarm/server/...`
Expected: 编译成功

---

### Task 6: 新增 PermissionContext context key 和工具函数

**Files:**
- Modify: `internal/swarm/schema/permission.go`

在 `permission.go` 中追加 context key 类型定义和 4 个工具函数。需要新增 `"context"` import。

- [ ] **Step 1: 添加 import**

在 `import` 块中添加 `"context"`。

- [ ] **Step 2: 在全局变量区块添加 context key 类型**

在 `// ──────────────────────────── 全局变量 ────────────────────────────` 区块中追加：

```go
// toolPermChannelIDKey 权限上下文 channelID 的 context key
type toolPermChannelIDKey struct{}

// toolPermContextKey 权限上下文的 context key
type toolPermContextKey struct{}
```

- [ ] **Step 3: 在导出函数区块追加 4 个工具函数**

在 `// ──────────────────────────── 导出函数 ────────────────────────────` 区块末尾（`WithPermissionWebUserID` 之后）追加：

```go
// WithToolPermissionChannelID 将 channelID 注入 context。
// 对齐 Python: TOOL_PERMISSION_CHANNEL_ID.set(channel_id)
// Go 使用 context.WithValue 不可变值模式，无需 reset/cleanup。
func WithToolPermissionChannelID(ctx context.Context, channelID string) context.Context {
	return context.WithValue(ctx, toolPermChannelIDKey{}, channelID)
}

// ToolPermissionChannelIDFromCtx 从 context 中获取 channelID。
// 对齐 Python: TOOL_PERMISSION_CHANNEL_ID.get()
func ToolPermissionChannelIDFromCtx(ctx context.Context) string {
	if v, ok := ctx.Value(toolPermChannelIDKey{}).(string); ok {
		return v
	}
	return ""
}

// WithPermissionContextValue 将 PermissionContext 注入 context。
// 对齐 Python: TOOL_PERMISSION_CONTEXT.set(permission_context)
// Go 使用 context.WithValue 不可变值模式，无需 reset/cleanup。
func WithPermissionContextValue(ctx context.Context, pc *PermissionContext) context.Context {
	return context.WithValue(ctx, toolPermContextKey{}, pc)
}

// PermissionContextFromCtx 从 context 中获取 PermissionContext。
// 对齐 Python: TOOL_PERMISSION_CONTEXT.get()
func PermissionContextFromCtx(ctx context.Context) *PermissionContext {
	if v, ok := ctx.Value(toolPermContextKey{}).(*PermissionContext); ok {
		return v
	}
	return nil
}
```

- [ ] **Step 4: 验证编译**

Run: `cd /home/opensource/uap-claw-go && export GOPROXY=https://goproxy.cn,direct && go build ./internal/swarm/schema/...`
Expected: 编译成功

---

### Task 7: 为 PermissionContext 工具函数编写单元测试

**Files:**
- Modify: `internal/swarm/schema/permission_test.go`

- [ ] **Step 1: 添加测试函数**

在 `permission_test.go` 末尾追加：

```go
func TestWithToolPermissionChannelID(t *testing.T) {
	ctx := context.Background()
	ctx = schema.WithToolPermissionChannelID(ctx, "web")
	if got := schema.ToolPermissionChannelIDFromCtx(ctx); got != "web" {
		t.Errorf("ToolPermissionChannelIDFromCtx = %q, want %q", got, "web")
	}
}

func TestToolPermissionChannelIDFromCtx_空值(t *testing.T) {
	ctx := context.Background()
	if got := schema.ToolPermissionChannelIDFromCtx(ctx); got != "" {
		t.Errorf("空 context 应返回空字符串，got %q", got)
	}
}

func TestWithPermissionContextValue(t *testing.T) {
	pc := schema.NewPermissionContext(
		schema.WithPermissionChannelID("web"),
		schema.WithPermissionPrincipalUserID("user1"),
	)
	ctx := context.Background()
	ctx = schema.WithPermissionContextValue(ctx, pc)
	got := schema.PermissionContextFromCtx(ctx)
	if got == nil {
		t.Fatal("PermissionContextFromCtx 返回 nil")
	}
	if got.ChannelID != "web" {
		t.Errorf("ChannelID = %q, want %q", got.ChannelID, "web")
	}
	if got.PrincipalUserID != "user1" {
		t.Errorf("PrincipalUserID = %q, want %q", got.PrincipalUserID, "user1")
	}
}

func TestPermissionContextFromCtx_空值(t *testing.T) {
	ctx := context.Background()
	if got := schema.PermissionContextFromCtx(ctx); got != nil {
		t.Errorf("空 context 应返回 nil，got %v", got)
	}
}

func TestWithToolPermissionChannelID_覆盖(t *testing.T) {
	ctx := context.Background()
	ctx = schema.WithToolPermissionChannelID(ctx, "web")
	ctx = schema.WithToolPermissionChannelID(ctx, "feishu")
	if got := schema.ToolPermissionChannelIDFromCtx(ctx); got != "feishu" {
		t.Errorf("覆盖后应返回新值，got %q", got)
	}
}
```

- [ ] **Step 2: 运行测试**

Run: `cd /home/opensource/uap-claw-go && export GOPROXY=https://goproxy.cn,direct && go test ./internal/swarm/schema/... -run TestWithToolPermissionChannelID -run TestToolPermissionChannelIDFromCtx -run TestWithPermissionContextValue -run TestPermissionContextFromCtx -v`
Expected: 全部 PASS

---

### Task 8: 回填 deep_adapter.go 中 PermissionContext 注入

**Files:**
- Modify: `internal/swarm/server/adapter/deep_adapter.go`

- [ ] **Step 1: 修改 ProcessMessageImpl 中的权限上下文设置（~line 699）**

将：
```go
	// 步骤 10-11: 权限上下文设置
	// ⤵️ 10.1.8: token_cid = TOOL_PERMISSION_CHANNEL_ID.set(...); token_perm = setup_permission_context(request)
```

改为：
```go
	// 步骤 10-11: 权限上下文设置
	// 对齐 Python: TOOL_PERMISSION_CHANNEL_ID.set(channel_id) + setup_permission_context(request)
	ctx = schema.WithToolPermissionChannelID(ctx, req.ChannelID)
	if req.PermissionContext != nil {
		ctx = schema.WithPermissionContextValue(ctx, req.PermissionContext)
	}
```

- [ ] **Step 2: 修改 ProcessMessageImpl deferred cleanup（~line 739）**

将：
```go
			// ⤵️ 10.1.8: TOOL_PERMISSION_CHANNEL_ID.reset(token_cid); cleanup_permission_context(token_perm)
			// ⤵️ 11.10: _reset_runtime_cron_context(cron_context_tokens)
```

改为：
```go
			// Go context 不可变值模式，无需 reset/cleanup（Python 需要 reset(token)）
			// ⤵️ 11.10: _reset_runtime_cron_context(cron_context_tokens)
```

- [ ] **Step 3: 修改 ProcessMessageStreamImpl 中的权限上下文设置（~line 842）**

将：
```go
	// 步骤 11-12: 权限上下文设置
	// ⤵️ 10.1.8: token_cid = TOOL_PERMISSION_CHANNEL_ID.set(...); token_perm = setup_permission_context(request)
```

改为：
```go
	// 步骤 11-12: 权限上下文设置
	// 对齐 Python: TOOL_PERMISSION_CHANNEL_ID.set(channel_id) + setup_permission_context(request)
	ctx = schema.WithToolPermissionChannelID(ctx, req.ChannelID)
	if req.PermissionContext != nil {
		ctx = schema.WithPermissionContextValue(ctx, req.PermissionContext)
	}
```

- [ ] **Step 4: 确认 deep_adapter.go 已导入 schema 包**

检查 `import` 块中是否有 `schema "xxx/internal/swarm/schema"` 导入。如果没有，需要添加。deep_adapter.go 通常已有此导入，因为 `AgentRequest` 等类型来自 schema 包。

- [ ] **Step 5: 验证编译**

Run: `cd /home/opensource/uap-claw-go && export GOPROXY=https://goproxy.cn,direct && go build ./internal/swarm/server/...`
Expected: 编译成功

---

### Task 9: 清理 handle_envelope.go 中过时的 ⤵️ 注释

**Files:**
- Modify: `internal/swarm/server/handle_envelope.go`

- [ ] **Step 1: 修改 line 808 — injectACPCapabilities 注释**

将 `// ⤵️ 10.3.12: 补充 agent_manager.get_client_capabilities("acp") fallback` 改为 `// agent_manager.get_client_capabilities("acp") fallback 已实现（line 823-828）`

- [ ] **Step 2: 验证编译**

Run: `cd /home/opensource/uap-claw-go && export GOPROXY=https://goproxy.cn,direct && go build ./internal/swarm/server/...`
Expected: 编译成功

---

### Task 10: 全量编译验证与提交

**Files:**
- All modified files

- [ ] **Step 1: 全量编译**

Run: `cd /home/opensource/uap-claw-go && export GOPROXY=https://goproxy.cn,direct && go build ./...`
Expected: 编译成功

- [ ] **Step 2: 运行 schema 包测试**

Run: `cd /home/opensource/uap-claw-go && export GOPROXY=https://goproxy.cn,direct && go test ./internal/swarm/schema/... -v`
Expected: 全部 PASS

- [ ] **Step 3: 运行 server 包测试**

Run: `cd /home/opensource/uap-claw-go && export GOPROXY=https://goproxy.cn,direct && go test -tags=test ./internal/swarm/server/... -v -count=1`
Expected: 全部 PASS

- [ ] **Step 4: 提交**

```bash
git add internal/swarm/schema/permission.go internal/swarm/schema/permission_test.go internal/swarm/server/adapter/deep_adapter.go internal/swarm/server/adapter/deep_adapter_tools.go internal/swarm/server/adapter/code_adapter.go internal/swarm/server/runtime/agent_config.go internal/swarm/server/handle_envelope.go
git commit -m "fix(swarm/server): 清理过时⤵️注释、修正指向9.38-49→10.6.24、回填PermissionContext链路

三类修改：
1. 清理10处过时⤵️/⤴️注释（功能已实现但标记未清理）
2. 修正22处⤵️9.38-49→⤵️10.6.24（wiki/搜索/小艺等工具属Swarm内置工具集）
3. 回填PermissionContext链路：新增WithToolPermissionChannelID/WithPermissionContextValue
   等context工具函数，在ProcessMessageImpl/ProcessMessageStreamImpl中注入权限上下文
   对齐Python TOOL_PERMISSION_CHANNEL_ID.set() + setup_permission_context()"
```

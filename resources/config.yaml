# UapClaw 配置文件
# 本文件为最小配置骨架，随领域实现逐步扩展

# 服务配置
server:
  # AgentServer 配置
  agentserver:
    host: "0.0.0.0"
    port: 8765
  # Gateway 配置
  gateway:
    host: "0.0.0.0"
    port: 8766

# 日志配置
logging:
  level: "info"
  format: "json"

# 工作区配置
workspace:
  path: "~/.uapclaw"
